# Vízellátó rendszer felügyelet – követelmények

## Scope

ESP32/ESPHome alapú felügyeleti rendszer az aknában lévő vízellátó rendszerhez. A Home Assistant biztosítja a historizálást, statisztikát és megjelenítést, de az ESP működése nem függhet a HA vagy a hálózat elérhetőségétől.

## Impulzusszámláló modul

### Hardver

- Impulzusadó: **IZAR PULSE i, 4-wire 5m** (Diehl Metering, P/N 3108333), induktív érzékelésű, **open collector** kimenet, saját lítium elemmel (a jeladó nem igényel tápellátást az ESP-től).
  - Dokumentáció: [`docs/hardver/izar-pulse-i-adatlap.pdf`](docs/hardver/izar-pulse-i-adatlap.pdf), [`izar-pulse-i-telepitesi-utmutato.pdf`](docs/hardver/izar-pulse-i-telepitesi-utmutato.pdf).
  - **A beszerzett egységen ellenőrzött bekötés** (a konkrét darab címkéje alapján, 3-kimenetes, egyidejűleg 3 eltérő felbontású jelet ad):
    - fehér = impulzus, `RATIO 1:1` → 1 impulzus = 1 liter
    - sárga = impulzus, `RATIO 10:1` → 1 impulzus = 10 liter
    - zöld = impulzus, `RATIO 100:1` → 1 impulzus = 100 liter
    - barna = föld (közös GND)
  - Csak a **fehér (1:1) vezetéket** kötjük az ESP GPIO-jára a legjobb felbontásért, + barna GND. Sárga/zöld nem kerül bekötésre.
  - Kimenet nyitott kollektoros → ESP oldali pull-up szükséges.
  - `liters_per_pulse = 1` (a fehér vezetékkel), a paraméter így is konfigurálható marad.
  - Max. impulzusfrekvencia: 8 Hz, impulzushossz: 50–500 ms.
  - Kompatibilis Diehl/MOM óracsalád (a gyártói lista kifejezetten tartalmazza a **Corona M**-et).
- Vízóra: **MOM Corona D3 1"**.
- ESP board: **Waveshare ESP32-S3-RS485-CAN**, izolált RS485/CAN, DIN-sínes – **megérkezett, 2026-08-13**, a `water-collector.yaml` `substitutions`-ai (`board_type`, pulzus-GPIO-k) mostantól közvetlenül erre céloznak (a korábbi, nem-S3 tesztboard értékei kikommentezve maradtak referenciaként). Dokumentáció: [`docs/hardver/esp32-s3-rs485-can-board.md`](docs/hardver/esp32-s3-rs485-can-board.md) (+ eredeti PDF ugyanott). A jeladók a board SH1.0 csatlakozójára kerülnek: `1. vízmérő → GPIO1`, `2. vízmérő → GPIO2` (`meter1`/`meter2`, lásd Kezdeti implementációs döntések – melyik fizikai mérő melyik, az telepítésfüggő, nincs a kódba égetve).
- Két mérési pont, azonos modulstruktúrával, egymástól függetlenül. A jelen telepítésben (nem a firmware része, csak a konkrét beállítás):
  - **1. vízmérő ("Fő")** – teljes fogyasztás (ház + kert).
  - **2. vízmérő ("Locsoló")** – csak a kerti vízellátás (részhalmaza az elsőnek).
  - A ház-only fogyasztás (1. − 2.) HA oldali származtatott szenzorral számolható, nem ESP feladat.

### Funkcionális követelmények

- Minden érvényes impulzust pontosan egyszer számoljon.
- Aktuális térfogatáram: `L/min`. Összesített fogyasztás: `m³`.
- Diagnosztika: összes impulzusszám, impulzusráta.
- Kézi óraállás-szinkron vízóránként független, elérhető Home Assistantból és az ESP saját webes felületéről is.
- Szinkron azonnal perzisztálódik, nem várja meg a checkpointot.

### Elszámolási modell

Vízóránként két perzisztens állapot:

- `pulse_count` – normál futás közben monoton növekvő impulzusszám (soha nem módosul szinkronnál); tápvesztés utáni restore esetén legfeljebb egy checkpoint-időszaknyit visszaállhat (lásd Perzisztencia és hibakezelés).
- `offset_m3` – korrekciós eltolás.

```
Összes fogyasztás [m³] = offset_m3 + pulse_count × liters_per_pulse / 1000
```

Kézi szinkron a fizikai óra `célérték`-jére:

```
offset_m3 := célérték − pulse_count × liters_per_pulse / 1000
```

Ezzel a diagnosztikai impulzusszám törésmentes marad, a fogyasztás pedig azonnal a megadott értékre áll, és a rendszer minden mutatott adata konzisztens.

A kézi szinkron az `Összes fogyasztás`-t **lefelé is** mozgathatja (ha a fizikai óra állása kisebb, mint amit az ESP addig mutatott) – ilyenkor ugyanaz a HA `total_increasing` statisztikai következmény jelentkezik, mint a tápvesztés utáni visszaugrásnál, csak itt szándékosan. A kézi szinkron ritka, karbantartási jellegű esemény; ha a HA hosszú távú statisztikáját ez érzékenyen érinti, azt szükség esetén kézzel kell korrigálni – emiatt nem indokolt az ESP adatmodelljét bonyolítani.

### Pillanatnyi átfolyás számítása

- A Térfogatáram és az Impulzusráta **egymást követő impulzusok közti időből** (periódusidő) származik, nem fix időablakos impulzusszámlálásból – ez a fix időablakos számlálásnál jobb felbontást és reakciót ad alacsony átfolyásnál. A kettő ugyanabból a mérésből számolt, egymással konzisztens (ugyanaz az arány köztük, mint `liters_per_pulse`).
- **Nulla-átfolyás timeout**: ha egy vízórán X másodpercig nem érkezik új impulzus, a Térfogatáram/Impulzusráta explicit 0-ra áll (különben az utolsó periódusidőből számolt érték érvénytelenül "befagyva" maradna).
  - Alapértelmezett: `60 s`, vízóránként konfigurálható – futásidőben állítható, alacsony szintű paraméter, elsősorban az ESP saját webes felületéről, opcionálisan HA-ból is (nem fordításidőben beégetett érték).
  - A korábban javasolt `15 s` túl agresszívnak bizonyult: 1 l/impulzus mellett már egy hétköznapi, nem is lassú folyás (2–5 L/perc, pl. kézmosás) impulzusköze is 12–30 mp – ennél rövidebb timeout ezt is szaggatottá tenné. `60 s` a jóval lassabb, de még hétköznapi (~1 L/perc, csordogálás) folyást is villogás nélkül lefedi.
  - Kompromisszum: rövidebb timeout → gyorsabb, pontosabb "elzárva" jelzés, de tartós alacsony átfolyásnál (ahol az impulzusköz meghaladja a timeoutot) a kijelzett érték szaggatottan 0 és a tényleges ráta közt ugrál. Hosszabb timeout → simább alacsony átfolyás, de lassabb "elzárva" jelzés valódi leálláskor.
  - Ez csak a pillanatnyi Térfogatáram/Impulzusráta kijelzést érinti; az Összes fogyasztás (`pulse_count` alapú) ettől függetlenül pontos marad.

### Perzisztencia és hibakezelés

- Az ESP az elsődleges adatforrás; a működés nem függhet HA/MQTT/API/Wi-Fi elérhetőségétől.
- `pulse_count` RAM-ban él, periodikusan checkpointként kerül NVS-be. Az `offset_m3` **nem** része a periodikus checkpointnak – csak szinkronkor változik, és akkor azonnal, önálló írással perzisztálódik, nincs mit rajta rendszeresen menteni.
- Checkpoint időköz **fix `60 s`, fordítási időben rögzített** érték (nem futásidőben állítható, sem HA-ból, sem ESP webről) – az ESPHome `web_server` ugyanazokat az entitásokat adná ki az API-n (HA) keresztül is, mint a helyi weboldalon, tehát a "csak ESP-n állítható" elkülönítés egyedi komponenst igényelne. Az egyszerűség kedvéért ezt nem vállaljuk be.
  - A 60 s jó kompromisszum az adatvesztési ablak (tápvesztéskor legfeljebb ennyi impulzus veszhet el) és a flash-terhelés között. A tényleges fizikai írásszám ennél kevesebb is lehet, mert csak akkor kell írni, ha a `pulse_count` valóban változott az adott időszakban (lásd Kezdeti implementációs döntések) – nincs fogyasztás, nincs felesleges írás.
  - Ha később mégis más érték kellene, az egy YAML/`substitutions` módosítás + újraflashelés, nem éles konfigurációs paraméter.
- Kézi szinkron (`offset_m3`) mindig azonnal perzisztál, checkpointtól függetlenül.
- Újraindítás után a számlálás az utolsó perzisztált `pulse_count`/`offset_m3` alapján folytatódik.
  - Tápvesztéskor legfeljebb egy checkpoint-nyi impulzus veszhet el, emiatt újraindítás után az `Összes fogyasztás` kis mértékben visszaugorhat. Ez elfogadott mellékhatása a checkpoint-alapú perzisztenciának. A HA `total_increasing` szenzortípus alapvetően monoton növekvő értéket vár; a csökkenést bizonyos esetekben resetként kezeli, de ennek pontos viselkedése (pl. a kis mértékű csökkenésekre vonatkozó tolerancia) HA-verziófüggő és nem garantált – a hosszú távú statisztikák helyes működését implementáció közben külön ellenőrizni kell.
- Első implementáció nem igényel külső FRAM-ot vagy más kiegészítő nem felejtő memóriát.

### Hálózat és biztonság

- **Wi-Fi SSID/jelszó soha nem kerül fordítási időbe** (se `secrets.yaml`-ba, se máshova a firmware-t generáló repóban) – az eszköz Wi-Fi hitelesítő adat nélkül bootol, azt kizárólag futásidőben, az első indításkor kapja meg Bluetooth (BLE improv) vagy Wi-Fi hotspot (AP + captive portal) útján, és az ESP a saját flash-ébe menti (nem a build tárolja).
- Egyéb, eszköz-specifikus secretek (API titkosítási kulcs, OTA jelszó) ESPHome `secrets.yaml`-ban, verziókezelésből kizárva – ezeknek nincs futásidejű provisioning megfelelőjük az ESPHome-ban, de nem hálózati hitelesítők, nem szivárogtatnak semmit az otthoni hálózatról.
- Az ESP saját webes felülete (dashboard) **nem kér jelszót** – tisztán helyi hálózaton elérhető, iOS-en kezdőképernyőre kitehető appként is használt felület, aminek egy bejelentkezési képernyő csak zavaró súrlódás lenne, valódi biztonsági nyereség nélkül (aki már rajta van az otthoni Wi-Fi-n, úgyis eléri). Tudatosan vállalt kompromisszum – ha valaha távolról/nem megbízható hálózatról is elérhető lenne az eszköz, ezt felül kell vizsgálni.

### Home Assistant adatmodell

Vízóránként (a jelen telepítésben: 1. vízmérő/"Fő", 2. vízmérő/"Locsoló" – lásd Scope):

Üzemi entitások:

- `Térfogatáram` – `L/min`, `state_class: measurement`
- `Összes fogyasztás` – `m³`, `device_class: water`, `state_class: total_increasing`

Diagnosztikai entitások:

- `Impulzusráta` – `impulzus/min`, `state_class: measurement`
- `Összes impulzus` – `impulzus`, state class nélkül (nincs szükség rá HA-statisztikában – arra az `Összes fogyasztás` szolgál –, és ugyanúgy visszaugorhatna tápvesztéskor, mint a `pulse_count`)

Beállító/szerviz entitások:

- fizikai vízóra állásának megadása (`m³`) + szinkronizálás
- nulla-átfolyás timeout (`number`, s)

Checkpoint időköz nem entitás, fix `60 s`, fordítási időben rögzítve (lásd Perzisztencia és hibakezelés).

### Idősoros adatok és statisztikák

- Az ESP nem tárol hosszú távú idősoros adatot; ez, valamint az aggregáció (óra/nap/hét/hónap) HA feladata.
- Térfogatáram és összesített fogyasztás legyen historizált a HA-ban.
- HA kiesése alatt az ESP folytatja a számlálást; a részletes térfogatáram-idősor megőrzése ESP oldalon nem követelmény.
- Visszakapcsolódáskor az ESP aktuális abszolút `pulse_count`/`offset_m3` alapú értéke az irányadó.

### Kalibráció

- Az impulzus–térfogat átváltás (`liters_per_pulse`) vízóránként konfigurálható.
- Az impulzusbemenet pergésmentesítése konfigurálható (ESPHome `pulse_meter` `internal_filter` paramétere, nem külön logika).

**Javasolt kiegészítés (még nem eldöntött)**: `liters_per_pulse` jelenleg fordításidőben rögzített (`substitutions`) – tegyük futásidőben is állíthatóvá (pl. `number` entitás, hasonlóan a Nulla-átfolyás timeout-hoz), hogy egy más arányú vezeték bekötése (pl. a fehér 1:1 helyett sárga 10:1/zöld 100:1, lásd Hardver) vagy más gyártmányú impulzusadó ne igényeljen újraflashelést. Kockázat, amit kezelni kell: mivel `Összes fogyasztás = offset_m3 + pulse_count × liters_per_pulse / 1000`, egy futásidejű váltás a már felhalmozott `pulse_count`-ra visszamenőleg hatna – a váltáskor az `offset_m3`-at automatikusan újra kell számolni úgy, hogy az `Összes fogyasztás` ne ugorjon (hasonlóan a kézi szinkronhoz, csak automatikusan, a régi/új arány különbségére).

## Nyomásmérő modul

### Hardver

- Érzékelő: **QDW90A** (Anhui Qidian Automation Technology), diffúz szilícium piezorezisztív nyomás-távadó. Gyártói adatlap: [`docs/hardver/qdw90a-nyomastavado-adatlap.pdf`](docs/hardver/qdw90a-nyomastavado-adatlap.pdf), gyártói hivatalos Modbus-protokoll doksi: [`docs/hardver/qdw90a-modbus-protocol-gyartoi.pdf`](docs/hardver/qdw90a-modbus-protocol-gyartoi.pdf). **Modbus-regiszterek, bekötés, betanítás – saját, hardveren megerősített referencia**: [`docs/hardver/qdw90a-modbus-referencia.md`](docs/hardver/qdw90a-modbus-referencia.md).
  - **A beszerzett/tervezett kivitel**: RS485/Modbus RTU kimenet (az adatlap alapváltozata 4-20mA áramhurok, de a Modbus-kimenet is dokumentált kivitel, 4-vezetékes bekötéssel – **ezt a variánst rendeljük**), táp `24V DC`, mérési tartomány `0–10 bar` (`0–1.0 MPa`).
  - `G1/4` menetes csatlakozás, pontosság `±0.2% F.S.`, védettség `IP65`.
  - Kommunikáció a **Waveshare ESP32-S3-RS485-CAN** board saját, galvanikusan leválasztott RS485 illesztőjén keresztül (lásd [`esp32-s3-rs485-can-board.md`](docs/hardver/esp32-s3-rs485-can-board.md), `GPIO17` TX / `GPIO18` RX / `GPIO21` EN – ESPHome `uart: flow_control_pin` a hardveres RS485 fél-duplex módhoz) – **implementálva, 2026-08-13** (ld. Architekturális megfontolás v3 – Státusz), hardveres tesztelés még hátra van.
  - Darabszám: kezdetben **3 mérési pont** tervezve. Melyik fizikai pont mit mér (pl. bemenet/kimenet/szűrő előtt-után), az telepítésfüggő – ugyanúgy, mint a vízmérőknél, ez nem kerül a kódba égetve, futásidőben (Display Name) nevesíthető.
- **RS485 hub: CDEBYTE E810-R14** (1→4 irányú, opto-izolált RS485 repeater/hub) – a telepítés fizikai adottságai miatt a szenzorok nem köthetők egy szál kábelre daisy-chain-nel, csak csillag-topológiában; ez az eszköz oldja meg a csillag-kábelezést anélkül, hogy protokoll-szinten bármit is módosítana. Hivatalos user manual (letöltve, ellenőrzött forrás): [`docs/hardver/e810-r1x-rs485-hub-user-manual.pdf`](docs/hardver/e810-r1x-rs485-hub-user-manual.pdf).
  - **Fontos**: teljesen protokoll-átlátszó ("no configuration required, transparent data transmission between master and slave interfaces") – nem multiplexer, nem ismeri a Modbust. Pontosabban (a manual szerint): a host felőli adat egyszerre, broadcast-szerűen jut el mind a 4 kimeneti csatornára; a csatornák felől visszafelé jövő adatot a hub time-sharing módon (egyszerre csak egy csatornáról) továbbítja a host felé – ütközés nélkül, de a Modbus szintjén ez láthatatlan/irreleváns. A ráakasztott szenzoroknak emiatt továbbra is **egyedi Modbus-címre van szükségük** az egész buszon (ld. lent, "betanítás"), ez a hub bevezetésével nem változik.
  - **Csillag-kábelezés tisztázása**: a manual saját "Wiring Precautions" fejezete általános RS485-alapelvként "kerüld a csillag-topológiát, használj hand-in-hand (daisy-chain) bekötést" – ez viszont a **klasszikus, elágazásokkal teli busz** eseté vonatkozik (jel-visszaverődés/impedancia-illesztési probléma elágazási pontoknál). A mi terveink szerint **minden egyes hub-csatornára pontosan 1 szenzor** kerül (nem több szenzor elágaztatva egy csatornáról) – ez csatornánként egy tiszta, elágazás nélküli pont-pont összeköttetés, ami topológiailag **nem esik a "kerülendő csillag" kategóriába**, sőt jel-integritás szempontjából legalább annyira jó, mint egy daisy-chain. A hub saját csatlakozási rajzai (3. fejezet) kifejezetten mutatnak is egy hosztról induló, több szlévre ágazó elrendezést mint támogatott használati módot.
  - Saját táp: `DC 9–40V`, a busztól galvanikusan leválasztva (`1.5kV` izoláció, teljesen külön a jelföldtől – "power ground, not interoperable with digital signal ground"). A `24V DC`, ami a szenzoroknak amúgy is kell, ezen a tartományon belül van, tehát **valószínűleg ugyanarról a tápforrásról üzemeltethető**, csak külön föld-vezetékkel a hub saját táp-GND-jéhez. Van saját PE (védőföld) csatlakozója is, azt kösd földre.
  - Baud tartomány `300–230400` (egyéni baud is támogatott) – a szenzor `1200–115200` tartományát bőven lefedi. A busz össz-eszközszám korlátja `32` – ez az általunk tervezett 4 slot-nak bőven elég tartalékot ad.
  - Gyártói infó: [cdebyte.com/products/E810-R14](https://www.cdebyte.com/products/E810-R14).
  - **Miért kell a hub akkor is, ha a master board (ESP32-S3-RS485-CAN) RS485 illesztője már eleve izolált, és a kábelek csak pár méteresek?** Mert a master-oldali izoláció a masztert védi a buszról, nem a szenzorokat egymástól – hub nélkül csillagba kötve mind ugyanazon a közös, nem izolált buszszakaszon lennének, így egy eltérő földpotenciálú (más gépházban lévő) szenzor közös módusú feszültsége simán túlmehetne a driver tartományán. A hub csatornánként külön izolál, ez oldja meg ezt – a topológia (csillag vs. daisy-chain) önmagában, pár méteres kábellel és `9600` baudon valószínűleg amúgy sem okozna gondot.
- **Modbus regisztertérkép** – ⚠️ eredetileg nem hivatalos forrásból (HA közösségi fórum) állt össze, **2026-08-12-én kibővítve** egy sokkal részletesebb, szintén nem hivatalos, de alaposabb forrással (TapHome QDW90A kompatibilitási oldala, nyers jegyzet: [`docs/hardver/qdw90a-modbus-taphome-jegyzet.md`](docs/hardver/qdw90a-modbus-taphome-jegyzet.md) – munkaanyag, nem végleges referencia), **majd ugyanazon a napon egy teljes regiszter-scan-nel (`mbpoll`, `H:0–H:39`) a valós hardveren végigmérve**. Az alábbi táblázat **minden dokumentált regisztere hardveren megerősített**:

  | Reg. | Funkció | Mért érték | Állapot |
  | --- | --- | --- | --- |
  | `H:0` | Slave cím (`1–255`) | `1` | ✅ hardveren megerősítve |
  | `H:1` | Átviteli sebesség-kód (`0`=1200 … `7`=115200) | `3` = 9600 | ✅ hardveren megerősítve |
  | `H:2` | Mértékegység-kód (`3`=bar, teljes lista a TapHome-jegyzetben) | `3` = bar | ✅ hardveren megerősítve |
  | `H:3` | Tizedesjegyek száma – `érték = H:4 / 10^H:3` | `2` | ✅ hardveren megerősítve (tehát ténylegesen `/100`) |
  | `H:4` | Mérési érték, `Int16`, skálázás `H:3` szerint | `0` (terheletlen) | ✅ hardveren megerősítve, élőben reagál valós nyomásra (ld. lent) |
  | `H:5` | Méréstartomány alsó pontja | `0` | ✅ hardveren megerősítve |
  | `H:6` | Méréstartomány felső pontja | `1000` → `/100`-zal **10.00 bar** – egyezik a szenzor `0–10 bar` méréshatárával | ✅ hardveren megerősítve, és értelmes is |
  | `H:12` | Nullponteltolás, `Int16`, írható | `0` (gyári alapállapot) | ✅ hardveren megerősítve |
  | `H:15` | Beállítás mentése – `0` írása menti tartósan a `H:0`/`H:1`/stb. módosítását (**enélkül a változtatás elveszhet újraindításkor!**) | `0` (olvasva) | ✅ olvasva, ahogy vártuk – **írás még nincs kipróbálva** |
  | `H:16` | Gyári visszaállítás – `1` írása töröl mindent (cím, kalibráció is) | `0` (olvasva, **nem lett kiváltva**) | ✅ biztonságosan csak olvasva – **írást szándékosan nem próbáltunk ki** |
  | `H:22–H:23` | Mérési érték közvetlenül IEEE754 Float32-ként, big-endian/ABCD | `0.0` | ✅ hardveren megerősítve, egyezik a `H:4`-alapú olvasással – **firmware-hez ígéretes alternatíva** a `H:4`+`H:3` kombináció helyett |
  | `H:37` | Soros paritás (`0`=nincs, `1`=páratlan, `2`=páros) | `0` = nincs | ✅ hardveren megerősítve |

  **A TapHome-jegyzetben nem szereplő, de a teljes scan során talált, valószínűsíthetően valódi paraméterblokk** (`H:22`-től kezdve 2-2 regiszteres Float32 párok, "kerek" értékekkel – ez nem véletlen mintázat):

  | Regiszterpár | Float32 érték | Jelentés |
  | --- | --- | --- |
  | `H:24–H:25` | `0.0` | ismeretlen, illik a mintázatba |
  | `H:26–H:27` | `1.0` | feltételezés: kalibrációs szorzó/gain, jelenleg semleges értéken – **nincs megerősítve** |
  | `H:28–H:29` | `0.001` | feltételezés: felbontás/lépésköz paraméter – **nincs megerősítve** |

  **Egyéb, nem dokumentált, nem nulla regiszterek** a `H:0–H:39` scanből, jelentésük ismeretlen, firmware-ben nem használjuk őket: `H:8=2`, `H:9=-174`, `H:10=-180`, `H:11=13017`, `H:14=82`, `H:21=8`, `H:33=907`, `H:39=-21829`. (A `H:20` a nagy scan-ből valóban kimaradt, de **nem hiba/kihagyás volt** – ld. lent, "kérésméret-korlát", `H:20` maga rendben olvasható, `0` értékkel.)

  **⚠️ Kérésméret-korlát, firmware-releváns**: **egy `0x03` kérésben legfeljebb 20 regiszter kérhető le egyszerre** – 21 vagy több regiszterre a szenzor a teljes kérést elutasítja, a szabványostól eltérő `Illegal Function` kivétellel (nem a várt "Illegal Data Address"-szel). Reprodukálva: `H:1–H:20` (20 db, `-r 1 -c 20`) sikeres, `H:0–H:20` (21 db, `-r 0 -c 21`) teljes hiba. Ez magyarázza a korábbi, `H:0–H:39` scan során tapasztalt "kimaradt `H:20`" jelenséget is (az a kérés minden bizonnyal 20-nál több regisztert próbált egyben lekérni, és valahogy csak részlegesen jelent meg az eredmény). **A firmware-es Modbus-olvasást ennek megfelelően kell darabolni** (max 20 regiszter/kérés), ld. részletesen: [`docs/hardver/qdw90a-modbus-referencia.md`](docs/hardver/qdw90a-modbus-referencia.md).

  - **Slave cím (`H:0`) – ✅ megerősítve valós QDW90A egységen**, 2026-08-12, kézi Modbus RTU kerettel (FTDI USB-RS485 adapter, `9600 8N1`, közvetlenül a szenzorra kötve, hub nélkül): `01 03 00 00 00 01 84 0A` kérésre `01 03 02 00 01 79 84` érkezett vissza (érték `0x0001` = `1`). A bekötés a gyári kábelszínezés szerint `Piros=24V+`, `Fekete=24V-`, `Kék=PC-A`, `Sárga=PC-B` (a "Ground Wire" felirat a csatlakozó silkscreen-jén félrevezető ezen a kivitelen – nincs külön föld-ér, csak a táp `Fekete` vezetéke). Külön GND-vezeték az FTDI és a szenzor közt **nem** kellett hozzá (rövid, asztali teszt).
  - **Nyomásérték (`H:4`) – ✅ regiszter és skálázás is teljesen megerősítve**: nyugalomban `0` (várt érték terheletlen/légköri állapotban), majd a nyomáscsatlakozóba szájjal belefújva (a tényleges mérési úton, nem mechanikai membrán-nyomogatással) folyamatosan, monoton nőtt: `0 → 3 → 4 → 5` nyers érték (mindegyik CRC-vel ellenőrizve, valódi válasz). A `H:2`/`H:3` regiszterek (`mbpoll`-lal) külön kiolvasva: `H:2 = 3` (bar) és `H:3 = 2` (2 tizedesjegy) – tehát a `0 → 3 → 4 → 5` nyers érték ténylegesen `0.00 → 0.03 → 0.04 → 0.05 bar`-t jelent (`érték = H:4 / 100`), pontosan egyezve a korábbi feltevésünkkel és az emberi fújásnyomás (`~0.01–0.05 bar`) várt tartományával. **A `H:4`-ből számolt `bar` érték firmware-kódba építhető, nincs többé feltételezés benne.**

### Funkcionális követelmények

- A dashboard "Home" oldalán megjeleníti az aktuális nyomásértéket (`bar`), a vízmérők kártyáihoz hasonló elrendezésben.
- Minden nyomásmérő pont átnevezhető (Display Name), futásidőben – ugyanaz a minta, mint a vízmérőknél.
- **Szerviz/"betanítás" folyamat**: a Modbus slave cím (és opcionálisan a baud rate, ha a gyári alapértelmezett nem egyezik egységeknél) az ESP saját webes felületéről, futásidőben állítható/módosítható, méterenként – ez a mechanizmus teszi lehetővé, hogy 3 azonos gyári címről induló egységet egy közös buszon egyedileg meg lehessen különböztetni felszereléskor, újraflashelés nélkül.
- **Betanítási előfeltétel**: egyszerre csak **1, még gyári alapcímen lévő** eszköz lehet a buszon – ha több egyforma című egység van fent egyszerre, mindegyik válaszolna a lekérdezésre, a cím nem állítható be egyértelműen. Gyakorlatban: az új szenzort a hub adott csatornáján egyedül (a többi lekötve, vagy még be sem kötve) kell címezni, csak utána köthető rá a közös buszra a többihez.

### Architekturális megfontolás: "dinamikus" nyomásmérő-hozzáadás

- Az ESPHome **fordításidőben rögzített, statikus entitásmodellt** használ – nincs natív támogatás arra, hogy futásidőben, újraflashelés nélkül vadonatúj entitás (pl. egy negyedik nyomásmérő) jöjjön létre a semmiből.
- **Javasolt kompromisszum** (a vízmérő-modul mintájára, ott is bevált): fix számú, előre bekötött "slot" – **4 db** (döntés, felülírva a korábbi 8-as tervet: pontosan annyi, ahány fizikai portja van a tervezett E810-R14 hubnak, tartalék nélkül – ha a jövőben egy második hub vagy több szenzor kerülne a rendszerbe, ez a szám firmware-oldali bővítést és újraflashelést igényelne, ez elfogadott kompromisszum). Mindegyik slot:
  - alapból inaktív/rejtett a dashboard "Home" oldalán, amíg nincs hozzá betanított (Modbus-címmel ellátott) szenzor,
  - saját, futásidőben állítható Modbus slave címmel (a fenti "betanítás"),
  - saját Display Name-mel.
- **Konfigurációs UX (v2, implementálva)**: nem egyszerű be/kikapcsoló kapcsoló, hanem **Hozzáadás/Törlés**, kompakt táblázat (Név | Cím oszlopok) a Service oldalon:
  - alapból **egyetlen betanított slot sem látszik** sem a Home, sem a Service oldalon – csak egy "+" gomb, ami egy névbekérő mezőt nyit ("Pressure Sensors New Sensor Name" + "Pressure Sensors Add" entitások, `water-collector.yaml`),
  - "Hozzáadás"-ra a soron következő szabad slot (`packages/pressure_sensor.yaml`, `try_commission` script) felveszi a megadott nevet, és **onnantól** látszik a táblázatban és a Home kártyák közt is,
  - "Törlés"-re (a meglévő JS `confirm()` visszakérdezéssel, `CONFIRM_ON_PRESS`) a slot adatai visszaállnak alapállapotba, és **teljesen eltűnik** mindkét helyről,
  - a slot sorszáma (melyik fizikai slot-ID kapja a nevet) nem jelenik meg sehol, csak a Display Name – ez szándékos, ld. `pressure_sensor.yaml` fejléce.
- **Fontos technikai tanulság, ami a végleges tervet meghatározta**: az első próbálkozás a fenti "csak akkor létezzen, ha be van tanítva" hatást az ESPHome `internal` flag-jének **futásidejű** átkapcsolásával (`set_internal()`) próbálta elérni. A ténylegesen telepített ESPHome forrásában (`esphome/core/entity_base.h`) ellenőrizve kiderült: ez **hivatalosan deprecated, "undefined behavior"** 2026.3.0 óta ("Components and Home Assistant are NOT notified... Use the 'internal:' YAML key instead"), 2027.3.0-ban törlik – tehát erre nem lehet architektúrát építeni. **A végleges megoldás**: minden entitás mindig létezik (soha nem `internal`), a "betanítva van-e" állapotot egy sima, mindig küldött `binary_sensor` ("Commissioned") **értéke** jelzi, amit a dashboard saját JS-e (`web/dashboard.js`) értelmez – ez teljesen támogatott, élőben frissül, nem deprecated API-ra épül.
  - **Ennek ára – korlát, amit el kell fogadni**: mivel nincs megbízható, támogatott mód egy entitás valódi eltüntetésére HA elől futásidőben, a **"csak a látható szenzorokat engedjük HA felé" nem valósul meg 100%-osan** – HA mindig látni fogja mind a 4 slot entitásait, csak `disabled_by_default: true`-val, alapból kikapcsolt állapotban (a meglévő konvenció). Ez a gyakorlati plafon ESPHome natív API-val, MQTT discovery-alapú, teljesen más architektúra nélkül.
  - Korábbi, elvetett "kétlépéses gomb-megerősítés" ötlet (JS `confirm()` helyett) szintén okafogyottá vált – a meglévő `CONFIRM_ON_PRESS` JS-mechanizmus (amit már Update/Restart/Forget Wi-Fi is használ) pont erre való, nem kellett hozzá semmi új.
- Ez **nem korlátlan dinamikus bővítés**, hanem "4 előre definiált, egyenként konfigurálható slot" – ez pontosan a jelenlegi vízmérő-modul mintája is (2 fix slot, futásidőben nevesíthető/kapcsolható). Ha a cél valódi, darabszám-korlát nélküli bővíthetőség, az egy jelentősen nagyobb architekturális váltás lenne – erről külön kell dönteni, ha ez ténylegesen cél.
- **Státusz (a fenti, v2 leírás)**: implementálva volt, de a lenti v3-as tervezés **felülírja/kibővíti** – a v2 "+" folyamata (üres névmező, cím utólag kézzel) helyébe a scan-alapú, cím-előre-ismert hozzáadás lép. Újraírás szükséges, még nincs implementálva.

### Architekturális megfontolás v3 (2026-08-13): scan-alapú, "dinamikus DB-nek tűnő" tábla

Hosszabb tervezési beszélgetés eredménye – a cél egy olyan felület, ami úgy **viselkedik**, mintha egy tetszőleges méretű, dinamikus adatbázis-táblát mutatna (sorok jönnek-mennek, nincs látható "slot 3/8" sorszámozás), miközben a valóságban **8 fix, fordításidőben rögzített slot** van mögötte (ESPHome-korlát, ld. fent – ezt "elfedni" lehet a felületen, de megszüntetni nem).

- **Döntés: `N` vissza `8`-ra** (a korábbi, "elég a 4" döntés felülírva) – a hub ténylegesen `32` node-ot bír a buszon, a `4` önkényesen a hub fizikai portszámához lett igazítva. A `8` olcsó tartalék, jelentősen csökkenti annak esélyét, hogy a plafon ténylegesen elő is jöjjön.
- **Szigorú szétválasztás: perzisztens vs. élő/efemer adat** – ez a kulcs-felismerés, ami az egész tervet meghatározza:
  - **Perzisztens, slotonként**: kizárólag **cím + Display Name**. Semmi más (nincs külön "betanítva" flag – ld. lent, ez már a címből következik).
  - **Élő/efemer, soha nem tárolt, mindig frissülő**: busz-scan jelenléte, folyamatos pollozás online/offline állapota (`modbus_controller` `on_online`/`on_offline`, ld. Diagnosztika) – ezekből **kliens-oldalon (JS) számolt, sosem flash-be írt** állapot.
- **A táblázat sorai egy JOIN eredménye** (busz-scan találatai × a 8 slot jelenlegi címei, cím szerint):
  - **"Regisztrált, elérhető"** – van slotunk erre a címre, ÉS a legutóbbi scan/pollozás válaszolt rá.
  - **"Regisztrált, nem elérhető"** (LOST/ERROR) – van slotunk erre a címre, de a legutóbbi scan/pollozás **nem** kapott választ – ez pontosan az eredeti "3 eszköz van bekötve, csak 2 válaszol" jelzés, a JOIN természetes mellékterméke, nem külön funkció.
  - **"Új eszköz"** – a scan talált egy választó címet, de egyik slotunk sem regisztrálja – ide kerülnek a még nem betanított eszközök.
- **Hozzáadás – soronként, nem egy közös folyamat**: ha egyszerre több "Új eszköz" sor is látszik, mindegyiknél **saját "Hozzáadás" gomb** van (nem egy általános "+", ami kétértelmű lenne, melyik eszközre vonatkozik). A hozzáadás a scan-ből már ismert címet azonnal felveszi, csak nevet kell hozzá kérni – ezzel a v2-es "előbb név, aztán kézzel beírt cím" folyamata feleslegessé válik.
- **Plafon, őszintén kezelve**: ha mind a 8 slot foglalt, és a scan egy 9. (vagy további) választó, nem regisztrált címet is talál, az a sor **továbbra is megjelenik "Új eszköz"-ként**, de a "Hozzáadás" gombja **inaktív, egyértelmű üzenettel** ("nincs szabad hely – firmware-bővítés kell hozzá") – soha nem egy csendben semmit sem csináló gomb.
- **Cím módosítása regisztrált eszközön**: **ténylegesen újraprogramozza a fizikai eszközt is** (a dokumentált kétlépéses Modbus írás+mentés szekvenciával, ld. `docs/hardver/qdw90a-modbus-referencia.md`, `include/rs485_modbus.h`'s `change_address_and_save()`), nem csak a mi rekordunkat írja át – utána a rekord frissül, hogy kövesse az új címet. Sikertelen újraprogramozás esetén a rekord **akkor is** frissül (a `TemplateNumber` optimista publikálását/perzisztálását `set_action`-ből nem lehet visszagörgetni – ld. `pressure_sensor.yaml` saját megjegyzése), ez elfogadott, önkorrigáló rés: ha a fizikai cím valójában nem változott, a következő pollozás a (tévesen felvett) új címen sikertelen lesz, a slot "Lost" állapotba kerül – ugyanaz a valódi diagnosztikai jelzés, mint egy ténylegesen leszakadt szenzornál.
- **Ez nem egy külön "Sensor Config" oldal** (ahogy korábban felmerült) – egyetlen, egységesített nézet, ami kiváltja a jelenlegi "Pressure Sensors" táblázatot.
- **Státusz**: implementálva, 2026-08-13, **valódi Modbus RTU-val** a végleges Waveshare ESP32-S3-RS485-CAN boardon (`water-collector.yaml`'s `uart: rs485_uart`, protokoll-logika `include/rs485_modbus.h`-ban – saját, kézzel írt Modbus-mester, nem az ESPHome beépített `modbus_controller:`-je, mert az egy fordításidőben rögzített slave-címet feltételez, nekünk viszont slotonként futásidőben állítható cím kell, plusz busz-szintű címscan, amire a beépített komponensnek nincs megfelelője). Lefedett funkciók: valódi busz-scan (`scan_bus()`), slotonkénti folyamatos nyomás-pollozás + élő "Online" állapot (`read_pressure_bar()`, H:22–H:23 Float32), és a fenti valódi cím-újraprogramozás (`change_address_and_save()`).
  - **Hardveren tesztelve, 2026-08-13** – a felhasználó saját HA ESPHome add-on-ján keresztül lefordítva/flashelve, valós QDW90A szenzorral. Menet közben talált és javított hibák:
    - **Watchdog-crash busz-scan alatt** – ld. "Diagnosztika", javítva.
    - **Súlyos hiba a `try_register` kölcsönös-kizárásában**: minden slot csak a *saját* címét ellenőrizte (`== 0`), nem azt, hogy egy másik slot már felvette-e ugyanazt a címet ebben a körben – friss (mind a 8 slot üres) boardon emiatt **mind a 8 slot** felvette ugyanazt a szenzort. Javítás: megosztott, nem perzisztens `modbus_add_claimed` globális flag (`water-collector.yaml` – szándékosan nem `pressure_add_claimed`, mert ez a koordináció bármilyen jövőbeli Modbus eszköztípusra vonatkozik, nem csak a nyomásmérőkre), amit az "Add" gomb minden nyomás előtt visszaállít, és amit az első sikeres `try_register` azonnal `true`-ra állít – így a körön belül tényleg csak egy slot nyerhet.
    - **A táblázat "X" (Delete) gombja hatástalan volt** – a kattintás-kezelő a megosztott `pressButton()` segédfüggvényt hívta, ami az `entity.btnEl`-t várja, de ezt semmi nem állította be a nyomásmérő-táblázat saját renderelési útján (a generikus `upsertServiceButton()`-t, ami máskor ezt tenné, a nyomásmérő-entitások sosem érik el). Kivétel nélkül elnyelt hibát dobott minden kattintásra. Javítva: a táblázat maga állítja be `btnEl`-t minden renderkor.
    - **Az "OK" státusz-jelző gombnak nézett ki** – a korábbi kitöltött, lekerekített "pill" design túl hasonlított a valódi gombokra (pl. "Scan Bus"). Átalakítva egyszerű színes pont + szöveg jelzésre, kattinthatatlan megjelenéssel.
    - **Ütközés-kimutatás** hozzáadva, ld. fent "Ütközés kimutatása" – valós, szándékos 3-eszköz-azonos-cím teszttel megerősítve.
  - **Második kör, valós hardveren, 2026-08-13 (ua. nap, a fenti javítások után)** – további talált/javított problémák:
    - **A "Scanning…" visszajelzés nem tartott a teljes scan-ig** – az eredeti megoldás a gomb saját `fetch()` promise-ának időtartamára támaszkodott, abból a (téves) feltételezésből, hogy az ESPHome `web_server`-je a gomb `on_press:`-ének teljes lefutásáig nyitva tartja a HTTP-választ. Valós hardveren megcáfolva: a `fetch()` szinte azonnal visszatért, jóval a ~6mp-es scan vége előtt. Javítás: egy valódi, élő `binary_sensor` (`modbus_scan_in_progress`), amit a scan eleje/vége explicit publikál – a felület ebből, nem a `fetch()` időzítéséből vezeti le a gomb "Scanning…" állapotát.
    - **Cím-újraprogramozás ellenőrzése tévesen hibásnak jelezte a sikeres átállítást** – a napló szerint mindkét írás (cím + mentés) sikeresen visszaigazolódott, csak a *közvetlenül utána* következő ellenőrző olvasás nem kapott választ (utána a normál pollozás azonnal, megbízhatóan működött az új címen). Valószínű ok: a `H:15=0` mentés a szenzor saját flash-írása, ami alatt pár ms-ig nem válaszol. Javítás: rövid (`50ms`) várakozás a mentés és az ellenőrző olvasás közé, ugyanaz a minta, mint a cím-írás és a mentés között már meglévő `20ms`.
    - **A táblázat Add/X gombja kilógott a sor keretéből** – a korábbi kereteket a `<tr>`-re rakta, ami `border-collapse`-es táblázatban nem biztosan követi megbízhatóan egy `width: 1%` cella tényleges (a gomb miatt szélesebb) méretét. Javítva: a keret minden `<td>`-n saját maga (a szokásos, robusztus minta), nem a `<tr>`-en.
    - **A Cím mező felesleges fel/le nyilai** eltávolítva (CSS-ből, `type="number"` megtartva a mobil numerikus billentyűzet és validáció kedvéért).
    - **Pollozási gyakoriság `5s` → `1s`, timeout `200ms` → `100ms`** – a `200–500ms`-es pollozási intervallum-javaslat a blokkolási matek miatt (8 regisztrált slot, mindegyik akár a teljes timeout-ig blokkolva, ha egyszerre elérhetetlenek) túl szorosnak bizonyult; `1s` + rövidebb timeout kb. 5×-ös reszponzivitás-javulás a korábbi blokkolási kockázat drasztikus növelése nélkül. Finomhangolható tovább, ha szükséges.
  - **Harmadik kör, felhasználói visszajelzésre, 2026-08-13 (ua. nap)**:
    - **Cím-szerkesztés után egy elavult, valójában téves "Új eszköz" sor jelent meg a régi címmel** – az első körben ezt tudatos, el nem fogadott kompromisszumnak jelöltem ("a Scan Results csak pillanatfelvétel"), de jogos ellenvetésre újragondolva: ez nem elfogadható – egy sorból logikailag nem lehet kettő, csak mert a cím megváltozott. **Javítva**: sikeres `change_address_and_save()` után a firmware maga frissíti a "Scan Results" CSV-t (`include/rs485_modbus.h`'s `update_scan_result_address()`) – kiveszi a régi címet, beteszi az újat (amit a visszaolvasás már úgyis megerősített) –, így a szellem-sor nem jöhet létre. Sikertelen újraprogramozásnál nem nyúl hozzá (ott a régi cím elvben tényleg érvényes maradhat).
    - **Pollozási gyakoriság tovább csökkentve, `1s`/`100ms` → slotonként `520–800ms` (40ms lépésekben szórva) / `50ms`** – a felhasználói kérésre ("legyen amilyen rövid csak lehet") újraszámolva: a korábbi közös, egyetlen `1s` érték helyett minden slot **saját, egymástól 40ms-mal eltérő** `update_interval`-t kapott (`water-collector.yaml`) – ezzel a 8 slot pollozása fokozatosan szétcsúszik egymástól (nem ragad örökre egy fázisban, ahogy egyetlen közös érték esetén tenné, mivel mind egyszerre indul boot-kor), csökkentve annak esélyét, hogy egyszerre több slot blokkolja a fő loop-ot. A timeout `50ms`-re csökkent (valós hardveren mért válaszidők ennél jóval gyorsabbak). Legrosszabb eset (mind a 8 egyszerre elérhetetlen): `8×50ms=400ms`, a legrövidebb slot saját `~520ms` ablakán belül.
  - **Negyedik kör, felhasználói visszajelzésre, 2026-08-13 (ua. nap)**:
    - **A nyomásmérő-táblázat blokkja láthatóan szélesebb volt, mint a Service oldal többi kártyája** – egyszerű CSS-elírás volt: `.dc-pressure-group`-nak `480px` max-width-je volt a többi kártya (`.dc-service-group`) `420px`-je helyett. Javítva.
    - **Élő ütközés-kimutatás a folyamatos pollozásban is, nem csak explicit Scan Bus-ra** – valós teszttel feltárt hiányosság: ha egy már regisztrált slot címére utólag (a legutóbbi scan óta) kerül egy második eszköz, a slot addig csak Lost/OK közt villogott, minden magyarázat nélkül, amíg valaki véletlenül újra le nem futtatta a Scan Bus-t. **Javítva**: a folyamatos pollozás (`read_pressure_bar()`) mostantól ugyanazt az "érkezett válasz, csak CRC-hibás" jelzést adja vissza, mint a scan, és ezt egy új `set_scan_collision_address()` segédfüggvénnyel közvetlenül a "Scan Collisions" CSV-be írja/törli minden pollozáskor – a dashboard meglévő JOIN-logikája (ami már eddig is ebből olvasta a regisztrált sorok "Collision?" állapotát) módosítás nélkül azonnal felveszi. Így az ütközés a felfedezés pillanatában látszik, nem csak a következő explicit scan-nél.
  - **Ötödik kör, felhasználói visszajelzésre, 2026-08-13 (ua. nap)**:
    - **Név/cím szerkesztése túl könnyen elsült** – a mezők minden `blur`-re (fókuszvesztésre) azonnal írtak, ami Cím esetén valódi újraprogramozást indított el – túl könnyű volt véletlenül elindítani (pl. Tab-bal továbblépve a soron). **Javítva**: a mezők alapból csak-olvasható (`readOnly`) állapotban vannak; egy soronkénti ceruza gomb kapcsolja szerkeszthetőre, ekkor a ceruza/kuka helyén Mentés (pipa) és Mégse (x) gomb jelenik meg – csak a Mentés írja ténylegesen az eszközt, a Mégse (vagy elnavigálás) eldobja a helyi módosítást (`web/dashboard.js`'s `upsertRegisteredPressureRow()`).
    - **"X" gomb → kuka ikon** – beszédesebb, egyértelműen "törlés"-ként olvasható első pillantásra is (ua. függvény, csak ikoncsere).
    - **Törlés után a cím azonnal, elavult adatból "Új eszköz"-ként visszatért a listába** – a Delete gomb eddig csak a mi rekordunkat törölte (cím→0, név→alapértelmezett), de nem nyúlt a legutóbbi busz-scan "Scan Results" pillanatfelvételéhez, amiben a törölt cím még mindig szerepelt – a dashboard JOIN-ja emiatt azonnal (friss scan nélkül, elavult adatból) újra "Új eszköz" sorként jelenítette meg, ami úgy nézett ki, mintha a törlés soha nem tudná ténylegesen eltávolítani az eszközt a listából. **Javítva**: a Delete gomb `on_press`-e (a cím nullázása *előtt*, amíg a slot még ismeri saját címét) kiveszi a címet a "Scan Results"/"Scan Collisions" CSV-kből is (`include/rs485_modbus.h`'s új `remove_scan_result_address()`, meglévő `set_scan_collision_address()`), így a sor ténylegesen eltűnik, amíg egy tényleges újraszkennelés meg nem erősíti, hogy még mindig válaszol ott valami.
    - **Új: a táblázat sorrendje állítható** (fizikai csőhálózati sorrend leképezéséhez, ne csak a Modbus cím sorrendje számítson) – minden slot kapott egy tisztán a felület saját belső állapotát képviselő, az eszközzel/címmel semmilyen kapcsolatban nem álló "Sort Order" mezőt (`pressure_sensor.yaml`, alapértelmezett `0` = "még nincs testreszabva", ekkor cím szerint rendez, mint eddig). Minden regisztrált soron Fel/Le nyíl gombpár (`web/dashboard.js`'s `movePressureRow()`) cseréli meg a sor pozícióját a szomszédjával – egy nyomás az **összes** regisztrált slot Sort Order értékét egyszerre írja újra (nem csak a két megcserélt sorét), így soha nem maradhat hátra elavult vagy ütköző rangsorszám, bármilyen állapotból induljon is. Törléskor a törölt slot saját Sort Order-je is nullázódik (a többi sorét nem kell újraszámozni – a fennmaradó számok közti "lyuk" a rendezés szempontjából ártalmatlan, csak a relatív sorrend számít), így egy később ugyanabba a slotba kerülő új eszköz a testreszabott sorrend aljáról indul, nem örökli a régi rangsorszámot.
    - **Válasz a felmerült kérdésre: árt-e a szenzornak, ha még gyakrabban pollozzuk?** A pollozás egy sima Modbus-olvasás (H:22–H:23 kiolvasása) – ez **nem ír semmit a szenzor flash-ébe**, tisztán a szenzor RAM-beli/ADC-mérési állapotát olvassa le digitálisan, mechanikai/élettartam-kopás nélkül (szemben a cím-átprogramozással, ami ténylegesen flash-írás – ld. `change_address_and_save()`-nál a `H:15=0` mentési lépés). Tehát a pollozási gyakoriság önmagában **nem élettartam-kérdés** a szenzoron. Amit valóban korlátoz: a fizikailag megosztott, félduplex RS485 busz sávszélessége (egyszerre csak egy tranzakció futhat rajta) – ld. a staggerelt intervallumok fenti indoklását –, illetve hogy a nyomás fizikailag lassan változik, így egy bizonyos ponton túl a gyorsabb pollozás már nem ad ténylegesen frissebb/hasznosabb adatot, csak feleslegesen terheli a buszt.
  - **Hatodik kör, felhasználói visszajelzésre, 2026-08-13 (ua. nap)**:
    - **Hozzáadás név nélkül** – az "Új eszköz" sor "Hozzáadás" gombja eddig üres névvel is elsült, ekkor a firmware egy generikus "Pressure Sensor N" alapértelmezett nevet adott a slotnak – ez hibás feltételezés, hiszen a slot nem feltétlenül nyomásmérő lesz (több Modbus eszköztípus tervezve). **Javítva**: a "Hozzáadás" gomb üres/csak-szóköz névnél inaktív (`web/dashboard.js`'s `upsertNewPressureRow()`), élőben (gépelés közben) is, nem csak a következő renderre várva.
    - **Szerkesztés gyakorlatilag lehetetlen volt** – a valódi ok egy, az előző körben (sorrend-funkció) bevezetett regresszió volt: minden renderelés (ami egy regisztrált slotnál gyakorlatilag minden egyes pollozásra lefut, ~fél-egy másodpercenként) feltétel nélkül `appendChild`-elte az összes regisztrált sort, hogy érvényesítse a sorrendet – ez akkor is "megmozgatta" (leválasztotta, majd újra beillesztette) a DOM node-ot, ha az már eleve a helyén volt, ami böngészőben elveszi a fókuszt bármelyik éppen fókuszban lévő mezőtől. **Javítva**: a sorrend-érvényesítés mostantól egy standard "csak akkor mozgat, ha tényleg nem a helyén van" algoritmust használ (`renderPressureTableBody()`) – a leggyakoribb esetben (nem történt tényleges átrendezés) nulla DOM-mozgatással jár, így a szerkesztés alatt lévő mező fókusza nem szakad meg a következő pollozás-eredményű újrarenderre.
    - **Szerkesztés/nézet mód nem volt vizuálisan megkülönböztethető, és a mezőkbe view módban is be lehetett lépni** – **javítva**: a mezők view módban `disabled` (nem csak `readOnly`) állapotban vannak, így kattintással/fókusszal sem érhetők el, amíg a ceruza gombot meg nem nyomjuk; a szerkesztés alatt álló sor emellett saját háttérszínt kap (`.dc-pressure-row-editing`), hogy a mód egyértelműen látszódjon.
    - **Ütközés-jelzés hamis "OK" villanása** – valós teszttel megerősítve: két, azonos címen ütköző eszköz esetén egyetlen tiszta (nem-ütköző) válasz nem bizonyítja, hogy az ütközés elmúlt (a buszarbitráció miatt bármelyik pollozás "véletlenül" tiszta választ kaphat, miközben a másik eszköz továbbra is ott van) – ez rövid, félrevezető "OK" villanásokat okozott. **Javítva, de nem "beragadó" megoldással** (a felhasználó kifejezett kérésére: nem szabad örökre Collision-ben ragadnia, ha közben tényleg lehúzzuk az ütköző eszközt) – hiszterézis: minden slot számol egy "egymást követő tiszta pollozások" streaket (`pressure_sensor.yaml`, `${id_prefix}_collision_clean_streak` globals), egyetlen ütköző válasz azonnal nullázza és jelzi az ütközést, de a jelzés csak **két egymást követő** tiszta pollozás után törlődik – egy sima timeout (se ütközés, se tiszta válasz) se nem nullázza a streaket, se nem törli a jelzést, mert önmagában semmit nem bizonyít.
    - **"Collision?" → "Collision"** (mind a regisztrált soron, mind az önálló ütközés-soron).
    - **Forgó "spinner" a Scan Bus gombon** a "Scanning…" felirat mellett, amíg a scan fut (`syncScanButtonBusyState()`), a szöveges visszajelzés mellett egy azonnal, első pillantásra is látható vizuális jelzésként.
    - **Service oldal szélesség-reszponzivitása** – a kártyák (`.dc-service-group`) és a nyomásmérő-táblázat (`.dc-pressure-group`) korábban egy fix, keskeny (`420px`) max-width-ön ragadtak minden képernyőméreten, feleslegesen keskenyen hagyva a helyet szélesebb (pl. asztali) nézetben. **Javítva**: `.dc-service-group` `560px`-re, `.dc-pressure-group` (mivel valódi, több oszlopos táblázatot tartalmaz, nem csak egy mezőt) `900px`-re emelve – mindkettő továbbra is csak max-width, keskeny nézetben ugyanúgy zsugorodik, mint eddig. A táblázat emellett saját, oldalirányban görgethető konténert kapott (`.dc-pressure-table-scroll`) – ha egy jövőbeli új oszlop (pl. eszköztípus – nyomásmérő vs. áramlásmérő, ld. alább) miatt a tábla szélesebb lenne, mint a rendelkezésre álló hely, ne az oszlopok zsugorodjanak olvashatatlanra vagy vágódjanak le, hanem a tábla önmagában görgethető legyen. Egy külön, mobil-specifikus "sor alatt kinyíló szerkesztő/részletek terület" elrendezés (a felhasználó saját, nem megkötő javaslata) egyelőre elhalasztva – érdemesebb majd egy konkrét új attribútum (pl. eszköztípus) bevezetésekor újragondolni, nem előre, spekulatívan.
  - **Hetedik kör, felhasználói visszajelzésre, 2026-08-13 (ua. nap)**:
    - **Kritikus hiba: a nyomásérték eltűnt a Dashboard oldal kártyájáról** – a kártya fejléce (ikon+név) rendben megjelent, de a törzs (az érték maga) végleg üresen maradt, amíg a felhasználó teljesen újra nem töltötte az oldalt. Valódi ok, kód-átvizsgálással megtalálva: `syncPressureHomeCard()` a slot regisztrálatlanná válásakor (cím átmenetileg vagy ténylegesen `0`) törli a Home-kártyát, de a "Pressure" entitáson gyorsítótárazott `entity.el` DOM-referenciát nem törölte – így egy későbbi újra-regisztrálódáskor az `upsertHomeMetric()` saját "már létrehoztam, nem kell újra" ellenőrzése (`if (!entity.el)`) tévesen igazra értékelődött ki egy már leválasztott, láthatatlan csomópontra mutató referenciával, és onnantól csak azt frissítette (soha nem csatolta vissza az új kártyához). Egy teljes oldal-újratöltés pont ezért oldotta meg (minden gyorsítótárazott referencia törlődik). **Javítva**: `syncPressureHomeCard()` a kártya törlésekor a "Pressure" entitás `.el` referenciáját is nullázza, így egy későbbi újra-megjelenítés valóban újraépíti és visszacsatolja a sort.
    - **Ütközés-jelzés "OK" villanása, második kör** – a felhasználó megerősítette, hogy az előző (streak-alapú) javítás után is előfordult még rövid, hamis "OK" villanás. Valós, statisztikailag indokolható ok: pusztán a *pollozások száma* alapú számláló (2 egymást követő tiszta olvasás) még mindig sebezhető, mert a buszarbitráció miatt akár két egymást követő pollozás is véletlenül tiszta választ kaphat, miközben az ütközés ténylegesen még fennáll. **Javítva, robusztusabb módszerrel**: a streak-számláló helyett egy **idő alapú "lehűlési" ablak** (`${id_prefix}_last_collision_ms` global, `millis()` időbélyeg) – bármely ütköző válasz azonnal nullázza az órát, és a jelzés csak akkor törlődik, ha **legalább 2 másodperce** nem volt ütközés ÉS az aktuális olvasás is tiszta. Ez nem attól függ, hány konkrét pollozás történt közben (ellentétben a streak-kel), hanem attól, hogy tényleg volt-e elég hosszú, valóban csendes időszak – statisztikailag jóval erősebb garancia, mégsem "beragadó" (ha lehúzzuk az ütköző eszközt, a felépülés ugyanúgy megtörténik, csak a lehűlési ablak leteltével).
    - **Nem-determinisztikus hiba a Scan Bus ütközés-detektálásában**: két azonos címen ütköző eszköz esetén egyetlen scan alkalmanként nem mutatta az ütközést (csak "Új eszköz" jelent meg), egy újabb scan viszont igen – ugyanaz a buszarbitráció-alapú jelenség, mint a folyamatos pollozásnál: egyetlen próba véletlenül tiszta választ kaphat, miközben a második eszköz ténylegesen ott van. **Javítva**: `scan_bus()`/`probe()` (`include/rs485_modbus.h`) minden "FOUND" (tiszta) eredményt egy második próbával megerősít, mielőtt elfogadná – ha a második próba ütközést (CRC-hibás, de érkezett választ) jelez, a cím "Collision"-re vált. Egy második `NO_RESPONSE` NEM írja felül a "FOUND"-ot (önmagában semmit nem bizonyít). Csak azokra a címekre kerül sor a plusz próbára, amik egyáltalán válaszoltak – a teljes 1-247 pásztázás sebessége gyakorlatilag változatlan marad.
    - **Scan Bus gomb hover-re kattinthatónak tűnt tiltott állapotban, és a szélessége megváltozott scan közben** – a `.dc-btn` osztálynak eddig nem volt saját `:disabled` stílusa, így a hover-kiemelés akkor is látszott, amikor a gomb inaktív volt; a "Scanning…" felirat emellett hosszabb volt mint a "Scan Bus", ezért a gomb ténylegesen szélesebbé vált scan közben. **Javítva**: `.dc-btn:disabled` explicit visszaállítja a nem-hover megjelenést; a gomb saját felirata mostantól mindig fix ("Scan Bus"), a "Scanning…" szöveg + a forgó jelző egy külön, a gomb mellett megjelenő elembe (`.dc-pressure-scan-status`) került át, így a gomb mérete állandó marad.
    - **Pollozási időzítés szórásának szűkítése**: a korábbi 40ms-onkénti létra (520-800ms, ~1.5x szórás leggyorsabb/leglassabb slot közt) helyett 10ms-onkénti, szűkebb létra (600-670ms, ~1.12x szórás) – megtartja a garantált, determinisztikus szét-csúszást (bármely 8 különböző fix érték nem esik tartós lockstep-be), de igazságosabban osztja el a frissítési sebességet a slotok közt. Valódi per-pollozás randomizálás megfontolva és elvetve: statisztikailag alkalmanként épp az egyidejű pollozást okozhatná (amit ez a mechanizmus kizárni hivatott), és nehezebben debug-olható lenne egyértelmű előny nélkül.
  - **Nyolcadik kör, felhasználói visszajelzésre, 2026-08-13 (ua. nap)**:
    - **A "Scanning…" felirat/forgó jelző függőlegesen rosszul illeszkedett a Scan Bus gombhoz** – a toolbar korábbi `align-items: flex-end` beállítása még akkor íródott, amikor a toolbarban csak maga a gomb volt; az újonnan mellé került, kisebb betűméretű `.dc-pressure-scan-status` elemmel együtt ez láthatóan a gomb közepe alá csúsztatta a szöveget/jelzőt. **Javítva**: `align-items: center`.
    - **Ütközés-jelzés hamis "OK" villanása, még mindig előfordulhat, de nem reprodukálható megbízhatóan** – a felhasználó megerősítette, hogy a jelenség ritkult, de nem tűnt el garantáltan; ez összhangban van a csatolt naplóval, ami megmutatja, hogy egy ténylegesen ütköző címen a válaszok **nem következetesen** ugyanazzal a hibával futnak (van tiszta CRC-hiba, van részlegesen megérkező majd megszakadó válasz, van teljes timeout is) – mind a három a buszarbitráció valós, változó kimenetele, amit a jelenlegi három-állapotú (`NO_RESPONSE`/`COLLISION`/`FOUND`) osztályozás és a 2 másodperces lehűlési ablak együtt már jól kezel, de statisztikailag sosem lehet 100%-ig kizárni egy nagyon szerencsés, két egymást követő tiszta választ. Nem tekintjük többé önmagában hibának – ez a mechanizmus alapvető, elfogadott korlátja (nem "bizonyíték", "erős jelzés", ld. korábbi körök), amit érdemben csak az ár (lassabb felismerés/lassabb törlés) tovább-emelésével lehetne tovább szűkíteni.
    - **Talált, valós következmény: a Dashboard oldal nyers nyomásértéke "-- bar" és "0 bar" közt villogott ütköző címen** – ez NEM új hiba, hanem a gyökérok (buszütközés) helyes, várt tünete volt, amit eddig semmi nem szűrt ki: egy ütköző címen egy CRC-helyes válasz sem bizonyítja, hogy a *saját* regisztrált szenzorunk válaszolt – ugyanolyan eséllyel a másik, ütköző eszköz saját (esetünkben `0 bar`, mert az nincs nyomás alá helyezve) értéke jött vissza. A Service oldal táblázatának "Collision" jelzője eddig is helyesen kezelte ezt (nem függ az aktuális pollozási eredménytől, a 2mp-es lehűlési ablak védi), de a Dashboard oldal nyers száma közvetlenül a legutóbbi pollozás eredményét mutatta, hysteresis nélkül. **Javítva**: a Dashboard kártya a Service táblázattal **ugyanazt** az ütközés-jelzést (a debounce-olt "Scan Collisions" CSV-t) nézi meg, és amíg egy slot ütközőnek van jelölve, `"--"`-t mutat a valódi (de nem megbízhatóan hozzárendelhető) érték helyett, ahelyett hogy villogva mutatná bármelyik ütköző eszköz aktuális válaszát.
  - **Kilencedik kör, felhasználói visszajelzésre, 2026-08-13 (ua. nap)**:
    - **"Lost" villant fel Hozzáadáskor, egy friss, egészséges szenzoron is** – valódi ok, kód-átvizsgálással megtalálva: `registeredPressureSlots()` `onlineEntity.value === true` alakú ellenőrzése `undefined === true`-t hamisra értékelte ki, amikor az Online bináris szenzor még sosem publikált (a szenzor entitása már létezik a kliens oldalon, csak a saját első pollozása még nem futott le) – a saját megjegyzése tévesen "undefined"-ot ígért erre az esetre, a tényleges kód viszont hamisat adott, ami "Lost"-ként jelent meg. **Javítva**: explicit `onlineEntity.value !== undefined` ellenőrzés, plusz egy új, harmadik állapot ("Checking…", semleges színnel) a Státusz jelzőn, ami elkülöníti "még nem tudjuk" a "megerősítetten offline"-tól.
    - **Új eszköz hozzáadása nem a lista aljára került, hanem cím szerint valahova középre** – a Sort Order alapértelmezett `0` értéke ("nincs testreszabva") eddig cím szerint rendezte egymás közt az még nem testreszabott sorokat, ami egy friss hozzáadást nem feltétlenül a lista végére tett. **Javítva**: egy új, megosztott, monoton növekvő számláló (`next_pressure_sort_order`, `water-collector.yaml`) minden sikeres regisztráláskor a slot saját Sort Order-jébe kerül, majd eggyel nő – ezzel minden új hozzáadás garantáltan magasabb rangsorszámot kap, mint bármelyik korábbi, tehát mindig a lista aljára kerül, függetlenül a címétől. A kézi Fel/Le rendezés (ami 1..N-re "tömöríti" a rangsorszámokat) nem ütközik ezzel, mivel a számláló onnantól is csak nő.
    - **A szerkesztő mezőkben Enter/Esc nem működött** – **javítva**: Enter a Mentés (pipa), Esc a Mégse (x) gombbal egyenértékű, amíg a mező ténylegesen szerkeszthető (letiltott mezőn a böngésző eleve nem küld billentyű-eseményt, tehát ez a kötés magától csak szerkesztés közben aktív).
    - **A törlés megerősítő üzenete a slot belső, szándékosan jelentés nélküli sorszámát mutatta** (pl. "Pressure Sensor 3"), nem a szenzor nevét/címét – ez pont az a belső azonosító, amiről a `pressure_sensor.yaml` fejléce is kimondja, hogy sosem szabadna megjelennie. **Javítva**: a megerősítő szöveg a Display Name-et mutatja, üres név esetén a Modbus címre esik vissza.
    - **Törlés után az új első sor kuka ikonja "beragadt" piros hover-állapotban**, amíg a felhasználó máshova nem kattintott/mozgatta az egeret – valódi böngésző-viselkedés, nem alkalmazáslogikai hiba: a törlést megerősítő natív `confirm()` dialógus blokkol, tehát a törlést kiváltó kattintás sosem járt azzal, hogy az egér ténylegesen a törlés utáni *új* első sor fölé mozdult volna – a böngésző mégis `:hover`-ként kezeli, mert az egérmutató pozíció szerint épp ott áll. **Javítva**: minden sor-eltávolítás után egy rövid életű CSS-osztály elnyomja a hover-színeket a táblán, amíg az egér ténylegesen nem mozdul.
    - **Kérdés: mennyire gyors a szenzor saját belső mérése?** A gyártói adatlap (`project-docs/docs/hardver/qdw90a-nyomastavado-adatlap.pdf`, "4.1 Basic Electrical Parameters") explicit megadja: **Response time: ≤10 ms** – ez a szenzor-mag (diffúziós szilícium piezorezisztív elem) saját analóg válaszideje, tehát az elméleti fizikai/analóg korlát jóval a jelenlegi (`600-670ms`) pollozási intervallumunk alatt van, nem az szűkíti a lehetőségeinket. A digitális (Modbus regiszter-frissítési) oldalra nincs explicit szám az adatlapban, de mivel ugyanaz a mérőmag van a 4-20mA és a Modbus kimenetű változat mögött is, ésszerű feltételezés, hogy a beépített mikrovezérlő saját frissítési ciklusa (ADC kiolvasás + regiszterírás) ennél nem sokkal lassabb – iparágban szokásos, hasonló árkategóriájú Modbus nyomástávadóknál ez tipikusan 10-100Hz (10-100ms) tartományban szokott lenni, becsült érték, nem ebből az adatlapból. Összegezve: a szenzor saját belső mérése minden valószínűség szerint **nem** a szűk keresztmetszet – a megosztott, félduplex RS485 busz és a 8 slot közti időosztás az.
  - **Tizedik kör, felhasználói visszajelzésre, 2026-08-13 (ua. nap)**:
    - **Üres név esetén a belső "Pressure Sensor N" névre esett vissza a Dashboard felirat** – a Hozzáadás gomb már korábban is megkövetelte a nevet (ld. "Hozzáadás név nélkül" fentebb), de a **szerkesztés** (ceruza → Mentés) útján egy már regisztrált slot neve továbbra is kiüríthető volt – egy üres Display Name mellett `groupLabel()` (a Dashboard-kártya és a Service-fejlécek közös segédfüggvénye) egyenesen a csoport nyers, belső azonosítójára esik vissza, pont arra a szándékosan jelentés nélküli névre, amit ez a felület mindenhol máshol gondosan elrejt. **Javítva**: a Mentés gomb is megköveteli a nem üres nevet, ugyanúgy, mint a Hozzáadás.
    - **Megjegyzett jövőbeli terv: eszköztípus (device class) alapú névjavaslat** – ld. külön "Eszköztípus (device class) – tervezett bővítés" alfejezet lentebb.
  - **Tizenegyedik kör, felhasználói visszajelzésre, 2026-08-13 (ua. nap)**:
    - **A belső "Pressure Sensor N" felirat még mindig felvillant a Dashboardon, most már friss, nem üres nevű regisztrációnál is** – a valódi ok: friss kapcsolódás/újracsatlakozás esetén az entitások nem egyszerre, hanem fokozatosan érkeznek (ESPHome egy fix, keresztmetszet-doménenkénti sorrendben dumpolja őket, nem a regisztráció sorrendjében) – a Modbus Address (number domén) simán megérkezhet a Display Name (text domén) előtt, ami alatt a kártya fejléce a `groupLabel()` régi visszaesési logikája miatt a nyers, belső csoportnevet ("Pressure Sensor N") mutatta, amíg a valódi név utol nem érte. **Explicit felhasználói kérés**: inkább semmit ne mutasson, amíg a valódi nevet nem ismerjük biztosan. **Javítva**: `groupLabel()` mostantól nyomásmérő csoportoknál sosem esik vissza a nyers névre – amíg a valódi Display Name nem ismert, üres string jelenik meg (csak az ikon látszik), a meglévő "settle" mechanizmus pedig ugyanúgy egyben korrigálja a fejlécet, amint a kezdeti adatcsomag lezárul.
    - **A `try_register` üres név esetén a belső azonosítót ("${friendly_name}") adta a szenzornak, ténylegesen elmentve azt Display Name-ként** – ez volt az az egyetlen hely, ahol a belső név ténylegesen, tartósan Display Name-mé válhatott (a dashboard Hozzáadás/Mentés gombjai már megkövetelik a nevet, de a nyers API-n keresztül ez a védőháló még mindig elérhető maradt). **Javítva, a felhasználó kifejezett kérésére ("ilyen szövegnek már nem is szabadna maradnia a kódban")**: a script mostantól egyszerűen nem regisztrál üres névvel (nem helyettesíti be a belső azonosítót).
    - **"Water Meter N" ugyanez a probléma, más doménben** – a felhasználó megerősítette, hogy ugyanez a jelenség (nyers belső név villanása) a vízmérőknél is fennáll, de kérésére **nem** kapott önálló, ideiglenes javítást – lásd a következő pontot, ami architekturálisan úgyis megoldja majd, nem érdemes duplán dolgozni rajta.
    - **Megjegyzett, nagyobb jövőbeli architekturális terv: a "Pulse meter" (jelenlegi vízmérő) szekció átalakítása a nyomásmérőkkel megegyező, dinamikus 0-2 elemű minta szerint** – a felhasználó explicit kérése: ugyanaz a megjelenés/vezérlés-logika (Hozzáadás/Törlés/szerkesztés-zár/Sort Order stb.), mint a Modbus eszközöknél, hogy a karbantartás egyszerűsödjön és a rendszer átláthatóbb legyen. Fontos különbség, amit végig kell gondolni implementáció előtt: a vízmérők nem Modbus-buszon felfedezhető eszközök, hanem fixen bekötött GPIO-pinek (`meter1_pulse_pin`/`meter2_pulse_pin`) - nincs "Scan Bus" megfelelőjük, a "slot" itt egy fizikai GPIO, nem egy busz-cím. Nyitott kérdések, amiket implementáció előtt tisztázni kell: mit jelentsen a "törlés" egy fizikailag bekötött GPIO-n (csak a megjelenítést/nyilvántartást törli, vagy a pulzusszámlálást is leállítja?), mi történjen a már felhalmozott "Összes fogyasztás" adattal törléskor, és hogyan képződjön le a "Scan"-hez hasonló felfedezési lépés egy olyan doménre, ahol nincs mit felfedezni (a 2 GPIO fixen adott). **Státusz**: **implementálva, 2026-08-13, ua. nap** - ld. "Tizenkettedik kör" alább a végleges válaszokkal és a megvalósítás részleteivel.
  - **Tizenkettedik kör: a "Pulse Meters" egységesítés megvalósítása, 2026-08-13 (ua. nap)**:
    - **Nyitott kérdések, tisztázva a felhasználóval, implementáció előtt**:
      1. **Törlés jelentése**: csak elrejti/kikapcsolja - a pulzusszámlálás és a felhalmozott Összes fogyasztás a háttérben tovább fut/megmarad, semmilyen adat nem vész el.
      2. **Hozzáadás UX, felfedezés nélkül**: mindkét fizikai GPIO automatikusan "Új eszköz" sorként jelenik meg, amíg nincs regisztrálva - nincs "Scan Bus" megfelelője, nincs rá szükség (a 2 lehetséges meter fixen adott, fordításidőben ismert).
      3. **Kalibrációs mezők láthatósága**: teljes egységesítés - a Reading/Update/Zero-Flow Timeout (és a Total Pulses diagnosztikai mező) is csak Hozzáadás után jelenik meg, pontosan úgy, mint a Modbus eszközök bármely attribútuma.
    - **Firmware (`packages/water_meter.yaml`)**: az eddigi "Show on Dashboard" kapcsoló (aminek eddig kizárólag a Dashboard-kártya láthatóságára volt hatása) átnevezve/újraértelmezve **"Registered"**-re - mostantól ez az egyetlen igazságforrás arra, hogy a meter commissionálva van-e, ugyanúgy, ahogy egy nyomásmérő slotnál a Modbus Address `!= 0`. Új **"Delete"** gomb hozzáadva (`switch.turn_off` a Registered kapcsolóra - nem nullázza a Display Name-et, mert egy vízmérő GPIO-ja mindig ugyanaz a fizikai mérő marad, nincs "másik eszköz veheti át a helyét" bizonytalanság, szemben egy nyomásmérő címmel). Minden más entitás (Total Consumption, Flow Rate, Reading, Update, Zero-Flow Timeout, Total Pulses) változatlan - továbbra is mindig lefordítva/elküldve, csak a *megjelenítés* feltételes (ugyanaz az "existence model", mint a nyomásmérőknél).
      - **Biztonsági megfontolás, első verzió (később felülbírálva, ld. "Tizenharmadik kör")**: a `restore_mode`-ot ekkor **szándékosan NEM** változtattam `RESTORE_DEFAULT_OFF`-ra, mivel az átnevezés (`name:` mező) megváltoztathatja az ESPHome saját, entitásnév-alapú restore-kulcsát, és nem volt bizonyosság hogy ez nem törli-e érvénytelenné a már bekapcsolt eszköz meglévő, flash-ben tárolt "bekapcsolva" állapotát. `RESTORE_DEFAULT_ON` maradt ekkor.
    - **`water-collector.yaml`**: új `sorting_group_pulse_meters` ("Pulse Meters") umbrella csoport, a táblázat horgonypontja - nincs hozzá saját megosztott entitás (nincs rá szükség, ld. lent). A `sorting_group_meter1`/`sorting_group_meter2` neve (és a `friendly_name`) ekkor még szándékosan "Water Meter 1/2" maradt (később átnevezve, ld. "Tizenharmadik kör").
    - **`web/dashboard.js`**: új "Pulse meter table" szekció, ami ugyanazt az Add/Delete/szerkesztés-zár mintát adja, mint a nyomásmérő táblázat, de busz/cím/scan/collision/sorrend nélkül (ezek a fogalmak nem értelmezhetők egy fix GPIO-ra) - Hozzáadás egyetlen, per-sor, közvetlen 2-lépéses fetch-csel (Display Name beállítása, majd Registered bekapcsolása), nincs szükség a nyomásmérőknél muszáj megosztott "claim" logikára, mivel itt nincs cím-egyértelműség, minden sor eleve tudja melyik fizikai meterhez tartozik. A táblázat a nyomásmérő táblázat CSS osztályait (icon-btn, row-editing, stb.) újrahasznosítja - ezek doménfüggetlenek a névük ellenére, saját stílus-duplikálás helyett.
      - **Fontos, menet közben talált és javított buborékoló hiba**: mivel ESPHome az entitásokat fix, keresztmetszet-doménenkénti sorrendben dumpolja (nem regisztráció sorrendjében), a "Registered" (switch domén) nem garantáltan érkezik a "Total Consumption" (sensor domén) előtt - enélkül egy korábban érkezett, de akkor még "nem regisztrált"-ként kihagyott entitás soha többé nem frissült volna, amíg a saját, független következő eseménye meg nem érkezik (pl. Total Consumption csak a következő impulzusra). **Javítva**: amint "Registered" ismertté válik, a rendszer minden más, már ismert entitást a csoportban újra végigfuttat a megjelenítési logikán, hogy semmi ne ragadjon be "kihagyva" állapotban.
      - **Szintén menet közben talált és javított hiba**: az entitás DOM-referenciák (`.el`/`.inputEl`/`.readoutEl`/`.toggleEl`/`.btnEl`) törlés (un-registration) esetén explicit nullázódnak minden, az adott csoporthoz tartozó entitáson - ugyanaz a hiba-osztály, amit korábban a nyomásmérő Dashboard-kártyájánál találtunk és javítottunk (egy leválasztott, láthatatlan DOM-node végtelenített frissítése újra-regisztráláskor), csak itt kiterjesztve minden gyorsítótárazott referenciatípusra, mivel egy vízmérőnek több mezője/oldala van, mint egy nyomásmérő slotnak.
      - **Elhagyott, halott kódrészek takarítása**: a régi "Show on Dashboard"-specifikus `groupEnabled`/`applyGroupVisibility` logika mostantól sehonnan nem táplálódik (megtartva, mint általános, jelenleg parlagon heverő infrastruktúra egy esetleges jövőbeli hasonló igényre), a hozzá tartozó elavult `HELP_TEXT` bejegyzés törölve.
    - **`web/dashboard.css`**: `.dc-pulsemeter-group` (`max-width: 480px` - jóval keskenyebb, mint a nyomásmérő táblázat 900px-je, mivel csak Name+Action oszlopa van, nincs Address/Status/Order).
  - **Tizenharmadik kör, felhasználói visszajelzésre, 2026-08-13 (ua. nap)**: az előző kör két, óvatosságból meghozott döntését a felhasználó explicit felülbírálta, azzal az indoklással, hogy **még nagyon DEV fázisban vagyunk, nincs PROD, nincs HA integráció** - most (és csak most) érdemes elvégezni minden olyan átalakítást, ami később, egy éles/publikált rendszeren már fájdalmas volna. Mindkettő megváltoztatva:
    - **`friendly_name`/`sorting_groups` átnevezve "Water Meter N"-ről "Pulse Meter N"-re** (`water-collector.yaml`, `packages/water_meter.yaml`, `web/dashboard.js`'s `GROUP_ICON_BY_NAME`/`PULSE_METER_RE`) - ez a Home Assistant-ban generálandó entity_id-kat is megváltoztatná, de mivel jelenleg nincs HA integráció, ez most nem jár valódi kockázattal, csak egy pusztán kozmetikai, korábban indokolatlanul halogatott átnevezés. Az `id_prefix` (`meter1`/`meter2`) változatlan maradt - ennek átnevezése ténylegesen elveszítené a meglévő kalibrációs eltolást (`offset_m3`, flash-preference kulcsban), ez egy valódi, ha kicsi és helyrehozható (egy manuális ismételt szinkron) költség, HA-tól függetlenül is, ezért ezt továbbra sem érte meg átnevezni.
    - **`restore_mode`: `RESTORE_DEFAULT_ON` → `RESTORE_DEFAULT_OFF`** a Registered kapcsolón - egy vadonatúj eszköz mostantól valóban "nem regisztrált" állapotban indul, konzisztensen a nyomásmérő slotokkal. Ez a jelenleg futó eszközön a következő flashelés után **elméletileg** visszaállíthatja mindkét meter Registered állapotát kikapcsoltra (ha az átnevezés ténylegesen érvényteleníti az ESPHome saját restore-kulcsát) - ezt a kockázatot most tudatosan vállaltuk (a felhasználó explicit kérésére): ha bekövetkezik, a következmény pusztán annyi, hogy mindkét metert egyszer újra hozzá kell adni a táblázatban (semmilyen adat - `pulse_count`, `offset_m3`, azaz a felhalmozott fogyasztás - nem vész el, azok teljesen más, `id_prefix`-alapú preference-kulcsot használnak).
  - **Tizennegyedik kör, felhasználói visszajelzésre, 2026-08-13 (ua. nap)** - Modbus:
    - **"Lost" villant fel új Modbus eszköz Hozzáadásakor is (nem csak régin)** – gyökérok megtalálva: a Delete gomb explicit `false`-ra állítja az "Online" bináris szenzort (mivel regisztrálatlan állapotban ez az érték amúgy sem látszik/számít sehol), de ha ugyanaz a slot **később újra** felvesz egy eszközt, ez a régi, elavult `false` érték ott marad, amíg az első valódi pollozás (akár `~670ms`-ig) le nem futna. **Javítva**: `try_register` sikeres regisztráláskor azonnal, optimistán publikálja `Online: true`-ra – ez indokolt, mivel Hozzáadás csak olyan címre történhet, amit a busz-scan épp az imént talált válaszolónak; ha ez a feltételezés téved, a következő valódi pollozás úgyis korrigálja.
    - **A Service oldali Sort Order-nek (Fel/Le rendezés) nem volt hatása a Dashboard oldalra** – a Dashboard-kártyák sorrendje eddig a fordításidőben rögzített slot-súlyból jött (mindig slot-sorszám szerint), sosem a táblázat saját, dinamikus Sort Order-jéből. **Javítva**: `syncPressureHomeCard()` minden híváskor újraszámolja és felülírja az érintett kártya súlyát a Service tábla saját (Sort Order-alapú) sorrendje szerint, majd újrarendezi a Dashboard kártyákat, ha bármi változott.
  - **Tizenötödik kör, felhasználói visszajelzésre, 2026-08-13 (ua. nap)** - Pulse meters:
    - **A táblázat szélessége igazítva a Modbus táblázathoz** (`480px` → `900px`) – direkt kérésre a két commissioning-tábla egységesen ugyanakkora, nem a saját aktuális oszlopszámukhoz méretezi magát mindegyik.
    - **"Meter name" → "Device name"** (a nyomásmérő táblázat korábbi "Sensor name" placeholder-e is átállítva ugyanerre, a konzisztencia kedvéért).
    - **Nagyobb architekturális átalakítás: az attribútumok (Reading, Zero-Flow Timeout) a táblázat saját sorába költöztek** – korábban ezek egy mindig-látható, külön Service-szekcióban jelentek meg (6 sor 2 valódi mezőre), most a megfelelő táblázatsor alatt nyílnak ki, kizárólag amíg az a sor szerkesztés módban van (ugyanaz a ceruza nyitja, mint a Name mezőt, Pipa/X zárja) - a felhasználó explicit kérése, azzal a megjegyzéssel hogy ha bevállik, a Modbus eszközöknél is átvesszük majd ezt a mintát. Kompakt, egy-egy sor: "Reading" label + beviteli mező + Update gomb egy sorban; "Zero-Flow Timeout" label + beviteli mező egy sorban. **Csak egy tábla-sor szerkeszthető egyszerre az egész táblázatban** - egy másik sor szerkesztésbe nyitása a korábban nyitottat automatikusan menti-eldobja (Mégse-ekvivalens) és becsukja, mielőtt az újat megnyitná. Az összetartozás vizuálisan egyértelmű: a kinyíló sor ugyanazt az akcentszínű háttéret kapja, mint maga a szerkesztett sor, és a köztük lévő szegély el van tüntetve (folytonos, összetartozó blokknak látszik).
      - **Ez a redesign egyúttal javította a "csak page reload után jelennek meg a mezők" jelenséget is** (menet közben talált, valós hiba: mivel a Reading/Update/Zero-Flow Timeout entitások nem garantáltan a "Registered" kapcsoló előtt/után érkeznek ESPHome fix, keresztmetszet-doménenkénti dump-sorrendje miatt, egy korábban érkezett, de akkor még "nem regisztrált"-ként kihagyott mező korábban soha többé nem frissült élőben, csak egy teljes újratöltésre). A dinamikus élő-frissítés logikáját is megerősítettük: mostantól **minden** entitás-frissítés a csoportban újra végigfuttatja az összes ismert mezőt a megjelenítési logikán (nem csak a "Registered"/"Delete"/"Display Name" változásokkor), ugyanaz a "mindig újraszinkronizál" elv, amit a nyomásmérő Dashboard-kártyája is követ.
    - **Nyitott, tisztázatlan pontok, amikhez visszajelzés/screenshot kell**: (1) Enter/Esc a szerkesztő mezőkben a kódvizsgálat szerint helyesen van bekötve (ugyanaz a minta, mint a Modbus táblázatnál, ami működik) - nem találtam benne hibát, de mivel a teljes szerkesztési UX ebben a körben jelentősen átalakult, újra le kell tesztelni; (2) egy külön látható "Registered" gomb/kapcsoló, amit a felhasználó a kuka mellett lát - a kódvizsgálat szerint a "Registered" entitás sehol nem juthat el egy önálló, látható vezérlőelemig (mindenhol el van fogva, mielőtt az általános renderelési útra kerülne) - vagy egy elavult build tesztelése történt, vagy screenshot kell a pontos beazonosításhoz.
  - **Tizenhatodik kör, felhasználói kezdeményezésre ("Mit gondolsz?"), 2026-08-13 (ua. nap) - a két táblázat egyesítése egy közös "Devices" táblázattá**:
    - **Egy közös táblázat mindkét eszköztípusnak** - Elnevezés/Státusz/Edit/Delete/Order oszlopok, mindkét típusra érvényesen. A korábbi külön Address oszlop megszűnt: Modbus soroknál a cím a kinyíló szerkesztő-sávba költözött (ugyanaz a minta, mint a Reading/Zero-Flow Timeout a pulse metereknél - "Tizenötödik kör"), szerkesztéskor csak Név+Cím módosítható, ahogy kérve volt. Egy szkennelt-de-még-nem-regisztrált cím vagy egy ütköző cím esetén a cím most egy kis, halványított "Address N" feliratként jelenik meg a Név cella tetején.
    - **Egy közös "Scan Bus" + "Add" eszköztár** a táblázat felett. A Scan Bus pontosan úgy működik, mint eddig (Modbus-specifikus, a szkennelt sorok saját, egykattintásos Add gombjukkal maradtak). Az "Add" egy kliens-oldali, entitás nélküli kapcsoló, ami egy beépített táblázatsort nyit/csuk - típusválasztóval (Modbus / Pulse Meter), ami a választás szerint a megfelelő mezőket mutatja: Modbus esetén Név+Cím (kézzel beírt cím, ld. lentebb), Pulse Meter esetén Név+GPIO-slot választó (csak a még szabad "Pulse Meter N" slotok közül; ha mindkettő foglalt, a Pulse Meter opció letiltva a típusválasztóban).
    - **Kézzel beírt Modbus cím - próba-lekérdezéssel védve**: mivel ez nem szkennelésből jön (nincs előzetes megerősítés, hogy ott tényleg válaszol valami), a megosztott "Pressure Sensors Add" gomb (`water-collector.yaml`) mostantól a regisztrálás előtt egyszer megpróbálja a célcímet (`rs485_modbus::probe()`, ugyanaz a mechanizmus, amit a `scan_bus()` is használ) - csak tiszta `FOUND` esetén regisztrál. Egy új "Pressure Sensors Add Result" diagnosztikai szöveg (`ok`/`no_response`) viszi vissza az eredményt a dashboardnak, ami hiba esetén inline hibaüzenetet mutat a beviteli mezők mellett (nem natív `alert()`-tel, konzisztensen a fájl többi részével). A szkennelt sorok saját Add gombja ugyanezen az úton megy át (ugyanaz a `pressure_add` gomb), ártalmatlanul redundánsan újra-próbálva egy már az imént megerősített címet.
    - **Az Order (Sort Order) mostantól típusok között KÖZÖS** - explicit felhasználói kérés, mivel ez határozza meg a Dashboard sorrendjét is. A korábban `next_pressure_sort_order` néven csak a nyomásmérőket kiszolgáló, monoton növekvő számláló (`water-collector.yaml`) átnevezve `next_device_sort_order`-re, és mindkét eszköztípus try_register/Add-folyamata ugyanabból veszi a következő értéket - egy friss Add, bármelyik típusé, mindig a lista aljára kerül, típustól függetlenül. A pulse meterek is kaptak egy saját "Sort Order" number entitást (`water_meter.yaml`, ugyanaz a minta, mint a nyomásmérőknél) + Fel/Le gombot a táblázatban. A Dashboard (Home oldal) kártyasorrendje is ugyanezt a közös sorrendet követi mostantól (`resyncDeviceHomeCardOrder()`, a korábbi, csak-nyomásmérős `resyncPressureHomeCardOrder()` általánosítása) - korábban ez csak a nyomásmérőknél volt igaz.
    - **GPIO-slot választás Pulse Meter Add-nál**: egyelőre nincs általános "engedélyezhető GPIO-k" beállítóképernyő (ez egy megjegyzett, jövőbeli ötlet - ld. lent), csak annyi, hogy a jelenlegi 2 fix slot (`Pulse Meter 1`/`2`, GPIO1/GPIO2) közül a még szabad(ak) közül lehet választani a hozzáadáskor; ha mindkettő foglalt, a Pulse Meter típus nem választható.
    - **Pulse meter státuszjelzés finomítva**: a korábbi, nyomásmérőktől kölcsönzött OK/Lost-szerű jelzés helyett egy egyszerű, kétállapotú "Ready" (zöld pötty, mindig ez amíg regisztrálva van - egy helyi GPIO-impulzusszámlálónak nincs "elérhetetlen" állapota) + egy rövid, ~0,7 mp-es "gyűrű" felvillanás (`flashPulseMeterActivity()`, CSS `@keyframes dc-pulse-ring`) minden alkalommal, amikor egy valódi impulzus ténylegesen megérkezik (a "Total Pulses" diagnosztikai szenzor - a perzisztens `pulse_count` nyers leolvasása - változásán figyelve, ami csakis valódi impulzusra frissül). A felhasználó jelezte, hogy ezt még tovább finomítjuk később, hogy "szépebb és informatívabb" legyen - ez az első, funkcionális változat.
    - **Nyitva hagyott, tudatosan el nem végzett finomítás**: egy szkennelt (nem kézzel beírt) sor Add gombja nem kap explicit hiba-visszajelzést, ha a cím a szkennelés óta megszűnt válaszolni (a próba ugyanúgy lefut, csak a dashboard nem figyeli külön ennek az eredményét azon az úton) - a sor egyszerűen nem jelenik meg regisztráltként, ugyanúgy, mint a funkció bevezetése előtt volt, tehát ez nem regresszió, csak egy nem-továbbfejlesztett rész.
  - **Tizenhetedik kör, felhasználói visszajelzésre, 2026-08-13 (ua. nap) - a "Devices" táblázat finomítása**:
    - **Eszköztár a táblázat aljára költözött, Scan Bus és Add felcserélve**: korábban a táblázat FELETT volt egy "Scan Bus" + "Add" eszköztár; mostantól a táblázat ALATT van, és a sorrend is megfordult: Add-jellegű gombok elöl, Scan Bus utánuk.
    - **Egy "Add" gomb + típusválasztó helyett két önálló gomb**: "Add Pulse Meter" és "Add Modbus Device" - felhasználói felvetésre ("lehet jobb lenne a legördülő helyett?"), mert mindkét gomb önállóan tudja jelezni/tiltani magát (pl. "Add Pulse Meter" letiltva, ha mindkét GPIO-slot foglalt), és egyértelműbb elsőre, mint egy generikus "Add" + egy azt követő legördülő. Bármelyik megnyomása bezárja az esetlegesen nyitva lévő szerkesztés-módú sort (Mégse-ekvivalens), ugyanúgy, ahogy egy szerkesztés megnyitása is bezárja a nyitva lévő Add-sort - csak egy interaktív dolog lehet nyitva egyszerre, a táblázat egészében.
    - **A beépített Add-sor "Add" szövegű gombja helyett Pipa/X ikon**: vizuálisan konzisztensen a táblázat többi Mentés/Mégse ikonjával, nem egy külön kinéző, önálló gombként.
    - **Busz-szkennelés utáni "New device" sor Add gombja is Pipa/X-re cserélve**: a Pipa ugyanazt a regisztrációs folyamatot indítja, mint eddig; az X viszont NEM a szervert hívja, hanem csak ennyi a munkamenetre "elrejti" azt a címet (a következő valódi Scan Bus lenyomásakor törlődik ez a lista) - lásd a következő pontot, ez ugyanabból az elvből következik.
    - **Szkennelés eredménye mostantól munkamenet-helyi, nem perzisztens** - explicit felhasználói kérés/észrevétel: "az logikus, ha kilépek (vagy page refresh) akkor eltűnjenek [a New device sorok], ne okozzon perzisztens műveletet, ha csak szkeneltem a buszt". A "Scan Results"/"Scan Collisions" szenzorok maguk továbbra is a firmware-ben élnek (nem törlődnek), de a dashboard client-oldalon mostantól csak azokat az értékeiket veszi figyelembe "New device"/"Collision" sor építésére, amik a kezdeti SSE-dump (kapcsolódáskor mindig lezajló, teljes állapot-átküldés) LEZÁRULÁSA UTÁN érkeznek (a már meglévő `initialSettled` flag felhasználásával, amit korábban csak a csoport-láthatóság/-címke logika használt) - egy kapcsolódáskor kapott, már réginek számító szkennelési eredmény emiatt sosem villan fel "New device" sorként, csakis egy ezután ténylegesen megnyomott Scan Bus (akár ettől, akár egy másik klienstől) hozza létre újra a sorokat. Egy oldal-frissítés emiatt automatikusan "elfelejti" mind a régi szkennelési eredményt, mind bármely korábbi X-szel elutasított címet (ami amúgy is csak egy JS-memóriában élő `Set`, ami frissítéskor eltűnik).
    - **Típus-ikon minden sor elején** (mérőóra-ikon = Modbus nyomásmérő, csepp-ikon = pulse meter, ugyanazok az ikonok, amiket a Dashboard oldal kártya-fejlécei is használnak) - mivel a táblázat mostantól mindkét típust vegyesen listázza, egy átnevezett eszköz neve önmagában már nem árulja el a típusát.
  - **Tizennyolcadik kör, felhasználói visszajelzésre, 2026-08-13 (ua. nap)**:
    - **Pulse meter Delete mostantól tényleg töröl, nem csak elrejt** - explicit felhasználói felülbírálás a korábbi ("Tizenharmadik kör" előtti) tervezési döntésnek, ami szándékosan megőrizte a felhalmozott `pulse_count`/`offset_m3`/Display Name-et Delete után. Indoklás: mivel a fizikai GPIO-n lévő eszközt Delete nem törli/programozza át, egy korábbi mérő ottmaradt adata belezavarhat egy ténylegesen MÁSIK fizikai mérő friss Hozzáadásába; nincs olyan kritikus adat, ami emiatt elveszne, hiszen újra-hozzáadás után a "Reading"+"Update" mezőkkel bármikor visszaállítható a helyes érték. `water_meter.yaml`'s Delete gomb mostantól: Sort Order + Display Name visszaáll alapértékre (mint eddig is), ÉS `offset_m3`-at 0-ra állítja (+ azonnal flash-eli), ÉS `pulse_meter.set_total_pulses: 0`-t hív (ez a `pulse_count`-ot és a függő Total Pulses/Total Consumption/last_pulse_ms értékeket is nullázza, ugyanazon a szinkron `on_value` láncon keresztül, amit az `on_boot` seedelő lambda is használ).
    - **Impulzus-felvillanás javítva** (a "Tizenhatodik kör"-ben bevezetett `flashPulseMeterActivity()`/`.dc-pulse-ring` nem villant): két, egymástól független, gyanús pontot javítottunk egyszerre (mivel élő hardveren nem reprodukálható innen, mindkettő biztonságos, kis költségű javítás, függetlenül attól melyik volt a tényleges ok): (1) a CSS animáció `from` kulcskockája `color-mix()`-et használt, ami régebbi WebKit/iOS-en nem támogatott - egy `box-shadow` deklarációban egy fel nem ismert függvény az EGÉSZ deklarációt érvényteleníti, nem csak visszaesik valami ésszerűre, ez csendben "eltüntethette" a teljes animációt; mostantól `color-mix()` nélkül, sima `currentColor` → `transparent` átmenettel (ugyanaz a jól támogatott "ping" technika, amit böngészők natívan tudnak alfa-interpolálni). (2) a JS oldali `classList.add()` nem indítja újra a CSS animációt, ha az osztály MÁR jelen van (gyors, egymást követő impulzusoknál, 0,6mp-en belül) - mostantól explicit `remove` + kényszerített reflow (`void element.offsetWidth`) + `add` szekvenciával mindig ténylegesen újraindul az animáció.
    - **Nyitott, tisztázatlan pontok, visszajelzés kell**: (1) "ugyanazt az addresst le lehessen újra felvenni a Modbusnál" - kódvizsgálattal nem találtam olyan helyet, ami ezt technikailag blokkolná (Delete resetel Address/Sort Order/Display Name-et, törli a címet a Scan Results/Collisions CSV-kből is, a manuális "Add Modbus Device" újra probe-olja a címet hozzáadás előtt) - pontos reprodukálás kell (mit csinál pontosan, amikor újra megpróbálja felvenni: nem jelenik meg lehetőségként, a gomb nem csinál semmit, hibaüzenet jön, stb.); (2) "new státuszú eszközök perzisztensen tárolódnak, X gombbal sem törlődnek" - ez pont a "Tizenhetedik kör"-ben bevezetett munkamenet-helyi `dismissedScanAddresses`/`initialSettled`-kapu funkció, amit kódvizsgálattal újra átnéztünk, nem találtunk benne hibát - lehetséges, hogy a teszteléskor még a régebbi (a "Tizenhetedik kör" előtti) firmware volt élesben (a dashboard.js/css fordításidőben épül be a bináris firmware image-be, minden JS/CSS változtatás új fordítást+flashelést igényel, nem elég csak a böngészőt frissíteni) - visszajelzés kell a legfrissebb flash-elt build alapján, pontos reprodukálással (az X gombra kattintva eltűnik-e egyáltalán a sor, akár csak pillanatra is, vagy azonnal visszajön).
    - **Nagy vizuális/design átalakítás kérve, még nem kezdve**: a felhasználó szerint a jelenlegi felület "zavaros", vékonyabb vonalvezetést, "logikus és standard elrendezést" szeretne, konkrét design-rendszer/stílus-referencia nélkül - mielőtt egy ekkora, nehezen visszafordítható munkát elkezdenénk, tisztázó kérdés ment a felhasználónak a kívánt stílusirányról.
  - **Tizenkilencedik kör, felhasználói jóváhagyásra ("jó"), 2026-08-13 (ua. nap) - a vizuális redesign élesítve**:
    - **Előzmény**: a "Tizennyolcadik kör" végén egy önálló HTML-artifact-mockup-ban ("Instrument Panel") bemutattam egy javasolt vizuális irányt (egy soronkénti vékony elválasztó nem minden cellának külön kerete, valódi 4px-es spacing-skála, monospace/tabular számjegyek minden élő leolvasásnál és Modbus-címnél, vékonyabb ikon-vonalvezetés 1,8px→1,5px). A felhasználó ezután kérte a **színek** felülvizsgálatát is, "bevált design megoldás" alapján - ehhez a projekt saját `dataviz` skill-jének **validált, kontraszt-ellenőrzött referencia-palettáját** használtam fel (nem szemre választott értékekkel): a semleges színek és a státusz-színek (jó/figyelmeztetés/kritikus) e paletta már ellenőrzött sötét-módú lépcsői, az új akcentszín (égkék, a korábbi teal helyett) pedig egy friss, de ugyanígy WCAG-kontraszttal ellenőrzött választás (7,3-9,1:1 minden felületen). A felhasználó jóváhagyta ("jó") - ez a kör ezt élesítette a valódi `web/dashboard.css`/`web/dashboard.js`-ben.
    - **Token-szintű átszínezés** (`:root` `web/dashboard.css`-ben): a névkészlet nagyrészt megmaradt (`--dc-bg`, `--dc-surface`, `--dc-accent`, `--dc-danger`, `--dc-warning`, stb.) - csak az ÉRTÉKEK cserélődtek a validált palettára, plusz új tokenek: `--dc-text-faint` (egy még halványabb, "caption" szintű szürke), `--dc-accent-strong`, `--dc-status-good`/`--dc-status-good-dim` (ÚJ - lásd lent), `--dc-border-subtle` (halványabb, belső/ismétlődő elválasztókhoz), `--dc-radius-xs`.
    - **A lényegi, nem csak esztétikai változás: a "jó" állapot mostantól saját színt kapott, nem az akcentszínt** - eddig ugyanaz a teal jelezte az "OK"/pulse meter "Ready" állapotot ÉS minden gombot/linket/fókusz-gyűrűt, tehát egy státusz-jelzés és egy "ez kattintható" jelzés ugyanaz a szín volt. Egy rendes státusz-paletta (a saját dataviz skill is) ezt kifejezetten tiltja: a státusz-színek fenntartottak, sosem esnek egybe a márka/interakciós színnel. Minden olyan helyen, ahol egy szín valójában egy ÁLLAPOTOT (nem egy interaktív vezérlőt) jelzett, `--dc-accent` helyett `--dc-status-good`-ra cserélve: a "OK"/"Ready" jelvény, a fejléc kapcsolódás-pötty, a Wi-Fi jelerősség-sávok 3-4. szintje, a pipa-ikon hover színe (Confirm/Save). Ahol a szín valóban interaktív/választás-állapotot jelez (fókusz-gyűrűk, aktív navigáció, gomb alapállapot, "szerkesztés módban van" sor-kiemelés, kapcsoló ON állapota), ott maradt `--dc-accent`.
    - **A Devices táblázat szerkezete átalakítva a mockup szerint**: minden `<td>`-nek külön, mind a 4 oldalán kerete volt (`border-top`+`border-bottom`+szélső cellák `border-left`/`border-right`) - ez most EGY vékony `border-top` soronként (az első sor kivételével), `--dc-border-subtle` színnel, nem `--dc-border`-rel. A táblázat saját kártya-kerete (korábban minden `<td>`-n külön-külön kellett szimulálni sarok-lekerekítéssel) most egy ÚJ, közös wrapper divre (`.dc-devices-card`, `web/dashboard.js`'s `ensureDeviceTable()`) került, ami a táblázatot ÉS az alatta lévő eszköztárat (Add Pulse Meter/Add Modbus Device/Scan Bus) EGYETLEN folytonos kártyaként fogja össze, a kettő között csak egy vékony elválasztóvonallal - pontosan úgy, ahogy a jóváhagyott mockup mutatta, nem két külön, egymás alatti dobozként.
    - **Monospace/tabular számjegyek minden élő leolvasásnál és címnél**: Home oldal metrikák (`.dc-metric .v`), a Devices táblázat cím-hint-je (`.dc-pressure-addr-hint`), a Modbus Address beviteli mező, a manuális Add Modbus Device cím mezője - mind monospace-ra váltva (a mértékegység/label rész marad a normál UI betűtípusban).
    - **Ikon-vonalvezetés egységesítve és vékonyítva**: 1,6-1,8px-ről 1,5px-re, mindenhol (nav, Home kártya fejléc-ikon, táblázat sor-ikonok, típus-ikon) - egy konzisztens vonalsúly az egész alkalmazásban, nem soronként/szekciónként eltérő érték.
    - **Home kártya fejléc-ikonja** (gauge/water): eddig `--dc-accent` színű volt, most `--dc-text-muted` (halványabb, kategória-jelölő, nem interaktív elem - konzisztensen a Devices táblázat saját típus-ikonjával).
    - **Nyitva hagyott**: a Diagnosztika és Log oldalak, valamint a Service oldal generikus mezőkártyái (`.dc-field`) NEM kaptak teljes szerkezeti újratervezést ebben a körben (csak a token-szintű szín-/keret-frissítést, ami automatikusan érvényesül rájuk is) - a mockup és a felhasználói visszajelzés kifejezetten a Devices táblázatra és a Home kártyákra fókuszált, egy teljes, minden oldalra kiterjedő elrendezés-újratervezés vizuális ellenőrzés nélkül (hardveren nem tesztelhető innen egy teljes körre) túl kockázatos lett volna egyetlen körben.
  - **Huszadik kör, felhasználói visszajelzésre, 2026-08-15 - a manuális Modbus-hozzáadás eltávolítása, biztonsági kemény tiltás, és több UX-hiba**:
    - **A manuális "Add Modbus Device" gomb és a mögötte lévő teljes logika eltávolítva** - explicit felhasználói döntés ("csak bekavar"): a szkennelés-alapú felfedezés (Scan/Find Modbus Devices → "New device" sorok) marad az egyetlen módja Modbus eszköz felvételének. `mountDeviceAddButtons()` mostantól csak az "Add Pulse Meter" gombot építi, a `deviceAddType`/`pendingManualAddAlert` állapot és a hozzá tartozó al-form (`.dc-device-add-subform`, `updateDeviceAddSubformVisibility()`) törölve; a `deviceAddOpen` egyszerű logikai kapcsoló lett, a fennmaradó Add-sor (`buildDeviceAddRow()`) mostantól kizárólag Pulse Meter-specifikus, egyetlen lapos formként.
    - **"Scan Bus" gomb átnevezve "Find Modbus Devices"-re** - a régi név nem árulta el, mit csinál valójában a gomb; a tényleges entitásnév is átírva (`water-collector.yaml`'s `pressure_scan` gomb `name:` mezője), hogy a JS-oldali egyeztetés valóban a fordított firmware-hez illeszkedjen.
    - **"Address N" felirat → "Modbus address: N"** - a puszta "Address 1" szöveg egy közeli beviteli mező mellett a böngésző címzés-kitöltési (autofill) heurisztikáját triggerelte, felajánlva a felhasználó saját lakcímét; a szöveg átfogalmazásával (mind az új, mind az ütköző sorok cím-hint-jénél) ez a trigger megszűnt, kiegészítve az érintett beviteli mezők `autocomplete="off"` beállításával.
    - **Enter/Esc a szkennelt "New device" sor név mezőjében** - eddig csak a (most megszűnt) manuális Add-formban működött; most az `upsertNewPressureRow()` név mezője is ugyanazt a mintát kapta (Enter → Confirm, Esc → Dismiss), ugyanúgy, mint a többi szerkeszthető sor a táblázatban.
    - **Súlyos biztonsági hiba: Modbus-cím ütközés szerkesztéskor csak lágy, átkattintható figyelmeztetés volt** - a felhasználó valós incidenst jelentett: sikerült egy már regisztrált eszköz címét egy másik, szintén regisztrált eszköz címére állítania, ami azonnali, valódi busz-ütközést okozott, csak a hardver leválasztásával/újrabekötésével volt javítható. Ez a `change_address_and_save()` útvonal (`upsertRegisteredPressureRow()` mentés-kezelője), ami TÉNYLEGESEN újraprogramozza a fizikai szenzort - egy `confirm()`-mel átkattintható figyelmeztetés itt nem elegendő védelem. **Javítva**: a duplikált cím felismerése mostantól kemény tiltás (`alert()` + a szerkesztés nem zárható le), nincs felülbírálási lehetőség - a felhasználónak más címet kell választania, vagy előbb törölnie/módosítania az ütköző slotot.
    - **Pulse meter Add: legördülő "beragadt" fókusza** - a gyökérok az volt, hogy `refreshDeviceAddRow()` minden `renderDeviceTableBody()` hívásnál (ami regisztrált slotoknál gyakorlatilag minden pollozásra, ~félmásodpercenként lefut) feltétel nélkül újraépítette a `<select>` `innerHTML`-jét, akkor is, ha a szabad slot-halmaz nem változott - egy natív `<select>` opcióinak cseréje, amíg a böngésző saját legördülő felugrója esetleg nyitva/interakció alatt van, ismert módon okozhat pont ilyen "nem enged el a fókusz" tünetet. **Javítva**: a szabad slot-halmazt egy string-kulcsként gyorsítótárazva (`row._slotKey`) a DOM csak akkor módosul, ha a halmaz ténylegesen változott.
    - **Pulse meter Add: legördülő elnevezés "IO 1"/"IO 2"-re cserélve** - a korábbi "Pulse Meter 1"/"Pulse Meter 2" a felhasználó szerint furcsa volt; az új `pulseSlotOptionLabel()` az "IO N" formát adja, ami megegyezik a fizikai eszközre nyomtatott felirattal.
    - **Elavult "Collision"/"New device" sor túlélte a page refresh-t** - kiderült, hogy a "Tizenhetedik kör"-ben bevezetett `initialSettled` kapu (egyszeri, session-egész igaz-váltás a kezdeti SSE-dump lecsengése után) NEM védett egy KÉSŐBBI, teljesen független renderelés (pl. egy rutin nyomásmérő-pollozás) ellen, ami már `initialSettled === true` mellett futott le, de a "Scan Results"/"Scan Collisions" entitás még sosem kapott TÉNYLEGES friss frissítést ebben a session-ben - vagyis egy, még a kapcsolódás előttről származó elavult scan-eredmény simán "átcsúszhatott" a kapun, amint az bármilyen más okból kinyílt. **Javítva**: egy külön, szűkebb `scanResultsFresh` jelző, ami csak akkor válik igazzá, ha a "Scan Results"/"Scan Collisions" entitás ténylegesen frissül, MIUTÁN `initialSettled` már igaz - így egy még soha meg nem újított, elavult scan-eredmény véglegesen rejtve marad, amíg tényleg nem történik új szkennelés.
  - **Huszonegyedik kör, felhasználói visszajelzésre, 2026-08-15 (ua. nap) - oldal-átnevezések, gomb-áthelyezés, táblázat-finomítás**:
    - **Add Pulse Meter sor: a névmező megőrizte az előző hozzáadás nevét** - a hozzáadás-sor (`buildDeviceAddRow()`) egyszer épül fel és minden nyitás/zárás között újrahasznosítva marad, de sem sikeres hozzáadás után, sem Cancel-nél nem törlődött a beírt név - a következő megnyitáskor a régi szöveg (pl. "pm1") még mindig ott volt. **Javítva**: mindkét kilépési út (sikeres Add, Cancel), valamint `closeDeviceAddRow()` (amit egy másik sor szerkesztésbe lépése is meghív) most explicit üresre állítja a mezőt.
    - **"Service" oldal átnevezve "Devices"-re, "Diagnostics" "System"-re** - explicit felhasználói észrevétel: a Service oldal mára ténylegesen csak az egységesített eszköz-táblázatot mutatja (a kalibrációs mezők már a "Tizenötödik kör" óta magába a táblázat sor-szerkesztésébe költöztek), a régi név félrevezető lett. A "Diagnostics" új neve "System", mivel most már nemcsak olvasható diagnosztikai adatokat mutat, hanem a lejjebb részletezett eszköz-szintű akció-gombokat is befogadja. Csak a megjelenő címke változott (`PAGES` tömb `label` mezője) - a belső oldal-id-k (`service`/`diagnostics`), elem-id-k és függvénynevek (`ensureServiceGroup()` stb.) változatlanok, ugyanaz a minta, amit a "home"→"Dashboard" átnevezés is követett korábban.
    - **"Restart" gomb átnevezve "Reboot Device"-ra** - a felhasználó két javaslata közül ("Device Reboot" / "Reboot Device") a második mellett döntöttem, mert így illeszkedik a felületen már bevett, ige-elöl elnevezési mintához ("Find Modbus Devices", "Add Pulse Meter"). A tényleges entitásnév is átírva (`water-collector.yaml`), a hozzá tartozó JS-oldali egyeztetések (megerősítő párbeszéd szövege, `CONFIRM_ON_PRESS` halmaz) is frissítve.
    - **"Reboot Device" és "Forget Wi-Fi" áthelyezve a Devices oldalról a System oldalra** - explicit kérés, ugyanabból az indokból, ami a "Devices"/"System" átnevezést is kiváltotta: ez a két gomb nem egy adott eszköz-sorhoz tartozik, hanem magához az egységhez, így logikailag jobban illik a System oldal többi, ugyanerről az egységről szóló adata mellé. Megvalósítás: a Diagnosztika-csoportok (`diagGroups`) mostantól - a Service-csoportokéval (`serviceGroups`) azonos, újrafelhasznált `.dc-fields`/`.dc-field` stílussal - egy `fields` konténert is kapnak a puszta `list` (label/érték sorok) mellé (`upsertDiagButton()`, `reorderDiagFields()`), és a `render()` a két gombot név szerint ide irányítja át a `pageFor()` alapértelmezett szabálya (`entity_category: config` → Devices oldal) helyett. Az entitások `entity_category`-ja a YAML-ban szándékosan változatlan maradt (`config` - ez Home Assistant felől helyes marad, ez pusztán a dashboard saját, helyi elhelyezési döntése, ugyanaz a minta, mint a "Debug Log: Modbus" kapcsolónál).
    - **Devices táblázat sűrítve** - a sorok közötti belső cellapadding `10px 12px`-ről `7px 12px`-re csökkentve, hogy a táblázat tömörebb, "adattábla-szerűbb" benyomást keltsen, telefonon még mindig kényelmesen érinthető méretben maradva.
    - **Eszköztípus-ikonok kicsit nagyobbra és a sor közepéhez igazítva** - az ikon mérete `15px`-ről `17px`-re nőtt (a vonalvastagság maradt `1.5`, konzisztensen a felület többi ikonjával), és a névsor flex-elrendezése `align-items: flex-start`-ról `center`-re változott - az előbbi eredetileg a két soros (cím-hint + név) Új/Ütköző sorokhoz készült, de a gyakoribb, egysoros regisztrált soroknál láthatóan nem a sor közepére igazította az ikont.
    - **MAC-cím kiírva** - új "MAC Address" diagnosztikai szöveg-entitás (`wifi_info` platform `mac_address:` mező, `sorting_group_network`), a System oldal Network szekciójában, IP Address/Connected SSID mellett.
    - **Fejlesztési ötletek, dokumentálva, még nem implementálva** (a felhasználó saját szavaival "fejlesztési ötlet", nem azonnali kérés): log-letöltés, MQTT-integráció, hálózati konfiguráció (fix IP stb.) - ld. saját, önálló szakaszaikat lent, "tervezett bővítés" jelöléssel.
  - **Huszonkettedik kör, felhasználói visszajelzésre, 2026-08-15 (ua. nap) - további finomítások, log-letöltés élesítve**:
    - **Duplikált "Devices" felirat** - a `ensureDeviceTable()` saját, önálló `dc-section-label`-je ("Devices") közvetlenül a lap saját, szintén "Devices" h1-je alatt jelent meg, feleslegesen - eltávolítva, mivel ez az egyetlen szekció ezen az oldalon (ha valaha lenne második szekció, vissza kell tenni).
    - **"Reboot Device"/"Forget Wi-Fi" gombok túl szélesek voltak** - kiderült, hogy az első verzió (a "Huszonegyedik kör"-ben) a Devices-oldali gombokéval megegyező, bekeretezett `.dc-field` kártya-elrendezést használta, ami `.dc-btn` saját `width: 100%`-a miatt a teljes oldalszélességet kitöltötte - a Devices oldalon ez a `.dc-service-group`-ra állított 560px-es max-width miatt nem tűnt fel, de a System oldal `.dc-diag-group`-jának nincs ilyen korlátja. **Javítva**: a gombok mostantól nem egy bekeretezett kártyában, hanem egy önálló, tartalom-méretű (`.dc-btn-compact`) gombokból álló, sima flex sorban (`.dc-diag-actions`) jelennek meg - ugyanaz a vizuális súly, mint a Devices oldal eszköztár-gombjainak.
    - **Pulse Rate, Total Pulses, Sort Order eltávolítva a System oldalról** - mindhárom nyers/kalibrálatlan diagnosztikai adat volt, aminek ma is jobb, kalibrált megfelelője van máshol (Pulse Rate/Total Pulses → Flow Rate/Total Consumption a Home/Devices oldalon; Sort Order tényleges hatása, a sorrend, már úgyis látszik a Devices táblázat sorrendjén) - a nyomásmérőknél ez a hármas eleve sosem jutott el a System oldalra (`renderPressureEntity()` teljesen megkerüli az általános Diagnostics-elágazást), csak a pulse metereknél volt jelen, aszimmetrikusan - most már egyik eszköztípusnál sem jelenik meg.
    - **Log letöltés - implementálva** (nem csak dokumentálva, ld. lent, önálló szakasz).
  - **Huszonharmadik kör, felhasználói zöld jelzésre ("implementálható!"), 2026-08-15 (ua. nap) - log-letöltés és MQTT élesítve, statikus IP-nél valódi akadály**: ld. a "Log letöltés"/"MQTT-integráció"/"Hálózati konfiguráció bővítése" szakaszokat lent a teljes részletekért.
    - **Log letöltés és MQTT-integráció: mindkettő ténylegesen elkészült ebben a körben**, nem csak dokumentálva - a felhasználóval időközben tisztázott jelszó-tárolási döntés ("plaintext a flash-en, sosem olvassa vissza kitöltve") alapján az MQTT rész is lezárható volt kérdés nélkül.
    - **System oldal kibővítve valódi beviteli mezők (`.dc-field` kártyák) befogadására**, nemcsak a "Huszonegyedik kör"-ben bevezetett sima akció-gombokra (`.dc-diag-actions`) - a diagnosztikai csoportok (`ensureDiagGroup()`, `web/dashboard.js`) mostantól egy harmadik, `fields` nevű területet is kapnak (`.dc-fields.dc-fields-capped` - ugyanaz a kártya-stílus, mint a Devices oldalé, de saját 560px-es max-width-del, mivel a System oldalnak nincs `.dc-service-group`-szerű beágyazó korlátja). `upsertServiceNumber()`/`upsertServiceText()`/`upsertServiceSwitch()` mindhárom kapott egy opcionális `ensureGroupFn` paramétert (alapértelmezetten a Devices oldalra mutat, `ensureDiagGroup`-pal átirányítható a System oldalra) - ez tette lehetővé, hogy az MQTT beállításai a System oldalon jelenjenek meg, ne a (mára ismét csak eszköz-táblázatot jelentő) Devices oldalon.
    - **Hálózati konfiguráció (statikus IP): implementáció NEM indult el** - a `wifi_component.cpp` tényleges forráskódjának elolvasásával megerősített, valódi architekturális akadály miatt (ld. saját szakasza lent) - ez egyedüli kivétel a három "implementálható!" jelzést kapott ötlet közül, amihez a felhasználóval még egyeztetni kell, mielőtt belekezdenénk.
    - **Mellékesen felfedezett, dokumentált tanulság**: az MQTT `discovery:` beállítása (true/false) NEM befolyásolja, hogy az `mqtt:` komponens jelenléte önmagában entitásonként egy-egy statikus wrapper-objektumot foglal-e le (106-ot, ebben a projektben) - ezt csak a tényleges generált C++ kód megvizsgálásával (nem a Python-séma feltételezésével) lehetett megerősíteni, ld. az MQTT szakasz saját megjegyzését.
  - **Huszonnegyedik kör, felhasználói visszajelzésre, 2026-08-15 (ua. nap) - MQTT UX-finomítás, fejléc-ikonok, statikus IP lezárva**:
    - **Hálózati konfiguráció (statikus IP): lezárva, a felhasználó nem kéri** ("nem kell a statikus IP") - a lenti architekturális akadály ismeretében marad a jelenlegi, kizárólag DHCP-alapú működés.
    - **DHCP "renew" (új IP kérése) gomb - felvetve, de elengedve** - a felhasználó saját maga jelezte, hogy ha bonyolult, nem fontos ("Ha nem lehet vagy bonyolult akkor el lehet engedni") - a `wifi_component`-nek nincs erre publikus API-ja (ugyanaz a fajta korlát, mint a statikus IP-nél), ezért nem implementáltuk.
    - **Unit Temperature 2 másodpercenként frissül** (volt: ESPHome saját `PollingComponent` alapértelmezése, 60s) - ugyanaz a panasz/javítás, mint korábban a Wi-Fi Signal-nál, most ugyanígy megoldva (`update_interval: 2s` - olcsó, on-die ADC-olvasás, nincs valódi költsége a gyakoribb pollozásnak).
    - **MQTT UX újratervezve, közvetlen visszajelzés alapján** - ld. saját "MQTT-integráció" szakasza lent a teljes, frissített részletekért. Röviden: nincs többé külön Port mező (egy "MQTT Broker" mező fogadja a `host`/`host:port`/`scheme://host:port` formákat is, `include/mqtt_broker_url.h` fejti vissza); új, szerkeszthető "MQTT Topic Prefix" mező (a felhasználó saját kérdésére válaszul); "MQTT Enabled" szétválasztva egy tiszta "csak töröl, ha kikapcsolva" mester-kapcsolóra, plusz külön "MQTT Connect"/"MQTT Disconnect" gombokra (MQTT mostantól SOHA nem csatlakozik automatikusan, csak explicit Connect gombnyomásra - reboot után is); a jelszó-mező most már ténylegesen maszkolt bevitelt kap (kiderült: az előző kör client-side maszkolása hiányzott, `input.type` sosem lett `password`-re állítva) + szem-ikonos felfedés-kapcsolót + egy fix, 8 pontból álló placeholder-t, ha már van mentett jelszó.
    - **Valódi ESPHome-viselkedés felfedezve `web_server.cpp` forráskódjából**: egy `mode: password` `text:` entitás jelentett `value`-ja a SZERVER OLDALON, feltétel nélkül, "********"-re cserélődik (`text_json_()`), FÜGGETLENÜL a tényleges tartalomtól - emiatt "üres" és "van benne valami" a kliens felől MEGKÜLÖNBÖZTETHETETLEN lett volna, ha ez a mechanizmus marad. Megoldás: a látható "MQTT Password" mező `mode: text`-re váltva (NEM `password`-re) - a maszkolás (mind a gépelés közbeni pöttyök, mind a "van már jelszó" placeholder) mostantól teljesen kliens-oldali, dashboard.js-ben kézzel vezérelt, a szerver a valós ("" vagy a fix placeholder) állapotot hűen továbbítja.
    - **Fejléc: MQTT állapot-ikon a Wi-Fi mellett** - rejtett, ha az MQTT nincs engedélyezve; szürke, ha engedélyezve van, de nincs kapcsolódva; kék, ha kapcsolódva ("Connected").
    - **Fejléc: Wi-Fi jelző függőleges csíkok helyett köríves ("wifi ívek") ikon, és nem tier-szerinti szín (piros/sárga/zöld) többé, hanem mindig kék** - a felhasználó explicit, a korábbi (Tizenkilencedik kör) "a státusz-színek sosem eshetnek egybe a márka/akcentszínnel" elvét felülíró kérése ("az is kék") - ugyanaz a minta, mint a "Tizenharmadik kör"-ben, amikor egy korábbi, óvatosságból hozott tervezési döntést a felhasználó explicit felülbírált. **Módosítva a "Huszonötödik kör"-ben** - a "mindig kék" változatot a felhasználó ugyanaznap visszavonta ("Wi-Fi jelző lehet piros/sárga/kék") - lásd lent.
  - **Huszonötödik kör, felhasználói visszajelzésre, 2026-08-15 (ua. nap) - MQTT-reconnect finomhangolás, Topic Prefix garantált érték, Wi-Fi szín visszaállítva**:
    - **MQTT reboot utáni újracsatlakozás - pontosított logika**: a "Huszonnegyedik kör"-ben teljesen eltávolított automatikus újracsatlakozás túl radikálisnak bizonyult - a felhasználó pontosított kérése: "reboot után automatikusan kell kapcsolódnia ha már egyszer connectált. Ha disconnectáltunk kézzel akkor reboot után se connectáljon." Megvalósítva egy új, perzisztens (`restore_value: true`) `globals: mqtt_want_connected` boolean-nel - ez KÜLÖNBÖZIK az "MQTT Enabled" kapcsolótól (ami mostantól pusztán egy törlés-kapcsoló, nem jelzi, hogy "kapcsolódva kellene lennie"): "MQTT Connect" gombnyomás `true`-ra állítja, "MQTT Disconnect" és "MQTT Enabled" kikapcsolása `false`-ra - egy visszaállított `esphome: on_boot:` blokk (priority 200, `AFTER_WIFI`) ezt a flaget nézi, nem magát az Enabled kapcsolót, és ha igaz, lefuttatja ugyanazt a kapcsolódási logikát, mint a Connect gomb.
    - **"MQTT Topic Prefix" garantáltan ki van töltve, nem csak hint** - direkt visszajelzés ("legyen default kitöltve valamivel, ne csak hint"). Bár a `restore_value: true` + `initial_value: "${device_name}"` kombináció elvben már magától is ezt kellett volna eredményezze első bootnál, egy explicit, "biztos ami biztos" `esphome: on_boot:` lépés is hozzáadva, ami üres állapot esetén mindig kitölti az eszköz nevével - nem hagyatkozik kizárólag a restore-mechanizmus feltételezett (de éles hardveren nem 100%-ban megerősített) viselkedésére.
    - **Wi-Fi jelző színezése visszaállítva tier-alapúra, de zöld helyett kékkel** ("Wi-Fi jelző lehet piros/sárga/kék") - gyenge jel: piros, közepes: sárga/borostyán, jó/kiváló: kék (a korábbi, "Tizenkilencedik kör"-ben bevezetett zöld helyett, ami mostantól az akcentszínt kapja) - ugyanaz a "kumulatív" színezési minta, mint az eredeti függőleges-csík verzióban (az aktuálisan világító elemek MIND az aktuális tier színét kapják, nem egy fix, elemenkénti szín).
  - **Huszonhatodik kör, felhasználói visszajelzésre, 2026-08-15 (ua. nap) - MQTT-doboz újratervezve, MQTT bootolási log-szennyezés kiküszöbölve, Devices táblázat tovább vékonyítva, pulzusmérő-státusz újragondolva**:
    - **MQTT fejléc-ikon lecserélve** - a felhasználó saját referenciaképe alapján (kerekített négyzet, bal alsó sarokból induló, koncentrikus "broadcast" ívekkel, az általános MQTT-logó alakja) - ugyanabban a vékony, kontúros ikon-stílusban rajzolva újra, mint minden más ikon ebben a fájlban, nem egy kitöltött jelvényként átvéve.
    - **MQTT beállítások: egyetlen, összefüggő kártya, csak "MQTT Enabled" bekapcsolva nyílik ki, a két gomb alul, egymás mellett** - közvetlen visszajelzés ("az enabled-re nyíljanak meg csak a komponensek... a megnyílt komponens csoport alján legyen a két gomb egymás mellett... az attribútumokat ne tedd külön dobozba, az egész szépen egy doboz legyen"). A `.dc-fields-mqtt` konténer kapta meg a kártya-keretet/hátteret (az egyes `.dc-field`-ek elvesztették a sajátjukat - `background: none; border: none`), egy új `updateMqttFieldsVisibility()` a `.dc-mqtt-collapsed` osztályt kapcsolja "MQTT Enabled" állapota szerint (csak maga a kapcsoló-mező marad látható összecsukva), és egy új `upsertMqttActionButton()` a "MQTT Connect"/"MQTT Disconnect" gombokat egy közös, alul elhelyezkedő, oldalt-oldal mellett flex sorba (`.dc-mqtt-button-row`) rendezi, a mezők alá.
    - **MQTT bootolási log-szennyezés: gyökérok megerősítve egy valódi eszköz-log alapján, kijavítva** - a felhasználó csatolt egy valódi boot-logot ("Induálskor miért fosta tele a logot mqtt sorokkal? Nem is kéne alapból mqtt-vel foglalkoznia"); ebben megerősítve: pusztán az `mqtt:` blokk KONFIGURÁLT LÉTE (a "Huszonharmadik kör"-ben már felfedezett 106 statikus wrapper-objektum mindegyike) minden egyes bootnál kb. 200+ sornyi `[C]`-szintű `dump_config()`-kimenetet ír, `enable_on_boot: false` mellett is - ez a 16 kB-os `log_ring_buffer` kapacitásának nagy részét elviheti, még mielőtt bármi ténylegesen hasznos log keletkezne, függetlenül attól, hogy az MQTT-t egyáltalán használja-e valaki. **Megoldás**: a teljes MQTT-funkcionalitás (a `mqtt:` blokk, mind az öt `text:` mező, a "MQTT Enabled" switch, a "MQTT Connect"/"MQTT Disconnect" gombok, a "MQTT Status" text_sensor, a `mqtt_want_connected` global, a `sorting_group_mqtt` és a reconnect-on-boot `esphome: on_boot:` lépés) kiemelve egy önálló, opcionális `packages/mqtt.yaml` fájlba, amit `water-collector.yaml` saját `packages:` szótára egyetlen `mqtt: !include packages/mqtt.yaml` sorral von be - ez a sor kikommentezhető/törölhető, ha valaki MQTT nélküli buildet akar, és attól kezdve a 106 wrapper-objektum, a 200+ soros bootolási log-szennyezés, és minden hozzá tartozó RAM/flash-költség egyszerűen nem létezik. ESPHome `packages:` mechanizmusa (lista-típusú kulcsok, pl. `switch:`/`button:`/`text:`/`globals:`/`esphome: on_boot:`, összefűződnek a fő fájl és a becsatolt package(k) között) tette lehetővé a tiszta szétválasztást kódduplikáció nélkül - ellenőrizve `esphome config` + `esphome compile` (forráskód-generálásig) + a generált `main.cpp` kézi átvizsgálásával, hogy minden entitás/globális/sorting_group pontosan úgy jelenik meg összefésülve, mint korábban egyetlen fájlban.
    - **Devices táblázat: attribútum-mezők szélessége igazítva az adattípushoz** - közvetlen visszajelzés ("az attibútumok komponensei túl szélesek. Igazodjon a beviteli mező adattípusához"). A sorba nyíló szerkesztő-mezők (Modbus Address, Reading, Zero-Flow Timeout) korábban mind a teljes fennmaradó sorszélességet kitöltötték (`flex: 1 1 auto`); mostantól a szám-mezők (`input[type="number"]`) fix, 100px széles, tartalom-arányos mezők - egy 1-247 közötti Modbus-cím vagy egy pár számjegyű időkorlát nem indokolja a teljes sorszélességet, egy szabad szöveges mező (ha valaha lenne ilyen ebben a blokkban) továbbra is a régi, teljes szélességű szabályt kapná.
    - **Devices táblázat: tovább vékonyítva** - közvetlen visszajelzés, hogy a táblázat MÉG MINDIG nem táblázat-jellegű, túl vaskos (ez már a második ilyen visszajelzés, a "Huszonkettedik kör" 10px→7px csökkentése után is). A tényleges gyökérok: egy zárolt (nem szerkesztés alatt álló) sor Name mezője mindig egy teljes, bekeretezett/hátteres `<input>`-dobozként jelent meg, még olvasás közben is - emiatt nézett ki minden sor inkább egy űrlapnak, mint egy táblázat-sornak. **Megoldva**: egy `disabled` `<input>` mostantól teljesen doboz nélkül, sima szövegként jelenik meg (háttér/keret nélkül, a táblázat saját 13,5px-es betűméretével a korábbi 16px iOS-zoom-védő méret helyett - egy letiltott mező úgysem fókuszálható, nincs zoom-kockázat). Emellett a cellák belső margója 7px 12px → 4px 10px, az ikon-gombok margója 6px → 4px, a fel/le nyíl-gombok margója 2px → 1px (ikon 13px → 12px) - ez utóbbi kettő bizonyult a sor tényleges magasságát meghatározó elemnek, miután a Name mező már nem doboz.
    - **Pulzusmérő-státusz újragondolva, a felhasználó saját ötlete alapján implementálva** - közvetlen visszajelzés: a pulzusmérő badge-je eddig mindig a "Ready" szót mutatta, függetlenül bármitől ("csak ready, nem látom értelmét - adj javaslatokat"), saját ötlettel is: "mutatjuk hogy aktív mert volt pulse, de amikor lenullázzuk a flow-t akkor valamiféle várakozást mutatunk". **Megvalósítva pontosan ez az ötlet, új backend-entitás nélkül** - a `water_meter.yaml` zero-flow watchdogja már ma is nullázza a "Pulse Rate" entitást, amint a Zero-Flow Timeout lejár az utolsó impulzus óta; a dashboard mostantól ugyanezt az entitást olvassa a badge vezérléséhez: **"Flowing"** (zöld, ugyanaz a szín-osztály, mint a Modbus "OK") amíg a Pulse Rate nem nulla, **"Idle"** (semleges szürke, ugyanaz, mint a Modbus "Checking…"/"New" - szándékosan NEM a piros/borostyán "Lost" tónusa, mivel a nulla áramlás teljesen normális állapot, nem hiba) amint nullára esik vissza. A korábbi, per-impulzus "gyűrű-felvillanás" (`flashPulseMeterActivity()`) változatlanul megmaradt, mindkét státusz fölött megjelenik. A Modbus-oldal saját státuszkészlete (OK/Lost/New/Collision/Checking…) változatlan maradt - a felhasználó szerint is logikusnak tűnt -, de ugyanazt a szín-nyelvet használja (zöld=jó/aktív, szürke=semleges/várakozó, piros=probléma), így a két eszköztípus vizuálisan egységes, még ha a szóhasználat (Flowing/Idle vs. OK/Lost) szándékosan eltér is, a mögöttes fogalmak (kapcsolat megléte vs. áramlás ténye) valódi különbsége miatt. **Nyitva hagyott alternatívák, ha a felhasználó másképp szeretné**: más szóhasználat ugyanerre a két állapotra (pl. "Active"/"Waiting", "On"/"Standby"), vagy egy harmadik, "sosem volt impulzus" kezdőállapot megkülönböztetése az "impulzus volt, majd elállt"-tól (Total Pulses == 0 alapján) - ez utóbbit szándékosan nem vezettük be, mivel egy friss, de fizikailag még be nem kötött mérő és egy már bekötött, de pillanatnyilag zárt csapos mérő a jelenlegi jelkészletből nem különböztethető meg megbízhatóan.
  - **Huszonhetedik kör, felhasználói visszajelzésre egy második valódi boot-logra, 2026-08-16 - MQTT log-csendesítés futásidőben, valódi MQTT-logó, két megerősített JS-hiba javítva, egy negyedik pont még nyitott**:
    - **MQTT bootolási log-szennyezés: a valódi maradék ok pontosítva és külön is kijavítva** - a felhasználó egy MÁSODIK, a "Huszonhatodik kör" push-a UTÁN készült boot-logot csatolt, ami ugyanazt a ~200 soros `[C]`-szintű MQTT `dump_config()`-kimenetet mutatta - ez NEM regresszió, hanem a "Huszonhatodik kör" package-kiemelésének egy pontosításra szoruló mellékhatása: az új `packages/mqtt.yaml` sor alapértelmezetten BEKAPCSOLVA maradt (helyesen - a felhasználó továbbra is használja az MQTT-t), tehát a spórolás csak akkor jelentkezett volna, ha valaki teljesen kikommentezi a sort. A felhasználó pontosabb kérdése ("nem is kéne futásidőben ezekkel foglalkoznia, ha mqtt enabled") tisztázást igényelt: a `dump_config()` NEM egy futásidejű "MQTT enabled?" ellenőrzés eredménye, hanem minden ESPHome-komponens saját, egyszeri, a fordításba kerülés miatt automatikusan lefutó indulási önjellemzése - ez a viselkedés maga NEM hiba, és YAML-szinten nem is kapcsolható "csak ha enabled" feltételhez, mert az azt kiíró kód magáról a dashboard saját "MQTT Enabled" kapcsolójáról mit sem tud. Ami VISZONT valódi, YAML-szintű megoldás: a `logger: logs:` beállítás - minden érintett MQTT-wrapper log-tag (`mqtt`, `mqtt.sensor`, `mqtt.binary_sensor`, `mqtt.switch`, `mqtt.text`, `mqtt.text_sensor`, `mqtt.number`, `mqtt.button` - mind a nyolc, a csatolt logból sorról sorra azonosítva) szintje `WARN`-ra állítva `packages/mqtt.yaml`-ban - ez a naplózó makró saját szintjén szűri ki a `CONFIG`-szintű dump_config-sorokat MÉG MIELŐTT bármi kiírásra kerülne (nem csak elrejti, ténylegesen nem is fut le a formázás/kiírás), miközben egy valódi `[W]`/`[E]` futásidejű MQTT-hiba (pl. sikertelen kapcsolódás) továbbra is látszana, mert az ugyanazon a tag-en, de magasabb súlyossággal logolna. Megerősítve a generált `main.cpp`-ben: a nyolc `logger_id->set_log_level(...)` hívás `App.setup()` legelején fut le, jóval bármelyik MQTT-wrapper saját `setup()`/`dump_config()`-ja előtt - tehát a szűrés garantáltan érvényesül minden egyes wrapper elindulása előtt, nem csak elviekben. A `packages:` `logger:` blokkja (csak `logs:` kulccsal) `water-collector.yaml` saját `logger:` blokkjával (id/level/initial_level) egy közös, összefésült szótárrá olvad össze ESPHome `packages:` mechanizmusán keresztül, ugyanúgy, mint minden más kulcs eddig.
    - **MQTT fejléc-ikon: a valódi MQTT-logó, nem egy kézzel rajzolt közelítés** - közvetlen visszajelzés: "mqtt ikonra keress rá és jó minőségben tedd be, ez most el van mosódva mert az én képemből generáltad" (keress rá a valódi ikonra és tedd be jó minőségben, most életlen, mert a saját képemből generáltad). A "Huszonhatodik kör" saját, vékony vonalú közelítése (a felhasználó képéből kézzel újrarajzolva) valóban elmosódottnak hatott 17px-en - vékony, ívelt vonalak rosszul simulnak kis méretben. **Megoldás**: a Simple Icons projekt (simpleicons.org, MIT/CC0) hivatalos, pontos "mqtt" ikon path-adata (a közvetlen CDN a sandbox hálózati szabályzata miatt blokkolva volt, `npm pack simple-icons` a `registry.npmjs.org`-on keresztül - az explicit engedélyezett listán szereplő host - működött) - egy tömör, kitöltött (nem körvonalas) alakzat, ami kis méretben is élesen jelenik meg, a fájl minden más ikonjától eltérően (azok mind vékony körvonalasak) - a `#dc-mqtt-status svg` saját CSS-e emiatt `fill: currentColor; stroke: none;`-ra váltott a többi ikon `stroke`-alapú mintája helyett, mérete 17px→15px (arányosan illeszkedik a Wi-Fi ikon mellé egy tömör alakzatként).
    - **Hiba, megerősítve és javítva: a pulzusmérő státusz-jelző indokolatlanul felvillant sorrend-váltáskor, és néha magától is** - közvetlen visszajelzés: "pulse státusz jelző mellett akkor is pulzál ha sorrendet váltok és még néha meghatározhatatlan időben csak pulzál egyet". Kódvizsgálattal megerősített valódi hiba: `renderPulseMeterEntity()` (dashboard.js) egy adott mérő csoportjában BÁRMELY entitás frissítésekor (nem csak "Total Pulses"-nél) újra végigfuttatja az ÖSSZES ismert entitást a kalibrációs elágazáson - ezt egy Sort Order-váltás (Fel/Le gomb) IS kiváltja, és a `flashPulseMeterActivity()` a "Total Pulses" újra-átfutásakor feltétel nélkül elsült, FÜGGETLENÜL attól, hogy az érték ténylegesen változott-e - ez okozta mindkét tünetet (a sorrend-váltásos felvillanást közvetlenül, a "néha magától" esetet pedig a zero-flow watchdog saját Pulse Rate=0 publikálása, ami ugyanezt az egész-csoportos újrafuttatást váltja ki). **Javítva**: egy `lastSeenTotalPulses` Map nyilvántartja meterenként az utoljára LÁTOTT "Total Pulses" értéket, és a felvillanás csak akkor sül el, ha ez ténylegesen változott az előző látott értékhez képest - az ártalmatlan, dokumentált "első Add/boot utáni villanás" (a checkpoint-seedelés miatt) továbbra is megtörténik egyszer, de minden további indokolatlan újra-villanás megszűnik.
    - **"mqtt enabled gomb nincs is" - valódi hiba, megtalálva és javítva (a "cache" gyanú tévesnek bizonyult)** - a "Huszonhetedik kör" eredeti jelentésekor egy önálló teszt-oldal a szándékolt módon renderelte a kártyát, ezért akkor egy böngésző-cache-problémát gyanítottunk; a felhasználó ezt határozottan cáfolta ("ez nem cache probléma"), ami helyes volt - alaposabb átvizsgálás egy valódi, konkrét kódhibát talált. **A gyökérok**: `displayName(entity)` (dashboard.js) minden entitás nevéből levágja a saját csoportja nevét mint prefixet (pl. "Pulse Meter 1 Display Name" -> "Display Name") - ez a csoportonkénti mezőknél helyes és szándékos viselkedés. Az MQTT csoport azonban saját maga is "MQTT" nevű, és MINDEN MQTT-entitás neve "MQTT "-vel kezdődik ("MQTT Enabled", "MQTT Status", "MQTT Broker", stb.) - így a `displayName()` ezeknél MINDIG levágja a "MQTT " előtagot, az eredmény sosem "MQTT Enabled", hanem mindig csak "Enabled" (hasonlóan "Status", "Broker", "Password", "Topic Prefix"). Több helyen (`upsertServiceSwitch()`'s "MQTT Enabled"-ellenőrzés a `.dc-mqtt-toggle-field` osztályhoz, `upsertServiceText()`'s "MQTT Password"/"MQTT Broker"/"MQTT Topic Prefix"-ellenőrzések, `mqttEntity()`'s saját `displayName(e) === label` összehasonlítása, és a `HELP_TEXT` szótár "MQTT Broker"/"MQTT Topic Prefix" kulcsai) a kód a TELJES, előtaggal ellátott nevet hasonlította a levágott `label`-hez - ezek az összehasonlítások SOSEM egyeztek, tehát ezek az ágak sosem futottak le. A tényleges, látott tünet ebből következett: mivel `mqttEntity("MQTT Enabled")` mindig `null`-t adott vissza, `updateMqttFieldsVisibility()` mindig `.dc-mqtt-collapsed`-ot alkalmazott (a kártya MINDIG összecsukva maradt, függetlenül a kapcsoló tényleges állapotától), ÉS mivel a kapcsoló saját mezője sosem kapta meg a `.dc-mqtt-toggle-field` kivétel-osztályt, az ÖSSZECSUKOTT állapotban EZ IS elrejtődött a többivel együtt - az eredmény egy teljesen üres, de a `.dc-fields-mqtt` kártya saját keretét/hátterét/lekerekítését még mindig megjelenítő doboz volt, felirat és kapcsológomb nélkül - pontosan az, amit a felhasználó képernyőképe mutatott. Egy célzott, a pontos hibás DOM-állapotot (kivétel-osztály nélküli mező, összecsukott kártya) reprodukáló teszt-oldal fejnélküli Chromiummal renderelve KÖZVETLENÜL visszaadta ugyanazt az üres, lekerekített sávot, megerősítve az elméletet vizuálisan is, mielőtt a javítás megtörtént volna. **Javítva**: minden érintett összehasonlítás mostantól a levágott (ténylegesen megjelenő) `label`-t nézi ("Enabled", "Password", "Broker", "Topic Prefix"), csoport-ellenőrzéssel (`entity.groupName === "MQTT"`) kiegészítve a védelemhez; a `mqttEntity()` helper pedig `e.name`-t (a nyers, teljes entitásnevet) hasonlítja, nem a `displayName()` levágott kimenetét - ez utóbbi elválasztja a belső, pontos névre kereső helper-t a megjelenítési logikától, elkerülve, hogy ugyanez a hiba egy jövőbeli hasonló csoport-elnevezésnél megismétlődjön. **Vele együtt egy MÁSODIK, addig észrevétlen tünet is kiderült és javult**: ugyanez a hiba miatt a "MQTT Broker"/"MQTT Topic Prefix" mezők sosem kapták meg a teljes szélességű (`.dc-field-span-2`) elrendezést sem - tehát a "Huszonötödik kör"-ben leírt "URL egy sorban, Username/Password egy másik sorban" elrendezés a gyakorlatban SOSEM működött, mindig egy nem szándékolt 2 oszlopos elrendezésben jelent meg minden mező; és a jelszó-mező szem-ikonos felfedés/maszkolás funkciója (ugyanígy "Huszonnegyedik kör") is némán soha nem aktiválódott. Mindhármat ugyanez az egy javítási kör oldotta meg.
  - **Huszonnyolcadik kör, felhasználói visszajelzésre egy harmadik valódi boot-logra, 2026-08-16 (ua. nap) - MQTT teljes újratervezés (elrendezés, connect-megbízhatóság, hibaüzenet), jelszókezelő-tiltás, szem-ikon logika**:
    - **"kétszer is kellett kattintani a connect gombra" - valódi hiba, ESPHome saját forráskódjában megerősítve** - a `MQTTClientComponent::enable()` (a telepített esphome csomag `mqtt_client.h`-ja) egy NO-OP, ha a kliens nincs éppen `MQTT_CLIENT_DISABLED` állapotban (`if (this->state_ != MQTT_CLIENT_DISABLED) return;`) - egy sikertelen kapcsolódási kísérlet után a kliens NEM ebben az állapotban van (hanem `DISCONNECTED`/`CONNECTING`/stb.), tehát egy újbóli "Connect" nyomás a friss mezőértékeket (Broker/Username/Password) elmentette ugyan, de az `enable()` hívás semmit sem csinált - a tényleges újracsatlakozás csak a kliens saját, belső, automatikus retry-időzítőjére várt (max. ~5mp), ami "kellett kétszer kattintani"-ként hatott. **Javítva**: a gombnyomás/kapcsoló-bekapcsolás mostantól `id(mqtt_id).disable()`-t hív a mezőértékek beállítása ÉS az `enable()` ELŐTT - ez garantáltan `DISABLED` állapotba kényszeríti a klienst, így a következő `enable()` MINDIG valódi, azonnali új kapcsolódási kísérletet indít, minden egyes nyomásra.
    - **MQTT teljes átalakítás: "Enabled" kapcsoló + "Connect"/"Disconnect" gombok -> EGYETLEN Enable/Disable vezérlő** - a "Huszonnegyedik kör"-ben tudatosan szétválasztott két fogalom (Enabled = csak reset-kapcsoló; Connect/Disconnect = a tényleges akció) most a felhasználó kifejezett kérésére VISSZA lett vonva egybe: "bal oldalt a státusz alatt enable/disable gomb... jobb oldalt státusz mezőben ki tudjuk írni: connected/disconnected/disabled" - az Enable/Disable most MAGA a connect/disconnect akció (a régi "MQTT Connect" gomb teljes logikája átkerült "MQTT Enabled" saját `turn_on_action`-jébe, benne a fenti `disable()`-előbb javítással is), a külön "MQTT Connect"/"MQTT Disconnect" gombok pedig teljesen megszűntek (redundánssá váltak). Az "Enabled" kikapcsolása ("Disable") a "Huszonnegyedik kör" óta meglévő teljes mezőtörlést IS elhagyta - egy tisztán connect/disconnect szerepű gombtól nem várható el, hogy váratlanul kitöröl minden mentett beállítást; ha valaha kellene egy explicit "reset" funkció, az külön tervezendő.
    - **MQTT csoport elrendezése: Network és System közé ékelve** - `sorting_weight` 100 → 85 (Network=80, System=90 közé) - közvetlen kérés ("az egész elrendezése: network és system közé ékeljük be").
    - **MQTT fejléc-sor teljesen újratervezve** - egy bal oldali oszlop ("Status" felirat, alatta Enable/Disable ÉS "MQTT Settings" gomb egymás mellett) és egy jobb oldali élő státusz-érték (Connected/Disconnected/Disabled/Connecting…/hibaszöveg) - ez a régi, generikus "Status" lista-sor + kapcsoló-pötty mezőt váltja fel egy erre a csoportra szabott (`ensureMqttHeader()`, dashboard.js) elrendezéssel. A beállítások kártya (Broker/Username/Password/Topic Prefix) láthatósága mostantól KIZÁRÓLAG az új "MQTT Settings" gombtól függ (`mqttSettingsOpen`, tisztán kliens-oldali állapot) - nem az Enabled állapottól, ahogy a "Huszonhatodik kör"-ben volt - direkt kérés: "enable/disable gomb mellé kell egy MQTT settings gomb -> erre nyílik csak ki a connection adatai blokk". A kártya alján egy "Save" gomb zárja vissza (`ennek az alján Save vagy OK gomb becsukja a blokkot`) - tisztán UI-akció, nincs saját szerver-hívása, mert minden mező saját magát menti a `change` eseményen (ugyanaz a "commit mezőnként, nem billentyűnként" minta, mint a Reading/Update párnál).
    - **Kapcsolódási hiba olvasható szöveggel a Státusz mezőben, nem csak "Disconnected"** - közvetlen kérés: "reloadra disconnected jó állapot csak hibás csatlakozás után nem a disconnected kell hanem a hiba olvasható magyarázata". ESPHome saját `mqtt:` `on_disconnect:` triggere `MQTTClientDisconnectReason` paramétert kap, DE az ESP32/ESP-IDF backend (a telepített csomag `mqtt_backend_esp32.cpp`-jében megerősítve, benne egy saját `// TODO is there a way to get the disconnect reason?` megjegyzéssel) MINDEN hibatípusnál ugyanazt az egyetlen, generikus `TCP_DISCONNECTED` okot jelenti - a ténylegesen részletesebb ESP-TLS hibakód (amit a felhasználó csatolt logja is mutatott: `esp-tls error: 0x8008`) csak egy log-sorba kerül, sehova máshova nem jut el, forkolás nélkül nem elérhető YAML-ből. Emiatt egy pontosabb, "0x8008"-szintű üzenet NEM volt elérhető ebben a körben forkolás nélkül - de egy MÁSIK, teljesen elérhető jelzés IGEN: a `mqtt_enabled_switch` saját aktuális állapota. `on_disconnect:` mostantól: ha a kapcsoló még mindig BE van kapcsolva (tehát a leválás NEM egy szándékos Disable következménye, aminek saját `turn_off_action`-je már explicit "Disabled"-et publikált), a Státusz "Connection lost - check broker/port/credentials"-re vált; ha a kapcsoló már ki van kapcsolva, ez az ág egyáltalán nem ír felül semmit, a "Disabled" állapot érintetlen marad.
    - **Jelszó-mező: böngésző jelszókezelőjének felajánlása kikapcsolva** - közvetlen kérés: "password mezőre tudjuk tiltani a jelszókezelő felajánlását?". Kiderült: az előző kör `autocomplete="new-password"` értéke éppen az ELLENKEZŐJÉT csinálta annak, amit a felhasználó akart - ez a szabványos jelzés, amivel egy oldal KIFEJEZETTEN kéri Chrome saját "itt egy erős jelszó" felajánló buborékját, nem elnyomja azt. Javítva `autocomplete="off"`-ra, kiegészítve egy nem-jelszó-szerű `name` attribútummal, plusz a fő harmadik féltől származó jelszókezelők saját "hagyd ki ezt a mezőt" attribútumaival (`data-lpignore` - LastPass, `data-1p-ignore` - 1Password, `data-bwignore` - Bitwarden, `data-form-type="other"` - általános heurisztikus kezelők) - egyetlen attribútum sem garantáltan 100%-os minden böngészőben/kezelőben natív `type="password"` maszkolás megtartása mellett, de ez a kombináció a lehető legtöbbet nyújtja anélkül, hogy le kellene mondani a natív maszkolásról.
    - **Szem-ikon: explicit állapot + megfordított ikon-logika, a felhasználó saját megfigyelése alapján** - "A szem ikon nem működik... Szerintem fordítva van a csukott/nyitott szem... néha tudok beírni, de nem látom a logikát". Kódvizsgálattal (kézi, kétszeri végigkövetéssel) az eredeti logika önmagában belsőleg konzisztensnek bizonyult egy elterjedt konvenció szerint (nyitott szem = maszkolva, "kattints a megjelenítéshez"; áthúzott szem = felfedve, "kattints az elrejtéshez") - DE mivel ez a felhasználó saját, élő megfigyelését ("fordítva van") nem tudja megmagyarázni pusztán kód-elemzéssel (a valós renderelést csak ő látja), a döntés az ő közvetlen visszajelzését követte: megfordítva a másik, ugyanilyen elterjedt konvencióra (áthúzott szem = maszkolva, nyitott szem = felfedve - "amit épp látsz, az nyitott szemmel van jelezve"). Emellett az állapot mostantól egy explicit `let revealed` logikai változó, nem az `input.type` értékéből minden kattintáskor újra levezetve - ez kiküszöböli bármilyen elméleti eshetőséget, hogy az ikon és a tényleges maszkolt/felfedett állapot szinkronból kicsússzon, ami a "néha nem látom a logikát" panasz kiváltó oka lehetett.
  - **Huszonkilencedik kör, felhasználói visszajelzésre ugyanaznap, 2026-08-16 (ua. nap) - Enable/Disable és Connect/Disconnect szétválasztva vissza, egy valódi jelszó-mező hiba javítva**:
    - **MQTT vezérlés: Enabled + egyetlen Connect/Disconnect gomb, két külön switch-csel** - a "Huszonnyolcadik kör"-ben (ugyanaz nap, korábban) az Enabled kapcsoló ÉS a Connect/Disconnect akció egyetlen switch-csé lett összevonva - ezt a felhasználó ugyanaznap belüli, még pontosabb visszajelzése módosította: "legyen egy connect/disconnect gomb (állapottól függően). Az enabled csak annyit kell csináljon hogy bekapcsolja a komponenseket (settings, connect/disconnect, sttings dialog) - ha disabledet nyomunk, akkor az disconnectál és kikapcsolja a komponenseket" - tehát VISSZA két külön fogalomra, de nem ugyanúgy, mint a "Huszonnegyedik kör" eredeti verziója: "MQTT Enabled" (`mqtt_enabled_switch`) mostantól egy TISZTA láthatósági kapu - `turn_on_action`-je nincs is (a C++ oldalon nincs mit tennie, csak a saját kapcsoló-állapotát váltja, amit a JS olvas ki), `turn_off_action`-je pedig `switch.turn_off: mqtt_connect_switch`-öt hív (ami a TÉNYLEGES leválasztást elvégzi, lásd lent), majd felülírja a Státuszt kifejezetten "Disabled"-re. Egy ÚJ, önálló switch (`mqtt_connect_switch`, "MQTT Connect" - dashboard.js-ben egyetlen, állapottól függő feliratú gombként jelenik meg: "Connect"/"Disconnect", ugyanaz a gomb-nem-csúszka mintázat, mint az Enabled kapcsolóé) végzi a valódi kapcsolódást/bontást - ennek `turn_on_action`-je pontosan az, ami a "Huszonnyolcadik kör"-ben az Enabled saját `turn_on_action`-je volt (mezők alkalmazása, `disable()` a duplakattintás-hiba miatt `enable()` előtt, `mqtt_want_connected = true`), `turn_off_action`-je pedig a régi Disconnect logika (`disable()`, `mqtt_want_connected = false`, Státusz "Disconnected"-re). A `mqtt: on_disconnect:` trigger mostantól `mqtt_connect_switch` (nem `mqtt_enabled_switch`) állapotát nézi a "Connection lost..." vs. csendben-hagyás döntéshez, mivel EZ a switch követi, hogy "akarunk-e éppen kapcsolódni lenni", nem az Enabled. Az újracsatlakozás mindkét switch-et bekapcsolja sorban (Enabled - hogy a komponensek látszódjanak -, majd Connect - hogy ténylegesen próbálkozzon) - **hogy pontosan MIKOR fut le ez, azt a "Harmincadik kör" módosította, ld. lent, egy valódi crash után**.
    - **MQTT fejléc-sor: harmadik gomb hozzáadva, feltételesen látható** - a `.dc-mqtt-header-buttons` sor mostantól három gombot tartalmaz: Enable/Disable (mindig látható), Connect/Disconnect és "MQTT Settings" (mindkettő CSAK akkor látható, ha Enabled be van kapcsolva - `updateMqttEnableButton()`, dashboard.js, állítja a `hidden` attribútumukat) - pontosan a kért "Enabled csak bekapcsolja a komponenseket" viselkedés. Enabled kikapcsolásakor a beállítások-panel nyitott állapota (`mqttSettingsOpen`) is automatikusan visszaáll zártra, hogy ne maradjon "szellem" nyitva egy olyan panel, aminek a megnyitó gombja épp eltűnt.
    - **Valódi hiba javítva: a jelszó-mező szem-ikonja gyakorlatilag "kiírta" (elmentette) a jelszót idő előtt** - közvetlen visszajelzés: "a password komponens kezelése még teljesen szar! Ha beírom a jelszót rejtve majd megnyomom a szemet akkor írja ki a jelszót. Standard módon kell mukodjon". A tényleges gyökérok: a szem-gomb megnyomása (mint bármely más fókuszálható elem kattintása) alapértelmezetten elveszi a fókuszt a jelszó-mezőtől - ez kiváltja a mező saját `change` eseményét (a saját `set_action`-jét), ami AZONNAL elküldi a device-nak, amit épp beírtunk, még mielőtt a felhasználó ezt kérte volna - a device saját visszhangja (egy fix "" vagy a pöttyös placeholder, sosem a valós érték, ld. e mező saját megjegyzését) aztán felülírta a mező látható tartalmát, ami kívülről úgy nézett ki, mintha a szem gomb "kiírná" (törölné/felülírná) a frissen beírt jelszót. **Javítva**: a szem-gomb saját `mousedown` eseményén (nem `click`) egy `preventDefault()` hívás megakadályozza, hogy a böngésző egyáltalán elvegye a fókuszt a mezőtől a gomb megnyomásakor - így a láthatóság-váltás sosem vált ki `blur`/`change` eseményt, tisztán helyi, mellékhatás nélküli marad, pontosan a kért "standard módon" viselkedés (a legtöbb valódi jelszó-mező implementáció ugyanezt a technikát használja).
  - **Harmincadik kör, felhasználói jelentésre egy valódi eszköz-crash logról, 2026-08-16 (ua. nap) - MQTT reconnect-on-boot áthelyezve `esphome: on_boot:`-ról `wifi: on_connect:`-re, egy valódi boot-loop crash után**:
    - **Valódi crash, felhasználó jelentése**: "mukodott napokig, majd most kihuz/bedug és a log azt mondja hogy device crashed" - egy csatolt eszköz-log kemény bizonyítékot adott: hidegindítás (fizikai ki/bedugás) után az eszköz 10 egymást követő boot-ciklusban összeomlott (`assert failed: xQueueGenericSend queue.c:936 (pxQueue)`, egy alacsony szintű FreeRTOS-hiba, teljes backtrace-szel), amíg ESPHome saját `safe_mode` boot-loop védelme (10 sikertelen próbálkozás után) leállította a ciklust és Safe Mode-ba kapcsolt - onnantól az eszköz stabilan futott ("Boot seems successful"). Minden egyes összeomlás ugyanabban a pontban történt: közvetlenül a `[D][template.switch:044]: Restored state ON` és `[W][component:342]: mqtt set Warning flag: unspecified` sorok után, MÉG a tényleges Wi-Fi-kapcsolódás (`Connecting to 'IOT-AKN'`) előtt minden egyes ciklusban.
    - **Gyökérok**: az `esphome: on_boot:` (priority 200, "AFTER_WIFI") CSAK azt garantálja, hogy minden komponens saját `setup()`-ja lefutott - NEM azt, hogy a Wi-Fi ténylegesen kapcsolódott és a hálózati verem (LWIP, DNS) valóban használatra kész (ez a csatolt log saját idővonala alapján is megerősítve: a `Connecting to...` sor minden crash-ciklusban jóval a saját `on_boot:`-triggerelt MQTT-kapcsolódási kísérlet UTÁN jelent volna meg, ha egyáltalán eljutott volna odáig). A "Huszonhetedik kör"-től kezdve `mqtt_want_connected` esetén az `on_boot:` ténylegesen elindította az MQTT-kliens kapcsolódását (`id(mqtt_id).enable()`, a "MQTT Connect" switch saját `turn_on_action`-jén keresztül) - ez a hálózati verem készenléte előtt próbált egy ESP-IDF-szintű hálózati műveletet indítani, ami a legvalószínűbb kiváltója egy ilyen alacsony szintű FreeRTOS queue-crash-nek. **Javítva**: a reconnect-ha-akarjuk logika áthelyezve `esphome: on_boot:`-ról a `wifi: on_connect:` triggerre - ez KIZÁRÓLAG akkor fut le, amikor a Wi-Fi ténylegesen kapcsolódott (valódi IP-cím van), tehát a hálózati verem garantáltan kész. Ez nem csak workaround, valódi funkcionális javulás is: a `wifi: on_connect:` egy Wi-Fi-kapcsolat-vesztés utáni ÚJRA-kapcsolódáskor IS lefut (nem csak bootkor), tehát egy valódi Wi-Fi-kimaradás után az MQTT-kapcsolat is automatikusan helyreáll, amit a régi, csak-bootkor-triggerelt verzió sosem tett meg. A `packages/mqtt.yaml` saját `wifi: on_connect:` kulcsa `water-collector.yaml` saját, már meglévő `wifi: on_connect:` blokkjával (BLE-improv letiltás) egyetlen, összefésült `then:` akciólistává olvad össze ESPHome `packages:` mechanizmusán keresztül - ellenőrizve `esphome config` kimenetében (mindkét forrás akciói egy közös listában jelennek meg) és a generált `main.cpp`-ben (`wifi_id->get_connect_trigger()`-re regisztrált egyetlen Automation, `esphome: on_boot:` saját generált kódjában pedig többé semmi MQTT-vonatkozású hívás nincs).
  - **Harmincegyedik kör, felhasználói visszajelzésre egy negyedik valódi eszköz-logra, 2026-08-19 - Connect gomb tiltása hiányos mezőknél, Enabled vissza kapcsolóra, egy valódi fókusz-vesztési hiba gyökérokának megtalálása, hibaüzenet-logika és Connect/Save összevonása**:
    - **"connect gomb ne is legyen aktív amíg nincs minden szükséges kitöltve"** - a Connect gomb mostantól `disabled`, amíg az "MQTT Broker" mező üres (`updateMqttConnectAvailability()`, dashboard.js) - élőben, gépelés közben is frissül (`input` esemény a Broker mezőn), nem csak `blur`/SSE-visszhang után. Kizárólag Broker a valódi "must have": a Topic Prefix mezőnek mindig van garantált alapértéke (`esphome: on_boot:`, "Huszonötödik kör"), Username/Password pedig legitim módon opcionális - nem minden bróker kér hitelesítést (ld. lent, 10. pont, a csatolt log saját, hitelesítés nélkül sikeresen kapcsolódó eszközét).
    - **"Enable disable mégis legyen kapcsoló"** - a "Huszonhatodik kör"-ben gombra cserélt Enable/Disable most VISSZA lett alakítva valódi `.dc-toggle` csúszkára (ugyanaz a widget, amit a projekt más egyszerű switch-mezői is használnak), saját "Enabled" szöveges felirattal mellette (egy csupasz csúszkának nincs saját szövege) - a kapcsoló ÉS a felirat is kattintható, a felirat-kattintás ugyanúgy vált, mint egy natív checkbox-label pár.
    - **A "csak az első próbálkozásra hibás" fókusz-vesztés VALÓDI, megtalált gyökéroka** - `reorderServiceFields()` (dashboard.js) MINDEN mezőt feltétel nélkül újra `appendChild`-elt minden egyes híváskor, még akkor is, ha a sorrend valójában nem változott - egy már a helyén lévő elem `appendChild`-elése is "kivesz majd visszatesz"-nek számít minden böngészőben, ami elveszi a fókuszt bármelyik, épp fókuszban lévő mezőtől. Mivel EZ a függvény minden mező-frissítés (`upsertServiceText()`, `upsertServiceSwitch()`) végén lefut, egy username/password mezőbe kattintás után szinte azonnal érkező bármilyen SSE-frissítés (akár egy teljesen más MQTT-mezőé, akár csak a "MQTT Status" ketyegése) újrarendezte (=elmozgatta) az ÉPP FÓKUSZBAN lévő mezőt is, azonnal elvéve a fókuszt - egy valódi versenyhelyzet, ami pontosan magyarázza a "csak az elsőre" tünetet (a második próbálkozás egyszerűen szerencsésebb időzítést kapott, nem egy tényleges állapotváltozás miatt lett jó). **Javítva**: `reorderServiceFields()` most előbb összeveti a jelenlegi és a kívánt sorrendet, és csak akkor mozgat bármit, ha ténylegesen eltérnek - mivel a `sorting_weight` értékek statikusak (sosem változnak futásidőben), a sorrend a gyakorlatban szinte mindig már helyes, tehát ez a változtatás a mozgatást egy valódi no-op-pá teszi a legtöbb híváskor, megszüntetve magát a versenyhelyzetet (nem csak az MQTT csoportnál - bármelyik más csoport ugyanezt a függvényt használja).
    - **Hibaüzenet lerövidítve, ahogy kérve** - "Meg se adtam adatokat majd azt mondja 'Connection lost - check broker/port/credentials' - elég lenne csak connection error". `mqtt: on_disconnect:` (`packages/mqtt.yaml`) mostantól "Connection error"-t publikál alapesetben - EGY kivétellel: a csatolt eszköz-log konkrét bizonyítékot adott arra (`Couldn't resolve IP address for ''` közvetlenül `Disconnected: DNS Resolve Error` előtt), hogy egy üres/feloldhatatlan Broker-cím esetén a `MQTTClientDisconnectReason::DNS_RESOLVE_ERROR` a triggerbe kapott `x` paraméteren keresztül TÉNYLEGESEN elérhető, forkolás nélkül - ez korrigálja a "Huszonnyolcadik kör" saját, akkor még túl általánosnak bizonyult állítását ("az ESP32 backend soha semmilyen konkrétabb okot nem ad vissza") - ez csak az ESP32/IDF backend saját, TCP-szintű `MQTT_EVENT_DISCONNECTED` kezelőjére igaz (a telepített csomag `mqtt_backend_esp32.cpp`-je szerint, saját `// TODO`-jával), a DNS-feloldást viszont egy ATTÓL FÜGGETLEN, backend-független `check_dnslookup_()` (`mqtt_client.cpp`) kapja el, és jelenti helyesen. Ebben az egy esetben a Státusz most "Connection error - can't resolve broker address"-t ír.
    - **"connection error esetén még a disconnect az aktív" - gyökérok és javítás** - `mqtt_connect_switch` `optimistic: true`, tehát saját állapota SOSEM áll vissza magától egy sikertelen/megszakadt kapcsolat után - kizárólag azt tükrözi, hogy "akarunk-e éppen kapcsolódni lenni", nem azt, hogy "ténylegesen kapcsolódva vagyunk-e éppen". A Connect/Disconnect gomb kiemelt ("aktív") kinézete mostantól NEM a switch nyers értékéből, hanem a "MQTT Status" TÉNYLEGES szövegéből származik (`updateMqttStatusText()` állítja be a `.dc-btn-active` osztályt, kizárólag "Connected" szövegnél) - egy valódi hiba után a felirat helyesen "Disconnect" marad (a nyomása még mindig leállítja az újrapróbálkozást), de a kiemelés eltűnik, mivel ténylegesen nincs élő kapcsolat.
    - **"amíg csatlakozni próbál addig nem szabadna connect gombnak látszódnia... disconnect... azonnal meg kell szakítania"** - a gomb felirata mostantól AZONNAL, optimista módon "Disconnect"-re vált kattintáskor (a switch saját SSE-visszhangjára várás nélkül, ugyanaz a minta, mint az Enabled kapcsolónál) - így egy kapcsolódási kísérlet közben sosem látszik "Connect"-nek. Az azonnali megszakítás már korábban is garantált volt (`mqtt_connect_switch` saját `turn_off_action`-je azonnal `disable()`-t hív, a "Huszonnyolcadik kör" duplakattintás-javítása óta).
    - **"save gomb nem is kell, hanem connect gombra menne a mentés" - a Save gomb megszűnt** - `ensureMqttSaveButton()` és hívása törölve. A Connect gomb (KI -> BE váltáskor) most explicit módon, sorban elküldi a Broker/Username/Password/Topic Prefix mezők AKTUÁLIS (a DOM-ban lévő, nem feltétlenül még a device felé elküldött) értékét, MIELŐTT magát a kapcsolódást kéri, és bezárja a beállítások-panelt - pontosan a Save gomb régi egyetlen feladatát váltva ki egyetlen gombnyomással. A Password mezőnél kihagyja a küldést, ha az érték még mindig a pöttyös placeholder (nincs új begépelt jelszó - a placeholder tényleges elküldése felülírná a mentett jelszót nyolc szó szerinti ponttal).
    - **"beírtam egy szándékosan rossz jelszót -> save -> connect -> 'connected' - garantáltan súlyos hiba" - valószínű gyökérok megtalálva és javítva** - egy mező saját `change`/mentés-kérése csak `blur`-ra (fókuszvesztésre) váltódik ki; ha a felhasználó a jelszó begépelése UTÁN közvetlenül a Connect gombra kattint anélkül, hogy a mező előtte máshogy elvesztette volna a fókuszt, két, egymástól független HTTP-kérés versenyez (a mező saját mentése vs. a Connect-kapcsoló kérése) - ha a kapcsolódási kérés ér oda előbb, a device a RÉGI, korábban mentett jelszóval próbál kapcsolódni, nem az újonnan (de még el nem küldött) beírttal. A közvetlenül fenti "Connect menti a mezőket" változtatás ezt strukturálisan megszünteti: minden mező mentése garantáltan megelőzi (be van várva, `await`) magát a kapcsolódási kérést. **Fontos, őszinte megjegyzés**: egy MÁSIK, ezzel párhuzamosan is fennálló magyarázat sem zárható ki teljesen - a csatolt log végig NEM mutat semmilyen hitelesítési hibára utaló sort, és a későbbi, valódi üzenetforgalom (36 eldobott bejövő MQTT-esemény) egy olyan brókerre utal, ami egyáltalán nem kényszerít ki hitelesítést - ha ez a helyzet, a device onnantól bármilyen (akár rossz) jelszóval is "Connected"-et fog mutatni, mert a bróker maga sosem utasítja el. Ez utóbbi eset NEM dashboard-hiba, hanem a teszt-bróker saját konfigurációja - érdemes ellenőrizni egy éles, hitelesítést tényleg megkövetelő bróker felé, hogy a fenti versenyhelyzet-javítás önmagában elegendő volt-e.
    - **Jelszó-mező: a kért pontos viselkedés újra ellenőrizve** - a "Huszonkilencedik kör" (mousedown `preventDefault()`) és a korábbi körök (explicit `revealed` állapot, megfordított szem-ikon konvenció, `MQTT_PASSWORD_PLACEHOLDER` visszaállítás) által implementált viselkedés átvizsgálva a most kapott pontos specifikációval szemben - egyezik: alapból maszkolva (pöttyök minden begépelt karakterre), szem-kattintásra felfedi az ÉPP a mezőben lévő tartalmat (begépelt VAGY a mentett-jelző placeholder), újra kattintásra visszamaszkolja, a szem-gomb saját maga sosem törli/módosítja a mező tartalmát (`mousedown preventDefault()` óta), betöltéskor a placeholder (ha van mentett jelszó) csukott szemmel jelenik meg. **Egy pont, ami nem hiba, hanem szándékos, korábban már elfogadott architekturális korlát, és fontos újra tisztázni**: a szem-gomb sosem tudja megmutatni a TÉNYLEGESEN mentett jelszót, ha a mezőben csak a placeholder áll (nem újonnan begépelt szöveg) - ez nem hiányzó funkció, hanem a "Huszonnegyedik kör" óta szándékos biztonsági döntés következménye: a valódi jelszó egy `internal: true` entitásban van, ami a `web_server` REST/SSE felületéről forráskód-szinten teljesen ki van zárva - a böngésző SOSEM kaphatja meg a valódi mentett értéket, tehát a szem-gomb legfeljebb azt tudja megmutatni, amit épp MOST írtunk be, sosem azt, ami korábban el lett mentve és már nincs begépelve.
    - **Elhalasztva, a felhasználó saját kérésére**: a "táblázatos, mint a Network blokk, csak szerkeszthető" elrendezés-ötlet (3. pont) és általában "a felület logikája és a gombok" átgondolása - szó szerinti idézet: "Egyelőre javítsuk a hibákat de a felület logikáját és a gombokat még át kell szabni később".
    - **Validáció**: `esphome config` + `esphome compile` (forráskód-generálásig) átment, a generált `main.cpp`-ben kézzel ellenőrizve az `on_disconnect:` lambda és a `mqtt_connect_switch` bekötése; `node --check web/dashboard.js`; CSS-zárójel-egyensúly (`web/dashboard.css`); fejnélküli Chromium-screenshot az új fejléc-sorról (kapcsoló + felirat + Connect/Disconnect + MQTT Settings, kiemelt "Connected" állapot ÉS tiltott, üres Broker melletti Connect állapot egyaránt renderelve, vizuálisan megerősítve).

Amikor a rendszer a nyomásmérőn túl más Modbus eszköztípusokat is támogat (pl. áramlásmérő), egy **device class** entitást kell bevezetni slotonként (a `try_register`/scan folyamat már ma is eszköztípus-függetlenül van elnevezve - ld. `modbus_add_claimed` saját megjegyzése -, ez a következő lépés lenne rá építve). A felhasználó explicit kérése: a driver/eszköztípus-felismerés határozza meg a class-t, és **ebből** ajánljon fel a felhasználónak név-sablont a Hozzáadás előtt (pl. "Pressure"/"Flow Meter", a ténylegesen elérhető class-ok közül, egymástól megkülönböztethetően), a Modbus címmel együtt/mellett - nem egy általános, típus-független "Sensor name" placeholder, ahogy ma. Ez közvetlenül kapcsolódik a "Service oldal szélesség-reszponzivitása" (fenti "Nyolcadik kör") alatt már megjegyzett, ugyanerre az alkalomra elhalasztott tábla-oszlop kérdéshez is. A "Tizenhatodik kör" közös "Devices" táblázatának típusválasztója (Modbus / Pulse Meter) már most is ebbe az irányba mutat, de ez egyelőre kézzel, nem device-class alapon van elágaztatva.

**MEGVALÓSÍTVA, "Harminckettedik kör", 2026-08-19** - a fenti terv, a T3-1-2-H ultrahangos áramlásmérő megrendelése kapcsán ("Készítsük fel a rendszert a T3-1-2-H érkezésére... 'a' legyen mindenképpen" - az architektúra-alapú, generalizált megoldás, nem a fizikai eszköz beérkezéséig várás), explicit kéréssel: **"kellően generalizált architektúra és sw felépítés kell"** - ha jól van megcsinálva, a tényleges hardver beérkezése után csak a regiszter-mapping finomítása maradjon hátra, ne architekturális munka.

- **Új entitás slotonként: "Device Type"** (`select:`, `packages/pressure_sensor.yaml`) - "Pressure" vagy "Flow", alapértelmezetten (és minden korábban regisztrált, ezen módosítás előtti slotnál automatikusan) "Pressure" - egyetlen már meglévő telepítés viselkedése sem változik migráció nélkül. Eldönti, melyik két, MINDEN slotra egyformán deklarált sensor entitás (Pressure/Instant Flow - "existence model", ugyanaz a minta, mint a projekt minden más, mindig létező entitásánál) pollozza ténylegesen a buszt - a másik némán no-op marad, nulla extra költséggel.
- **`include/rs485_modbus.h` - eszköztípusonként egy izolált, hozzáadható reader függvény**: `read_pressure_bar()` (változatlan) mellett új `read_flow_instant()` - a hivatalos T3-1 V51 Modbus-protokoll dokumentum (ld. `docs/hardver/t3-1-2-h-aramlasmero-jegyzet.md`) "0001-0002" regiszteréből, ugyanaz a nagy-endián ABCD-szavas IEEE754 Float32 dekódolás, mint a nyomásmérőnél. **Szándékosan csak a pillanatnyi átfolyás van implementálva, a kumulált mennyiség (totalizer) NEM** - az utóbbi egy távolabbi, külön regiszterből olvasott skálázó-kitevőt (multiple factor) igényelne, amit hardveres megerősítés nélkül nem érdemes bevezetni; ez egy tudatos, dokumentált scope-vágás, nem hiányosság. Egy megosztott `publish_poll_result()` segédfüggvény (Online/Collision könyvelés) lett kiemelve mindkét reader közös utómunkájából - ez az egyetlen rész, ami minden eszköztípus között ténylegesen közös maradt, minden más eszköztípus-specifikus.
- **`water-collector.yaml`**: új "Pressure Sensors Add Device Type" `select:` (a Hozzáadás-folyamat saját, ideiglenes mező-készletéhez, ugyanaz a minta, mint Add Name/Add Target Address) - egy busz-scan önmagában nem tudja megkülönböztetni, milyen TÍPUSÚ eszköz válaszol egy címen (mindkettő csak "van itt valami"-t mond), ezért a hozzáadás pillanatában kell megválasztani. A `try_register` script mind a 8 slotnál kapott egy új `device_type` paramétert.
- **`web/dashboard.js`/`dashboard.css`**: a "Devices" táblázat regisztrált sorának saját ikonja (ÚJ "flow" ikon - három vízszintes hullám-vonal, megkülönböztethető a nyomásmérő "gauge" ikonjától és a pulzusmérők "water" csepp-ikonjától) és a Dashboard-kártya értéke mostantól a slot tényleges Device Type-jától függ, nem mindig "Pressure"-t feltételezve. A scan-felfedezett "New device" sor kapott egy típusválasztó `<select>`-et, élőben frissülő ikonnal, a Megerősítés (pipa) gomb pedig ezt is elküldi hozzáadás előtt. **Valódi, elszállítás előtt megtalált névütközés-hiba, elkerülve**: az új entitás NEM "Flow Rate" néven lett elnevezve, hanem "Instant Flow" - a `HELP_TEXT` szótár (a `displayName()`-levágott, csoport-független kulcsokkal dolgozó súgó-szöveg tár) már foglalt egy "Flow Rate" bejegyzést a pulzusmérők saját, pulzus-időzítés-alapú mezőjéhez, ami teljesen más és félrevezető szöveg lett volna egy Modbus-regiszteres áramlásmérőn - ugyanaz a hiba-kategória, ami korábban az MQTT csoportot is valóban érintette (ld. "Huszonhetedik kör"), itt viszont már tervezés közben kiszűrve, nem utólag javítva.
- **Belső elnevezés SZÁNDÉKOSAN nem generalizálva**: a slotok saját, fordítási idejű azonosítói (`slot1`.."slot8", "Pressure Sensor 1".."8") maradtak - ezek sosem jelennek meg a felhasználó felé (csak a futásidejű Display Name), a `.dc-pressure-*` CSS-osztályok/JS-függvénynevek átnevezése pedig pusztán kozmetikus lett volna, valós funkcionális előny nélkül, felesleges diff-mérettel és regressziós kockázattal - ld. `packages/pressure_sensor.yaml` saját GENERALIZATION megjegyzését a teljes indoklásért.
- **Validáció**: `esphome config` + `esphome compile` (forráskód-generálásig) átment mindkét körben (a backend YAML/C++ munka után és az "Instant Flow" átnevezés után is), a generált `main.cpp`-ben kézzel ellenőrizve a `select::SelectSetAction<std::string, int, std::string>` (a `try_register` 3 paraméteres hívása) és a `rs485_modbus::read_flow_instant()`/`publish_poll_result()` tényleges (nem csak kommentben szereplő) hívásait; `node --check web/dashboard.js`; CSS-zárójel-egyensúly; fejnélküli Chromium-screenshot a regisztrált sor "flow" ikonjáról és az új sor típusválasztójáról.
- **Még nyitott, hardveres megerősítésre váró pontok** (ld. `docs/hardver/t3-1-2-h-aramlasmero-jegyzet.md` a teljes listáért): a "0001" regiszter tényleges PDU-címe (0 vagy 1 - off-by-one bizonytalanság a fordított adatlap-stílus miatt), a tápellátás (DC a Piros/Feketére + RS485 a Sárga/Zöldre egyidejűleg), és maga a totalizer-olvasás bevezetése a most már bizonyítottan működő minta (egy izolált reader függvény + egy új mindig-létező sensor entitás) egyszerű megismétlésével.

### GPIO-bővítés – biztonsági elemzés elvégezve, 2026-08-15 ("Huszonharmadik kör"), implementáció még nem indult

A jelenlegi hardver (`docs/hardver/esp32-s3-rs485-can-board.md`) csak a dedikált SH1.0 csatlakozón kivezetett GPIO1/GPIO2-t huzalozza impulzusszámlálásra. A felhasználó explicit kérése (2026-08-15): egy felületi beviteli mező, ahol a szabad GPIO-k közül tetszőlegesen fel lehet venni továbbiakat impulzusszámlálónak, firmware-módosítás nélkül - de saját maga is jelezte a valós kockázatot (egy "rossz" GPIO engedélyezése boot-problémát vagy nemkívánt viselkedést okozhat), és kifejezetten kérte, hogy vitassuk meg, mielőtt belekezdenék.

**A szó szerint kért forma (szabad szöveges/tetszőleges GPIO-szám beviteli mező) NEM biztonságos, konkrét, ezen a boardon valós okokból:**

- A 2,0 mm-es pin header ténylegesen kivezetett "szabad" pinjei (a doksi saját táblázata szerint): **IO3-IO14, IO19/20 (`D_P`/`D_N` felirattal), IO43/44 (`TXD`/`RXD` felirattal)**.
- **IO19/20 minden jel szerint az ESP32-S3 natív USB D+/D- vonalai** (a `D_P`/`D_N` felirat és a terméklap saját "USB Type-C (táp + debug + flashelés)" pontja együtt erre utal) - impulzusszámlálóként engedélyezve ez valószínűleg **megszakítaná a USB-n keresztüli debug/flashelés útvonalát**, ami a terméklap szerint az elsődleges (talán egyetlen) flashelési mód.
- **IO43/44 az UART0, a soros konzol/programozó interfész** (a `TXD`/`RXD` felirat egyértelműen ezt jelzi) - engedélyezve elveszne a soros konzol és (valószínűleg) a soros flashelés lehetősége is.
- **IO3 hivatalos "strapping pin"** (ESP32-S3 JTAG-forrás kiválasztás, boot pillanatában mintavételezve) - ennek kockázata alacsonyabb (nem boot-loop, csak a debug-interfész forrás-kiválasztása változhat meg egy jövőbeli reboot-nál, ha a rákötött érzékelő épp úgy húzza a vonalat), de a felhasználó saját, kifejezetten óvatos megfogalmazása ("nem szabad engedélyezni... esetleges boot esetén valami nem kívánt állapotba viszi a kártyát") miatt ezt is inkább kizárnám az alapértelmezett listából, külön, egyértelmű figyelmeztetéssel hagyva meg opcionálisnak.
- **Jó hír**: a board maga (a fizikai header) már eleve NEM vezeti ki a ténylegesen legveszélyesebb pineket (GPIO0 - letöltési mód strapping; GPIO45/46 - VDD_SPI/log-szint strapping; GPIO26-37 - a beépített octal PSRAM/flash SPI busza az `esp32-s3-devkitc1-n16r8` modulon, ezek szoftveres engedélyezése valószínűleg azonnal összeomlasztaná az eszközt, hiszen maga a futó firmware is ezeken keresztül éri el a memóriáját) - ezekkel emiatt konkrétan nem is kell foglalkozni, mert fizikailag nem elérhetők a csatlakozón.
- **Ráadásul architekturális korlát is van**: az ESPHome beépített `pulse_meter` platformja fordításidőben köti a GPIO-t egy adott entitáshoz - nincs "futás közben bármelyik GPIO-t bárhogy átkötni" API. Egy valódi, tetszőleges-GPIO szabad szöveges mező ezért amúgy sem volna megvalósítható pusztán YAML-lal; egy teljesen kézzel írt, ESPHome `pulse_meter`-t megkerülő GPIO-driver kellene hozzá - jelentősen nagyobb és kockázatosabb munka, mint bármi eddig ebben a projektben.

**Javasolt, biztonságosabb alternatíva** (még nincs implementálva, megerősítésre vár): a jelenlegi 2 fix slot mintájának kiterjesztése **N előre ellenőrzött, biztonságos GPIO-ra** (a fenti listából **IO3-IO14** - IO19/20/43/44 kizárva, IO3 külön jelöléssel/óvatossággal) - mindegyik továbbra is FORDÍTÁSI IDŐBEN bekötött `pulse_meter` slot (ugyanaz a `water_meter.yaml` minta, csak N példányban 2 helyett), de a már meglévő Add/Delete/Regisztrált-e commissioning UI-n keresztül rejtett/megjeleníthető - a felhasználó szemszögéből ez pontosan úgy néz ki, mint "engedélyezem a felületről, nincs firmware-módosítás", miközben a firmware maga SOSEM ajánl fel/enged meg egy nem-vetett-biztonságos pint, mert az fizikailag nincs is a választható listában. Nyitott kérdés a felhasználó felé: hány/mely biztonságos pint (mind a 12-t IO3-IO14 közül, vagy csak egy részüket, a compile méret/RAM/entitásszám költsége miatt) provizionáljunk elő.

### Log letöltés (2026-08-15: implementálva, "Huszonharmadik kör")

A felhasználó kérése: a Log oldal eddig csak azt mutatta, ami a böngésző kapcsolódásának ideje alatt, élőben befut az SSE-n - a felhasználó szerint az eszköz belsejében valószínűleg ennél több log is elérhető, amit szeretne tudni letölteni, kifejezett aggállyal a flash-kopás iránt (ld. a "Huszonegyedik kör"-ben feljegyzett, akkor még nyitott kérdések - mind megválaszolva, lásd alább).

- **Új helyi komponens: `components/log_ring_buffer/`** - ugyanaz a minta, mint a projekt saját `web_server_idf` fork-ja (`__init__.py` + `.h`/`.cpp`, `external_components:` blokkal bekötve `water-collector.yaml`-ban). Egy RAM-ban tárolt, fix méretű (alapértelmezetten 16 kB) körkörös szöveg-puffer, ami minden bejövő log-sort eltárol - **NEM flash-ben**, a felhasználó explicit flash-kopási aggálya miatt: ez a puffer minden reboot-tal elvész, pontosan ugyanazt a "csak az aktuális futás óta" horizontot fedi le, mint a Log oldal élő nézete, csak visszamenőlegesen is elérhetően, nem csak amíg egy böngészőfül nyitva van rá.
- **Letöltés**: a puffer egy `GET /log.txt` HTTP-végponton érhető el, `Content-Disposition: attachment` fejléccel (böngésző natívan letölti, nem csak megjeleníti) - ugyanazon a webszerveren, amin a dashboard is fut (`web_server_base::global_web_server_base->add_handler()`), nincs külön port/entitás. A Log oldal eszköztárában egy sima `<a href="/log.txt">Download Log</a>` link (`web/dashboard.js`), semmilyen JS-logika nem kell hozzá.
- **Log-sorok elfogása**: `logger::global_logger->add_log_callback()` (a logger komponens saját, dokumentált mintája erre) - **fontos technikai részlet**: ez egy fix méretű `StaticVector`-ba kerül regisztrálásra (`ESPHOME_LOG_MAX_LISTENERS`), aminek mérete kizárólag abból számolódik, hányszor hívódik meg a `logger`-modul saját `request_log_listener()` Python-függvénye FORDÍTÁSI IDŐBEN, minden ezt igénylő komponensnél (pl. az `api:` is ezt hívja, hogy HA felé tudja továbbítani a logokat) - ezért ez a komponens is egy valódi ESPHome Python-komponens (nem csak egy `includes:`-szel behúzott header + `lambda:`), ami a saját `to_code()`-jában meghívja ugyanezt - egy pusztán C++ `lambda:`-ból hívott `add_log_callback()` némán túlcsordítaná ezt a fix vektort (defined behavior nélkül), nem fordítási hibát adna.
- **Trimelés**: a puffer a legrégebbi TELJES sorokat dobja el (`\n`-nél vágva), amint a méret túllépi a beállított korlátot - sosem nő korlátlanul. **Felhasználói kérdés, edge case, 2026-08-15: "ha mondjuk fut a rendszer 10 éve akkor a log mérete nem lesz túl nagy?"** - nem, pontosan ez ellen véd a fenti trimelés: a puffer mérete a `max_size`-nál (alapértelmezetten 16 kB) SOSEM nagyobb, függetlenül a futásidőtől - 10 nap vagy 10 év után is ugyanakkora, csak a benne lévő TARTALOM cserélődik folyamatosan (a legrégebbi sorok esnek ki, ahogy újak jönnek). Nem napló-archívum, hanem egy fix méretű ablak a legutóbbi eseményekre.
- **Validáció ebben a sandboxban**: `esphome config` átment; `esphome compile` a C++ forrás generálásáig (`Generating C++ source...`) sikeresen eljutott (ez már lefuttatja a Python `to_code()`-ot is, tehát ez VALÓDI validáció a komponens Python-oldalára), utána a tényleges xtensa-gcc fordítás a ESP-IDF toolchain letöltésénél elakadt - ez a sandbox hálózati szabályzatának korlátja (a proxy 403-at ad a platformio letöltésekre), nem kódhiba. A tényleges C++ fordítás emiatt **csak a következő valódi `esphome run`/flashelésnél derül ki** - ha van fordítási hiba, az onnan a normál módon (hibaüzenettel) javítható, ugyanúgy, ahogy a projekt eddigi minden más, hardveren tesztelt C++ kódja (pl. `include/rs485_modbus.h`) készült.

### MQTT-integráció (2026-08-15: implementálva, "Huszonharmadik kör" — **2026-08-19: TELJESEN ELTÁVOLÍTVA, "Harminckettedik kör"**)

**ELTÁVOLÍTVA, felhasználói döntés, 2026-08-19**: közvetlen kérés, miután egy megelőző beszélgetésben átbeszéltük, ténylegesen mire jó az MQTT ebben a projektben - "engedjük el a teljes mqtt-t egyelőre. Teljesen irtsuk ki a kódból, amugy is hemzseg a hibáktól." (let's drop MQTT entirely for now - completely eradicate it from the code, it's crawling with bugs anyway). A döntés háttere: a `water-collector.yaml` saját `api:` blokkjának megjegyzése szerint jelenleg SEMMI nem csatlakozik a natív ESPHome API-n keresztül sem (se Home Assistant, se más) - az egyetlen élesben használt fogyasztó a saját dashboard (HTTP/SSE). MQTT-nek nem volt konkrét, azonosított célrendszere ("fejlesztési ötletként" indult, ld. lent), ugyanakkor 6+ körön át visszatérő, valódi hibák forrása volt (boot-loop crash, fókusz-vesztés, jelszó-race, stb. - ld. lent a teljes történetet). **Mit takar a teljes eltávolítás**: `packages/mqtt.yaml` és `include/mqtt_broker_url.h` törölve; `water-collector.yaml` saját `packages: mqtt: !include ...` sora törölve; `web/dashboard.js`/`web/dashboard.css` MINDEN MQTT-specifikus kódja eltávolítva (fejléc-ikon, `ensureMqttHeader()`/`updateMqtt*()`/`mqttEntity()` család, a jelszó-mező szem-ikonos speciális esete - vele együtt az `eye`/`eyeOff` ikonok is, mivel más semmi nem használta őket -, a diagnosztika-oldali `fields` alterület is, mivel kizárólag MQTT töltötte fel). Ellenőrizve: `esphome config` átment, a generált `main.cpp`-ben (`esphome compile`, forráskód-generálásig) **nulla** MQTT-hivatkozás maradt (a korábbi 106 wrapper-objektum és a vele járó boot-log-szennyezés is teljesen megszűnt ezzel), `node --check` és a CSS-zárójel-egyensúly is átment. **Ha valaha újra szükség lenne rá**: ez a szakasz (lent) megtartva, teljes történelmi kontextusként - minden korábbi döntés/hiba/javítás megmarad dokumentálva, csak maga a kód nincs többé a repóban.

A felhasználó eredeti kérése: MQTT be/kikapcsolható legyen a felületről, kapcsolódási paraméterekkel (felhasználónév/jelszó, szerver) együtt, jelszó biztonságos tárolásával, kikapcsoláskor a paraméterek törlésével, és élő visszajelzéssel a kapcsolat állapotáról. A felhasználóval tisztázott, konkrét döntés a jelszó-tárolásról: **"a jelszó titkosítatlanul kerül a flash-be, de a webes felület sosem olvassa vissza kitöltve - csak írásra. Ez elfogadható."** (2026-08-15) - ez alapján implementálva:

- **Nincs szükség egyedi C++ komponensre/fork-ra** - az `mqtt::MQTTClientComponent` publikus API-ja (`set_broker_address()`/`set_broker_port()`/`set_username()`/`set_password()`/`enable()`/`disable()`) mind elérhető közvetlenül `lambda:`-ból (`id(mqtt_id).xxx()`), tehát a teljes megvalósítás sima YAML entitásokkal (switch/text/number/text_sensor) + `lambda:` automatizálásokkal történt, ugyanaz a minta, mint a projekt már meglévő `globals:`/entitás-kombinációi (pl. `modbus_add_claimed`, "Add Name"/"Add Target Address"/"Add" hármas).
- **Entitások** (`sorting_group_mqtt`, "MQTT" csoport, a System oldalon - ld. lent): **"MQTT Enabled"** (switch - ld. lent az újratervezett szerepéért), **"MQTT Connect"**/**"MQTT Disconnect"** (gombok, "Huszonnegyedik kör"), **"MQTT Broker"** (nincs külön Port mező, ld. lent), **"MQTT Topic Prefix"**, **"MQTT Username"** (sima, látható, `restore_value: true` mezők), **"MQTT Password"** (látható, de write-only mező), **"MQTT Status"** (diagnosztikai text_sensor: "Connecting…"/"Connected"/"Disconnected"/"Disabled", az `mqtt:` `on_connect:`/`on_disconnect:` triggerei vezérlik).
- **Nincs külön Port mező (2026-08-15, "Huszonnegyedik kör", direkt visszajelzés)** - egyetlen "MQTT Broker" mező fogad el sima hosztnevet, `host:port`-ot, vagy `scheme://host:port`-ot is (a `mqtts://`/`ssl://` séma szintaktikailag elfogadott, de TLS ténylegesen nincs implementálva - a dashboard saját hint-szövege ezt egyértelművé teszi) - egy új, önálló header (`include/mqtt_broker_url.h`) fejti vissza a hosztot/portot, alapértelmezett port 1883 ha nincs megadva.
- **"MQTT Topic Prefix" mező hozzáadva** - válasz a felhasználó saját kérdésére ("a topic nevét nem kéne tudni megadni, szerkeszteni?") - `MQTTClientComponent::set_topic_prefix()` ugyanúgy publikus API, mint a többi setter, szerkeszthető mező lett belőle, alapértelmezetten az eszköz saját nevére. **Garantáltan kitöltve, "Huszonötödik kör"**: bár a `restore_value: true` + `initial_value` kombináció elvben magától is ezt eredményezné első bootnál, egy explicit `esphome: on_boot:` lépés is biztosítja (üres állapot esetén kitölti az eszköz nevével) - direkt visszajelzés, hogy ez ne csak egy hint legyen, hanem ténylegesen látható alapérték.
- **"MQTT Enabled" újratervezve, "Huszonnegyedik kör", direkt visszajelzés** ("Kéne egy connect gomb és csak arra csatlakozna... Így az enabled csak a komponenseket kapcsolná be vagy ki") - a switch már NEM csatlakozik/bont saját maga: `turn_on_action`-je nincs is (no-op), csak `turn_off_action`-je maradt, ami valódi, teljes törlés (`disable()` + `text.set:`/`number.set:`-tel Broker/Username/(tárolt) Password/Topic Prefix visszaállítása üresre/alapértékre). A tényleges csatlakozást/bontást a két új gomb ("MQTT Connect"/"MQTT Disconnect") végzi - Connect beolvassa az aktuális mezőértékeket (a Broker mezőt a fenti parse()-on átengedve), alkalmazza őket, majd `enable()`-t hív (és bekapcsolja az Enabled switch-et is, hogy a fejléc-ikon/kijelzés szinkronban maradjon); Disconnect csak `disable()`-t hív, a mentett mezőket NEM törli - újbóli Connect ugyanazokkal a beállításokkal folytatja.
- **Automatikus újracsatlakozás reboot után - CSAK ha korábban ténylegesen csatlakozva volt** (finomítva a "Huszonötödik kör"-ben, a "Huszonnegyedik kör" túl radikális, teljes eltávolítása után) - egy perzisztens `globals: mqtt_want_connected` boolean tárolja a szándékot: "MQTT Connect" gombnyomás `true`-ra, "MQTT Disconnect" VAGY "MQTT Enabled" kikapcsolása `false`-ra állítja. Egy `esphome: on_boot:` blokk (priority 200, `AFTER_WIFI`) ezt a flaget ellenőrzi (NEM magát az "MQTT Enabled" kapcsolót, aminek már más a szerepe) - ha igaz, lefuttatja ugyanazt a kapcsolódási logikát, mint a Connect gomb. Direkt idézet, ami ezt pontosította: "reboot után automatikusan kell kapcsolódnia ha már egyszer connectált. Ha disconnectáltunk kézzel akkor reboot után se connectáljon."
- **Jelszó-tárolás konkrét megvalósítása**: egy rejtett, `internal: true` (a `web_server:` REST/SSE felületéről teljesen kizárva, forráskód-szinten megerősítve - ld. `web_server.cpp`'s `include_internal_` ellenőrzése) `text:` entitás (`mqtt_password_stored`) tárolja a TÉNYLEGES jelszót, `restore_value: true`-val, plaintext-ben a flash-en (megerősítve elfogadhatónak, ld. fent) - a látható "MQTT Password" mező sosem perzisztálja saját magát (`restore_value: false`), a beírt értéket a saját `set_action`-je átmásolja a rejtett entitásba (`make_call().set_value(x).perform()`), majd `publish_state()`-tel (NEM `text.set:`-tel, ami újra kiváltaná a saját set_action-jét és felülírná a frissen mentett jelszót) vagy üresre (ha a törlés szándéka volt), vagy egy fix 8-pontos placeholder-re ("••••••••", "Huszonnegyedik kör", direkt visszajelzés: "Ha megadom a jelszót akkor legyen benne ******") áll vissza - sosem a valódi jelszóra vagy annak valódi hosszára. **Egy őszinte, dokumentált korlát**: a beírt jelszó a saját `set` HTTP-kérésének törzsében/az arra adott egyszeri SSE-visszhangban ténylegesen egyszer, plaintext-ben megjelenik a vezetéken (ez elkerülhetetlen bármilyen "jelszó módosítása" űrlapnál, kliens-oldali maszkolással nem elrejthető) - ez nem új kockázat a dashboard már elfogadott "csak helyi hálózat, nincs bejelentkezés, nincs TLS" fenyegetettségi modelljéhez képest (ld. "Hálózat és biztonság").
- **Valódi ESPHome-viselkedés felfedezve, "Huszonnegyedik kör" - `mode: password` NEM azt csinálja, amit elsőre gondolnánk**: `web_server.cpp`'s `text_json_()` forráskódját elolvasva kiderült, hogy egy `mode: password` `text:` entitás jelentett `value`-ja a SZERVER OLDALON, feltétel nélkül (a tényleges tartalomtól teljesen függetlenül) "********"-re cserélődik, minden JSON-kimenetben (mind a kezdeti dump, mind a terse SSE-frissítések) - ez azt jelentette volna, hogy "üres" és "van benne valami" a kliens felől SOSEM különböztethető meg, ha ez a mechanizmus marad használatban (ráadásul az ELŐZŐ kör "MQTT Password" mezője emiatt ténylegesen SOHA nem is jelent meg maszkolva a böngészőben - a kliens-oldali `input.type` sosem lett `"password"`-re állítva, ez a tény csak MOST derült ki, a felhasználó szem-ikon kérése kapcsán). **Javítva**: a mező mostantól `mode: text` (nem `password`) - a maszkolás (gépelés közbeni pöttyök ÉS a "van már jelszó" placeholder) teljes egészében kliens-oldali, `web/dashboard.js` saját, névvel egyeztetett különleges esete kezeli, a szerver a valós, általunk választott állapotot ("" vagy a fix placeholder) hűen továbbítja.
- **`discovery: false`** - a tényleges generált C++ kód megvizsgálásával (`esphome compile`, a forráskód-generálásig, ld. lent a validáció részleteinél) kiderült valami, amit érdemes rögzíteni: pusztán az `mqtt:` komponens JELENLÉTE (a `discovery:` beállítástól FÜGGETLENÜL, mindkét értékkel tesztelve) minden egyes entitáshoz statikusan lefoglal egy `MQTTSensorComponent`/`MQTTSwitchComponent`/stb. wrapper objektumot - ebben a projektben 106 darabot, a generált `new(mqtt_...)` hívások megszámolásával megerősítve - ez a `discovery:` kapcsolóval NEM elkerülhető, csak az MQTT jelenlétének elkerülhetetlen ára. A `discovery: false` mégis a helyes alapértelmezés marad, más okból: a projekt Home Assistant integrációja már a natív `api:`-n keresztül megoldott (ld. "Home Assistant adatmodell") - ha az MQTT discovery is aktív lenne ugyanahhoz a HA-hoz kötött brókerre mutatva, minden entitás duplán regisztrálódna HA-ban.
- **Bootolási log-szennyezés felfedezve és kiküszöbölve, "Huszonhatodik kör"** - a fenti 106 wrapper-objektum mindegyike minden egyes bootnál 2-3 `[C]`-szintű `dump_config()` sort ír, `enable_on_boot: false` mellett IS - egy felhasználó által csatolt valódi eszköz-log alapján megerősítve, kb. 200+ sornyi tiszta MQTT-zaj minden bootnál, mielőtt bármi ténylegesen hasznos log keletkezne, ami a 16 kB-os `log_ring_buffer` kapacitásának nagy részét elviheti. **Megoldás**: a teljes MQTT-funkcionalitás kiemelve egy önálló, opcionális `packages/mqtt.yaml` fájlba - `water-collector.yaml` saját `packages:` szótára egyetlen `mqtt: !include packages/mqtt.yaml` sorral vonja be, ez a sor kikommentezhető/törölhető MQTT nélküli buildhez, ekkor sem a 106 wrapper-objektum, sem a bootolási log-szennyezés nem létezik. Ellenőrizve `esphome config` + `esphome compile` (forráskód-generálásig) + a generált `main.cpp` kézi átvizsgálásával, hogy minden entitás/globális/sorting_group pontosan úgy jelenik meg összefésülve, mint korábban egyetlen fájlban.
- **Log-szennyezés futásidőben is elnémítva (a fenti mellett), "Huszonhetedik kör"** - a fenti package-kiemelés csak azoknak segít, akik teljesen kikommentezik az include sort; aki (mint a felhasználó) TOVÁBBRA IS használja az MQTT-t, ugyanazt a ~200 soros `dump_config()`-kimenetet kapja minden bootnál, ami NEM hiba (minden ESPHome-komponens egyszer, kompilálás miatt automatikusan kiírja saját konfigurációját, függetlenül bármilyen futásidejű "enabled" kapcsolótól), de zavaró. **Megoldás, YAML-szinten valódi**: `packages/mqtt.yaml` saját `logger: logs:` beállítása mind a nyolc érintett MQTT-wrapper log-tag-et (`mqtt`, `mqtt.sensor`, `mqtt.binary_sensor`, `mqtt.switch`, `mqtt.text`, `mqtt.text_sensor`, `mqtt.number`, `mqtt.button`) `WARN` szintre állítja - a naplózó a `CONFIG`-szintű dump_config-sorokat a forrásnál szűri (nem íródnak ki), miközben egy valódi `[W]`/`[E]` futásidejű probléma (pl. sikertelen kapcsolódás) ugyanazon a tag-en továbbra is látszik. Megerősítve a generált `main.cpp`-ben, hogy ez a nyolc `set_log_level()` hívás `App.setup()` legelején fut le, garantáltan minden MQTT-wrapper saját `setup()`/`dump_config()`-ja előtt.
- **Validáció ebben a sandboxban**: `esphome config` átment; `esphome compile` a C++ forrás generálásáig sikeresen eljutott (ez lefuttatja a Python `to_code()`-ot minden komponensre, beleértve a `lambda:` blokkok string-tartalmát is beágyazza a generált `main.cpp`-be - ezt manuálisan átnézve megerősítve, hogy minden `id()`-hivatkozás, `make_call()`, `switch.is_on:` feltétel stb. a várt, helyes C++ hívássá fordult), utána ugyanaz a hálózati korlát (ESP-IDF toolchain letöltés blokkolva) állította meg, mint a log-letöltésnél - a tényleges xtensa-gcc fordítás csak a következő valódi flashelésnél derül ki.

### Hálózati konfiguráció bővítése (statikus IP) – **elvetve, felhasználói döntés** (2026-08-15, "Huszonharmadik kör")

**Lezárva**: a lenti architekturális akadály (a `wifi_component.cpp` fork-olásának kockázata/terjedelme) ismeretében a felhasználó úgy döntött, nem kéri ezt a funkciót ("nem kell a statikus IP") - a jelenlegi, kizárólag DHCP-alapú működés marad, nincs tervezett további munka ezen a téren.

A felhasználó kérése: több hálózati beállítási lehetőség a felületről, például fix (statikus) IP - a mezőket illetően a felhasználó a saját javaslatomra bízta ("amit javasolsz"). **Mélyebb vizsgálat után kiderült, hogy ez - a log-letöltéssel és az MQTT-vel ellentétben - NEM tisztán YAML+lambda-szinten megoldható, és egy valódi architekturális akadályba ütközik**, amit a tényleges `wifi_component.cpp` forráskódjának elolvasásával erősítettem meg (nem csak a header alapján feltételezve):

- **A `WiFiComponent::start()` (amit a saját `setup()`-ja szinkron módon meghív) MINDIG felülírja a `sta_` konfigurációt a saját mentett SSID/jelszó preferenciájából, ha van elmentett hálózat** (`pref_.load(&save)` → `sta.set_ssid(...)`/`set_password(...)` → `this->set_sta(sta)`) - ez a `WiFiAP` objektum NEM tartalmaz `manual_ip`-t, tehát bármi, amit egy másik komponens ELŐTTE (`on_boot:`, magasabb priority) beállított volna a statikus IP-re, ezzel felülíródik. UTÁNA (alacsonyabb priority) meg már túl késő: a tényleges kapcsolódás (`start_initial_connection_()`) ugyanabban a szinkron `setup()`/`start()` hívásban elindul, mielőtt bármelyik későbbi (alacsonyabb priority-jú) komponens saját `setup()`-ja lefutna.
- **A `WiFiAP::set_manual_ip()` publikus API létezik, de nincs hozzá tartozó, a mentett SSID/jelszóhoz hasonló futásidejű PERZISZTENCIA-mechanizmus** (`save_wifi_sta()` KIZÁRÓLAG SSID+jelszót ment, semmi mást) - `manual_ip` kizárólag arra lett tervezve, hogy a YAML `manual_ip:` blokkjából, fordításidőben, EGYSZER kerüljön be a generált kódba, nem futásidejű, felhasználó általi módosításra.
- **A tényleges, a `start()`-on belüli IP-konfigurációt alkalmazó metódus (`wifi_sta_ip_config_()`) `protected`**, nem publikus API - kívülről, egy másik komponensből nem hívható, csak magának a `WiFiComponent`-nek a saját belső logikájából.
- **Amit ez ténylegesen jelentene, ha mégis meg akarnánk csinálni**: a `wifi_component.cpp` egy részének módosítása/fork-olása (hasonlóan a projekt saját `web_server_idf` fork-jához) - DE ez egy sokkal nagyobb, kockázatosabb munka lenne, mint a `web_server_idf` fork: az egy konkrét, szűken körülhatárolt, megerősített hibát javít; ez egy teljesen ÚJ KÉPESSÉGET adna hozzá a projekt hálózati kapcsolódásért felelős, a legkritikusabb komponenséhez - egy itt elkövetett hiba nem "csak" egy dashboard-funkció törését jelentené, hanem azt, hogy az eszköz elveszítheti a hálózati elérhetőségét (fizikai hozzáféréssel/újraflasheléssel helyrehozható, nem "brick", de valós kényelmetlenség).
- **Státusz**: implementáció **nincs elkezdve**, amíg ezt a felhasználóval meg nem beszéljük - konkrétan azt, hogy elfogadható-e a `wifi_component.cpp` fork-olásának kockázata/terjedelme egy statikus IP funkcióért, vagy inkább maradjunk a jelenlegi, kizárólag DHCP-alapú működésnél.

### Busz-szkennelés és cím-váltás (2026-08-13: implementálva, valós Modbus-szal)

- **Busz-szkennelős betanítás**: a busz automatikus végigpásztázása (`1–247` cím, rövid `~25ms`/cím timeout-tal), hogy listázza, mely Modbus-címek elérhetők, de még nincsenek felvéve a mi listánkba – **implementálva** (`include/rs485_modbus.h`'s `scan_bus()`, a "Pressure Sensors Scan Bus" gomb, `water-collector.yaml`). A cím írása/mentése/olvasása a `docs/hardver/qdw90a-modbus-referencia.md`-ban dokumentált, gyártói hivatalos Modbus-doksi alapján megerősített eljárást követi.
- **Cím-váltás a már bekötött eszközön**: a pontos, hardveren megerősített eljárás – **implementálva** (`include/rs485_modbus.h`'s `change_address_and_save()`, `pressure_sensor.yaml`'s Modbus Address `set_action`) – **fontos részlet**: ez **nem egy Modbus-művelet**, a cím-írás válasza még a régi címről jön, a mentést (`H:15=0`) már az **új** címre kell küldeni; ezt a kétlépéses logikát a firmware kezeli, végén egy read-back-kel ellenőrizve.
- **Ütközés kimutatása – két különböző dolog, nem szabad összekeverni**:
  - **"Ugyanaz a cím szerepel kétszer a mi listánkban"** – ez tiszta szoftver, **implementálva van** (a táblázat Address mezőjének módosításakor kliens-oldali ellenőrzés fut, ha a beírt cím már foglalt egy másik, általunk már betanított slotnál). **2026-08-15-től kemény tiltás, nem csak figyelmeztetés** – korábban egy `confirm()`-mel átkattintható figyelmeztetés volt, de mivel ez pont az az útvonal, ami TÉNYLEGESEN újraprogramozza a már bekötött fizikai szenzort (`change_address_and_save()`), egy valós felhasználói incidens (átkattintott figyelmeztetés → azonnali fizikai busz-ütközés, csak hardver-leválasztással javítható) megmutatta, hogy ez nem elegendő védelem – lásd "Huszadik kör".
  - **Valódi elektromos/protokoll-szintű ütközés** (két fizikai eszköz tényleg ugyanazon a címen válaszol egyszerre) – **pontosítva, 2026-08-13, valós hardveren tesztelve**: a korábbi feltételezés ("megkülönböztethetetlen attól, mintha senki nem válaszolt volna") túl pesszimista volt. Szándékosan 3 eszközt azonos (`1`) címre állítva a busz-scan **nem** timeoutot kapott, hanem egy ténylegesen megérkezett, de CRC-hibás választ (`include/rs485_modbus.h`'s `scan_bus()`/`probe()` most ezt a két esetet külön kezeli: `NO_RESPONSE` vs. `COLLISION`) – tehát a "valami válaszolt, csak korrupt" **igenis megkülönböztethető** a "tényleg senki nem válaszolt"-tól, méréssel megerősítve. Ez **nem bizonyíték** ütközésre (elvben sima buszzaj is okozhatná), de **erős, gyakorlatban használható jelzés** – a dashboard táblázatban külön "Collision?" státuszként jelenik meg (mind regisztrált slotnál, mind egy még nem regisztrált címnél). **Élő, folyamatos ütközés-figyelés** (a slotonkénti pollozásra is kiterjesztve, nem csak a scan-re) **implementálva**, ld. fent "Negyedik kör".

### Kalibráció

- A nyers regiszterérték (`H:4`) `bar`, 2 tizedesjegy skálázással érkezik (`/100`) – **hardveren és a gyártó hivatalos Modbus-doksijával is megerősítve** (`docs/hardver/qdw90a-modbus-referencia.md`), külön finomskálázás nem szükséges.
- **Nyitott kérdés lezárva**: kell-e finomkalibrációs eltolás? **Igen, és ez már be van építve magába az eszközbe** – a `H:12` (nullponteltolás) regiszter írható, `kimeneti nyomás = kalibrált mérés + H:12` képlettel (gyártói hivatalos doksi, 2026-08-13). **Nem kell saját szoftveres offset-réteget építeni az ESP oldalán** – elég egy UI-mező, ami közvetlenül ezt a regisztert írja (majd `H:15=0`-val menti), a vízmérők "Reading" mezőjéhez hasonló UX-szel.

### Diagnosztika

- Kommunikációs állapot méterenként (Modbus timeout/hiba) – egy busz-osztott szenzor hibája **nem befolyásolhatja** a többi szenzor működését (a busz maga megosztott, de a hibakezelés méterenként független legyen).
- **"Communication OK" – implementálva, 2026-08-13, a tervezettől eltérő úton**: az eredeti terv az ESPHome beépített `modbus_controller` komponensének `on_online`/`on_offline` mechanizmusára épített volna – ez viszont egy **fordításidőben rögzített** slave-címet feltételez controllerenként, ami összeegyeztethetetlen azzal, hogy nálunk slotonként **futásidőben** változhat a cím (ld. "Architekturális megfontolás v3"). Emiatt a `modbus_controller:`-t végül sehol nem használjuk – helyette saját, kézzel írt Modbus-mester (`include/rs485_modbus.h`), és a "Communication OK" jelzést egy slotonkénti "Online" `binary_sensor` adja (`pressure_sensor.yaml`), amit a nyomás-pollozás lambdája frissít minden sikeres/sikertelen olvasás után.
  - Nincs `max_cmd_retries`-szerű "N sikertelen próba után offline" logika – minden egyes `5s`-enkénti pollozás önmagában dönt, egyetlen kimaradt válasz azonnal "Lost"-ra váltja a slotot, a következő sikeres pollozás pedig azonnal vissza "OK"-ra. Ez egyszerűbb, mint a tervezett retry-alapú logika, és eddig elegendőnek tűnik – ha a gyakorlatban túl "villódzónak" bizonyul (átmeneti zajra túl érzékeny), egy egyszerű "N egymást követő hiba után Lost" számláló utólag hozzáadható.
  - **Érték-tartomány elleni külön védelem (`0–10 bar`) egyelőre nincs beépítve** – a pollozás jelenleg a Modbus-kommunikáció sikerességét tekinti "OK"-nak, a leolvasott értéket magát nem validálja tartomány szerint. Nyitva hagyott finomítás, nem blokkoló.
- **Gyors, teljes busz-scan diagnosztikai eszköz – implementálva, 2026-08-13**: a "Busz-szkennelés és cím-váltás" szakaszban leírt `scan_bus()` pontosan ezt a szerepet is betölti (jelzi pl. "3 eszköz van bekötve, csak 2 cím válaszol" eltérést) – a teljes `1–247` Modbus-tartományt pásztázza (nem csak `1–32`-t, ahogy eredetileg tervezve volt, mivel ez a korlát nem tartotta vissza a megvalósítást). **Egy halott eszköz és egy cím-ütközés mostantól megkülönböztethető** (ld. fent "Ütközés kimutatása") – **nem** tudja viszont megmondani, hány/melyik eszköz ütközik egy adott címen, csak azt, hogy ott valami korrupt választ ad.
- **Watchdog-crash a busz-scan alatt, valós hardveren reprodukálva és javítva, 2026-08-13**: egy teljes `1–247` scan szinkron módon, egy gombnyomásból fut le (~6mp, ha semmi nem válaszol) – ez alatt sosem tér vissza az ESPHome saját fő ciklusába, ami rendes körülmények között eteti az ESP-IDF task-watchdogot, ezért ~5mp után az eszköz összeomlott/újraindult. Javítás: a várakozó ciklus (`include/rs485_modbus.h`'s `wait_for_bytes()` – minden Modbus-tranzakció mögött ott van, nem csak a scan-nél) explicit `App.feed_wdt()`-et hív minden ciklus-fordulón.
- **Modbus-kommunikáció naplózása – implementálva, 2026-08-13**: a `rs485_modbus.h` minden művelete a `"modbus"` log-tag alatt naplóz. Alapból (a `logger:` `initial_level: DEBUG` beállításával) is látszik a busz-scan kezdete/vége (talált címek listája) és egy regisztrált slot sikertelen pollozása (a "Lost" jelzés oka) – ehhez nem kell semmit bekapcsolni. A teljes vezetékszintű részletezés (minden kiküldött/fogadott Modbus-keret hexában, CRC-hiba/időtúllépés/rossz cím oka soronként) `VERY_VERBOSE` szinten érhető el, amit a Log oldal saját "Debug: Modbus" kapcsolója (`water-collector.yaml`'s `switch:` szekció, `logger.set_level` akció) futásidőben be/ki kapcsol – alapból kikapcsolva, mert egyetlen teljes scan is ~250 sornyi naplót termelne rajta.

### Perzisztencia

- A slave cím (betanítás eredménye) és a Display Name futásidőben, azonnal perzisztálódjon (NVS) – a vízmérők `offset_m3`-ához hasonlóan, nem várja meg a checkpointot.
- A pillanatnyi nyomásérték maga **nem** perzisztens állapot (nincs "felhalmozás", mint a vízmérőknél) – újraindítás után egyszerűen az első sikeres Modbus-olvasás adja az aktuális értéket.

## Kezdeti implementációs döntések

- A `pulse_meter` végzi az impulzusérzékelést, pergésmentesítést és periódusidő-alapú rátamérést. A futásidőben állítható nulla-átfolyás timeout külön ESPHome logikával történik, mert a `pulse_meter.timeout` nem futásidőben konfigurálható.
- Közös logika egy ESPHome csomagban, vízóránkénti példányosítás `substitutions`-szel (`meter1`/`meter2` id-prefix – szándékosan generikus, semmilyen konkrét telepítésre jellemző név (pl. "Fő"/"Locsoló") nincs a kódba égetve; a kódban és fájlnevekben angol terminológia, lásd `water-collector.yaml`). Melyik fizikai mérő melyik, az a dashboard "Display Name" mezőjével, futásidőben állítható be.
- **Checkpoint (`pulse_count`)**: a futó `pulse_count` egy ESPHome `preferences`/`global` állapotban él, minden impulzusnál frissül. A tényleges flash-írást a `flash_write_interval: 60s` korlátozza – ez önmagában **nem** a checkpoint mechanizmusa, csak a fizikai flush gyakoriságát szabályozza, a checkpointot maga a preference-alapú tárolás adja. Ha a `pulse_count` az adott flush-időszak alatt nem változott, ne történjen felesleges fizikai írás – ezt implementáció közben ellenőrizni kell (az ESP-IDF NVS réteg elvben már önmagában kihagyja az azonos érték újraírását, de erre tesztelés nélkül nem szabad vakon támaszkodni).
- **Kézi szinkron (`offset_m3`)**: módosításkor explicit, azonnali preference-sync/`save()` történik, nem várja meg a `flash_write_interval`-t.
- **Nulla-átfolyás timeout megvalósítása**: saját, egyszerű "watchdog" logika – egy `interval:` komponens rendszeresen összeveti az utolsó impulzus időbélyegét a beállított timeout-tal, és lejáratkor explicit 0-ra állítja a Térfogatáram/Impulzusráta szenzorokat.
- **Helyi `web_server_idf` komponens-felülírás** (`components/web_server_idf/`, bekötve `water-collector.yaml`-ban `external_components:`-tel): valós eszközön reprodukált, forrásból (backtrace `addr2line`-nal dekódolva + upstream forrás olvasva) megerősített use-after-free hiba javítása az ESPHome beépített webszerver-komponensében – a "stuck EventSource connection" takarítás korábban a saját C++ objektumot törölte, mielőtt a `httpd` ténylegesen lezárta volna a hozzá tartozó socketet/session-t, ami később (más, ártatlan `free()` hívásnál, jellemzően az lwIP saját housekeeping-jében) heap-korrupcióhoz vezetett. A javítás a részletes indoklással együtt magában a felülírt `web_server_idf.cpp`-ben van dokumentálva. Upstreamben (esphome/esphome, 2026.7.3 és a jelenlegi main is) még nincs javítva; ha ez megtörténik, ez a felülírás (és a teljes `components/web_server_idf/` másolat) törölhető.

## Jelenleg nem követelmény

- Hibaészlelés/riasztás (szivárgás, csőtörés, tartós nulla fogyasztás stb.) – később, HA oldalon kerül definiálásra. A szükséges nyers adat (historizált `Összes fogyasztás`) már rendelkezésre áll ehhez, a jelenlegi adatmodell emiatt nem igényel bővítést. Felbontási korlát: `liters_per_pulse`-nál (1 l) kisebb szivárgás egy impulzusköznyi időn belül nem észlelhető.
- Fizikai/környezeti kialakítás (ház, védettség, tápellátás) – nem szoftverkövetelmény.
- **Nyomásmérő modul**: riasztás/küszöbérték-figyelés (alacsony/magas nyomás, pl. szivattyú-védelem) – ugyanúgy HA oldalon, később, ugyanazon elv szerint, mint a vízmérőknél. Historizáció szintén HA feladata (Scope szerint amúgy is általános elv).
- **Nyomásmérő modul**: finomkalibrációs UI (`H:12` nullponteltolás) – a regiszter írható és a képlet ismert (ld. "Kalibráció"), de nekünk explicit nem szükséges (a gyártói gyári kalibráció elég), ezért egyelőre nincs hozzá felületi elem.
