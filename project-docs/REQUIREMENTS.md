# Vízellátó rendszer felügyelet – követelmények

## Scope

ESP32/ESPHome alapú felügyeleti rendszer az aknában lévő vízellátó rendszerhez. A Home Assistant biztosítja a historizálást, statisztikát és megjelenítést, de az ESP működése nem függhet a HA vagy a hálózat elérhetőségétől.

## Impulzusszámláló modul

### Hardver

- Impulzusadó: **IZAR PULSE i, 4-wire 5m** (Diehl Metering, P/N 3108333), induktív érzékelésű, **open collector** kimenet, saját lítium elemmel (a jeladó nem igényel tápellátást az ESP-től).
  - Dokumentáció: [`docs/hardver/izar-pulse-i-adatlap.pdf`](docs/hardver/izar-pulse-i-adatlap.pdf), [`izar-pulse-i-telepitesi-utmutato.pdf`](docs/hardver/izar-pulse-i-telepitesi-utmutato.pdf).
  - **A beszerzett egységen ellenőrzött bekötés** (a konkrét darab címkéje alapján, 3-kimenetes, egyidejűleg 3 eltérő felbontású jelet ad):
    - fehér = impulzus, `RATIO 1:1` → 1 impulzus = 1 liter
    - sárga = impulzus, `RATIO 10:1` → 1 impulzus = 10 liter
    - zöld = impulzus, `RATIO 100:1` → 1 impulzus = 100 liter
    - barna = föld (közös GND)
  - Csak a **fehér (1:1) vezetéket** kötjük az ESP GPIO-jára a legjobb felbontásért, + barna GND. Sárga/zöld nem kerül bekötésre.
  - Kimenet nyitott kollektoros → ESP oldali pull-up szükséges.
  - `liters_per_pulse = 1` (a fehér vezetékkel), a paraméter így is konfigurálható marad.
  - Max. impulzusfrekvencia: 8 Hz, impulzushossz: 50–500 ms.
  - Kompatibilis Diehl/MOM óracsalád (a gyártói lista kifejezetten tartalmazza a **Corona M**-et).
- Vízóra: **MOM Corona D3 1"**.
- ESP board: **Waveshare ESP32-S3-RS485-CAN**, izolált RS485/CAN, DIN-sínes – **megérkezett, 2026-08-13**, a `water-collector.yaml` `substitutions`-ai (`board_type`, pulzus-GPIO-k) mostantól közvetlenül erre céloznak (a korábbi, nem-S3 tesztboard értékei kikommentezve maradtak referenciaként). Dokumentáció: [`docs/hardver/esp32-s3-rs485-can-board.md`](docs/hardver/esp32-s3-rs485-can-board.md) (+ eredeti PDF ugyanott). A jeladók a board SH1.0 csatlakozójára kerülnek: `1. vízmérő → GPIO1`, `2. vízmérő → GPIO2` (`meter1`/`meter2`, lásd Kezdeti implementációs döntések – melyik fizikai mérő melyik, az telepítésfüggő, nincs a kódba égetve).
- Két mérési pont, azonos modulstruktúrával, egymástól függetlenül. A jelen telepítésben (nem a firmware része, csak a konkrét beállítás):
  - **1. vízmérő ("Fő")** – teljes fogyasztás (ház + kert).
  - **2. vízmérő ("Locsoló")** – csak a kerti vízellátás (részhalmaza az elsőnek).
  - A ház-only fogyasztás (1. − 2.) HA oldali származtatott szenzorral számolható, nem ESP feladat.

### Funkcionális követelmények

- Minden érvényes impulzust pontosan egyszer számoljon.
- Aktuális térfogatáram: `L/min`. Összesített fogyasztás: `m³`.
- Diagnosztika: összes impulzusszám, impulzusráta.
- Kézi óraállás-szinkron vízóránként független, elérhető Home Assistantból és az ESP saját webes felületéről is.
- Szinkron azonnal perzisztálódik, nem várja meg a checkpointot.

### Elszámolási modell

Vízóránként két perzisztens állapot:

- `pulse_count` – normál futás közben monoton növekvő impulzusszám (soha nem módosul szinkronnál); tápvesztés utáni restore esetén legfeljebb egy checkpoint-időszaknyit visszaállhat (lásd Perzisztencia és hibakezelés).
- `offset_m3` – korrekciós eltolás.

```
Összes fogyasztás [m³] = offset_m3 + pulse_count × liters_per_pulse / 1000
```

Kézi szinkron a fizikai óra `célérték`-jére:

```
offset_m3 := célérték − pulse_count × liters_per_pulse / 1000
```

Ezzel a diagnosztikai impulzusszám törésmentes marad, a fogyasztás pedig azonnal a megadott értékre áll, és a rendszer minden mutatott adata konzisztens.

A kézi szinkron az `Összes fogyasztás`-t **lefelé is** mozgathatja (ha a fizikai óra állása kisebb, mint amit az ESP addig mutatott) – ilyenkor ugyanaz a HA `total_increasing` statisztikai következmény jelentkezik, mint a tápvesztés utáni visszaugrásnál, csak itt szándékosan. A kézi szinkron ritka, karbantartási jellegű esemény; ha a HA hosszú távú statisztikáját ez érzékenyen érinti, azt szükség esetén kézzel kell korrigálni – emiatt nem indokolt az ESP adatmodelljét bonyolítani.

### Pillanatnyi átfolyás számítása

- A Térfogatáram és az Impulzusráta **egymást követő impulzusok közti időből** (periódusidő) származik, nem fix időablakos impulzusszámlálásból – ez a fix időablakos számlálásnál jobb felbontást és reakciót ad alacsony átfolyásnál. A kettő ugyanabból a mérésből számolt, egymással konzisztens (ugyanaz az arány köztük, mint `liters_per_pulse`).
- **Nulla-átfolyás timeout**: ha egy vízórán X másodpercig nem érkezik új impulzus, a Térfogatáram/Impulzusráta explicit 0-ra áll (különben az utolsó periódusidőből számolt érték érvénytelenül "befagyva" maradna).
  - Alapértelmezett: `60 s`, vízóránként konfigurálható – futásidőben állítható, alacsony szintű paraméter, elsősorban az ESP saját webes felületéről, opcionálisan HA-ból is (nem fordításidőben beégetett érték).
  - A korábban javasolt `15 s` túl agresszívnak bizonyult: 1 l/impulzus mellett már egy hétköznapi, nem is lassú folyás (2–5 L/perc, pl. kézmosás) impulzusköze is 12–30 mp – ennél rövidebb timeout ezt is szaggatottá tenné. `60 s` a jóval lassabb, de még hétköznapi (~1 L/perc, csordogálás) folyást is villogás nélkül lefedi.
  - Kompromisszum: rövidebb timeout → gyorsabb, pontosabb "elzárva" jelzés, de tartós alacsony átfolyásnál (ahol az impulzusköz meghaladja a timeoutot) a kijelzett érték szaggatottan 0 és a tényleges ráta közt ugrál. Hosszabb timeout → simább alacsony átfolyás, de lassabb "elzárva" jelzés valódi leálláskor.
  - Ez csak a pillanatnyi Térfogatáram/Impulzusráta kijelzést érinti; az Összes fogyasztás (`pulse_count` alapú) ettől függetlenül pontos marad.

### Perzisztencia és hibakezelés

- Az ESP az elsődleges adatforrás; a működés nem függhet HA/MQTT/API/Wi-Fi elérhetőségétől.
- `pulse_count` RAM-ban él, periodikusan checkpointként kerül NVS-be. Az `offset_m3` **nem** része a periodikus checkpointnak – csak szinkronkor változik, és akkor azonnal, önálló írással perzisztálódik, nincs mit rajta rendszeresen menteni.
- Checkpoint időköz **fix `60 s`, fordítási időben rögzített** érték (nem futásidőben állítható, sem HA-ból, sem ESP webről) – az ESPHome `web_server` ugyanazokat az entitásokat adná ki az API-n (HA) keresztül is, mint a helyi weboldalon, tehát a "csak ESP-n állítható" elkülönítés egyedi komponenst igényelne. Az egyszerűség kedvéért ezt nem vállaljuk be.
  - A 60 s jó kompromisszum az adatvesztési ablak (tápvesztéskor legfeljebb ennyi impulzus veszhet el) és a flash-terhelés között. A tényleges fizikai írásszám ennél kevesebb is lehet, mert csak akkor kell írni, ha a `pulse_count` valóban változott az adott időszakban (lásd Kezdeti implementációs döntések) – nincs fogyasztás, nincs felesleges írás.
  - Ha később mégis más érték kellene, az egy YAML/`substitutions` módosítás + újraflashelés, nem éles konfigurációs paraméter.
- Kézi szinkron (`offset_m3`) mindig azonnal perzisztál, checkpointtól függetlenül.
- Újraindítás után a számlálás az utolsó perzisztált `pulse_count`/`offset_m3` alapján folytatódik.
  - Tápvesztéskor legfeljebb egy checkpoint-nyi impulzus veszhet el, emiatt újraindítás után az `Összes fogyasztás` kis mértékben visszaugorhat. Ez elfogadott mellékhatása a checkpoint-alapú perzisztenciának. A HA `total_increasing` szenzortípus alapvetően monoton növekvő értéket vár; a csökkenést bizonyos esetekben resetként kezeli, de ennek pontos viselkedése (pl. a kis mértékű csökkenésekre vonatkozó tolerancia) HA-verziófüggő és nem garantált – a hosszú távú statisztikák helyes működését implementáció közben külön ellenőrizni kell.
- Első implementáció nem igényel külső FRAM-ot vagy más kiegészítő nem felejtő memóriát.

### Hálózat és biztonság

- **Wi-Fi SSID/jelszó soha nem kerül fordítási időbe** (se `secrets.yaml`-ba, se máshova a firmware-t generáló repóban) – az eszköz Wi-Fi hitelesítő adat nélkül bootol, azt kizárólag futásidőben, az első indításkor kapja meg Bluetooth (BLE improv) vagy Wi-Fi hotspot (AP + captive portal) útján, és az ESP a saját flash-ébe menti (nem a build tárolja).
- Egyéb, eszköz-specifikus secretek (API titkosítási kulcs, OTA jelszó) ESPHome `secrets.yaml`-ban, verziókezelésből kizárva – ezeknek nincs futásidejű provisioning megfelelőjük az ESPHome-ban, de nem hálózati hitelesítők, nem szivárogtatnak semmit az otthoni hálózatról.
- Az ESP saját webes felülete (dashboard) **nem kér jelszót** – tisztán helyi hálózaton elérhető, iOS-en kezdőképernyőre kitehető appként is használt felület, aminek egy bejelentkezési képernyő csak zavaró súrlódás lenne, valódi biztonsági nyereség nélkül (aki már rajta van az otthoni Wi-Fi-n, úgyis eléri). Tudatosan vállalt kompromisszum – ha valaha távolról/nem megbízható hálózatról is elérhető lenne az eszköz, ezt felül kell vizsgálni.

### Home Assistant adatmodell

Vízóránként (a jelen telepítésben: 1. vízmérő/"Fő", 2. vízmérő/"Locsoló" – lásd Scope):

Üzemi entitások:

- `Térfogatáram` – `L/min`, `state_class: measurement`
- `Összes fogyasztás` – `m³`, `device_class: water`, `state_class: total_increasing`

Diagnosztikai entitások:

- `Impulzusráta` – `impulzus/min`, `state_class: measurement`
- `Összes impulzus` – `impulzus`, state class nélkül (nincs szükség rá HA-statisztikában – arra az `Összes fogyasztás` szolgál –, és ugyanúgy visszaugorhatna tápvesztéskor, mint a `pulse_count`)

Beállító/szerviz entitások:

- fizikai vízóra állásának megadása (`m³`) + szinkronizálás
- nulla-átfolyás timeout (`number`, s)

Checkpoint időköz nem entitás, fix `60 s`, fordítási időben rögzítve (lásd Perzisztencia és hibakezelés).

### Idősoros adatok és statisztikák

- Az ESP nem tárol hosszú távú idősoros adatot; ez, valamint az aggregáció (óra/nap/hét/hónap) HA feladata.
- Térfogatáram és összesített fogyasztás legyen historizált a HA-ban.
- HA kiesése alatt az ESP folytatja a számlálást; a részletes térfogatáram-idősor megőrzése ESP oldalon nem követelmény.
- Visszakapcsolódáskor az ESP aktuális abszolút `pulse_count`/`offset_m3` alapú értéke az irányadó.

### Kalibráció

- Az impulzus–térfogat átváltás (`liters_per_pulse`) vízóránként konfigurálható.
- Az impulzusbemenet pergésmentesítése konfigurálható (ESPHome `pulse_meter` `internal_filter` paramétere, nem külön logika).

**Javasolt kiegészítés (még nem eldöntött)**: `liters_per_pulse` jelenleg fordításidőben rögzített (`substitutions`) – tegyük futásidőben is állíthatóvá (pl. `number` entitás, hasonlóan a Nulla-átfolyás timeout-hoz), hogy egy más arányú vezeték bekötése (pl. a fehér 1:1 helyett sárga 10:1/zöld 100:1, lásd Hardver) vagy más gyártmányú impulzusadó ne igényeljen újraflashelést. Kockázat, amit kezelni kell: mivel `Összes fogyasztás = offset_m3 + pulse_count × liters_per_pulse / 1000`, egy futásidejű váltás a már felhalmozott `pulse_count`-ra visszamenőleg hatna – a váltáskor az `offset_m3`-at automatikusan újra kell számolni úgy, hogy az `Összes fogyasztás` ne ugorjon (hasonlóan a kézi szinkronhoz, csak automatikusan, a régi/új arány különbségére).

## Nyomásmérő modul

### Hardver

- Érzékelő: **QDW90A** (Anhui Qidian Automation Technology), diffúz szilícium piezorezisztív nyomás-távadó. Gyártói adatlap: [`docs/hardver/qdw90a-nyomastavado-adatlap.pdf`](docs/hardver/qdw90a-nyomastavado-adatlap.pdf), gyártói hivatalos Modbus-protokoll doksi: [`docs/hardver/qdw90a-modbus-protocol-gyartoi.pdf`](docs/hardver/qdw90a-modbus-protocol-gyartoi.pdf). **Modbus-regiszterek, bekötés, betanítás – saját, hardveren megerősített referencia**: [`docs/hardver/qdw90a-modbus-referencia.md`](docs/hardver/qdw90a-modbus-referencia.md).
  - **A beszerzett/tervezett kivitel**: RS485/Modbus RTU kimenet (az adatlap alapváltozata 4-20mA áramhurok, de a Modbus-kimenet is dokumentált kivitel, 4-vezetékes bekötéssel – **ezt a variánst rendeljük**), táp `24V DC`, mérési tartomány `0–10 bar` (`0–1.0 MPa`).
  - `G1/4` menetes csatlakozás, pontosság `±0.2% F.S.`, védettség `IP65`.
  - Kommunikáció a **Waveshare ESP32-S3-RS485-CAN** board saját, galvanikusan leválasztott RS485 illesztőjén keresztül (lásd [`esp32-s3-rs485-can-board.md`](docs/hardver/esp32-s3-rs485-can-board.md), `GPIO17` TX / `GPIO18` RX / `GPIO21` EN – ESPHome `uart: flow_control_pin` a hardveres RS485 fél-duplex módhoz) – **implementálva, 2026-08-13** (ld. Architekturális megfontolás v3 – Státusz), hardveres tesztelés még hátra van.
  - Darabszám: kezdetben **3 mérési pont** tervezve. Melyik fizikai pont mit mér (pl. bemenet/kimenet/szűrő előtt-után), az telepítésfüggő – ugyanúgy, mint a vízmérőknél, ez nem kerül a kódba égetve, futásidőben (Display Name) nevesíthető.
- **RS485 hub: CDEBYTE E810-R14** (1→4 irányú, opto-izolált RS485 repeater/hub) – a telepítés fizikai adottságai miatt a szenzorok nem köthetők egy szál kábelre daisy-chain-nel, csak csillag-topológiában; ez az eszköz oldja meg a csillag-kábelezést anélkül, hogy protokoll-szinten bármit is módosítana. Hivatalos user manual (letöltve, ellenőrzött forrás): [`docs/hardver/e810-r1x-rs485-hub-user-manual.pdf`](docs/hardver/e810-r1x-rs485-hub-user-manual.pdf).
  - **Fontos**: teljesen protokoll-átlátszó ("no configuration required, transparent data transmission between master and slave interfaces") – nem multiplexer, nem ismeri a Modbust. Pontosabban (a manual szerint): a host felőli adat egyszerre, broadcast-szerűen jut el mind a 4 kimeneti csatornára; a csatornák felől visszafelé jövő adatot a hub time-sharing módon (egyszerre csak egy csatornáról) továbbítja a host felé – ütközés nélkül, de a Modbus szintjén ez láthatatlan/irreleváns. A ráakasztott szenzoroknak emiatt továbbra is **egyedi Modbus-címre van szükségük** az egész buszon (ld. lent, "betanítás"), ez a hub bevezetésével nem változik.
  - **Csillag-kábelezés tisztázása**: a manual saját "Wiring Precautions" fejezete általános RS485-alapelvként "kerüld a csillag-topológiát, használj hand-in-hand (daisy-chain) bekötést" – ez viszont a **klasszikus, elágazásokkal teli busz** eseté vonatkozik (jel-visszaverődés/impedancia-illesztési probléma elágazási pontoknál). A mi terveink szerint **minden egyes hub-csatornára pontosan 1 szenzor** kerül (nem több szenzor elágaztatva egy csatornáról) – ez csatornánként egy tiszta, elágazás nélküli pont-pont összeköttetés, ami topológiailag **nem esik a "kerülendő csillag" kategóriába**, sőt jel-integritás szempontjából legalább annyira jó, mint egy daisy-chain. A hub saját csatlakozási rajzai (3. fejezet) kifejezetten mutatnak is egy hosztról induló, több szlévre ágazó elrendezést mint támogatott használati módot.
  - Saját táp: `DC 9–40V`, a busztól galvanikusan leválasztva (`1.5kV` izoláció, teljesen külön a jelföldtől – "power ground, not interoperable with digital signal ground"). A `24V DC`, ami a szenzoroknak amúgy is kell, ezen a tartományon belül van, tehát **valószínűleg ugyanarról a tápforrásról üzemeltethető**, csak külön föld-vezetékkel a hub saját táp-GND-jéhez. Van saját PE (védőföld) csatlakozója is, azt kösd földre.
  - Baud tartomány `300–230400` (egyéni baud is támogatott) – a szenzor `1200–115200` tartományát bőven lefedi. A busz össz-eszközszám korlátja `32` – ez az általunk tervezett 4 slot-nak bőven elég tartalékot ad.
  - Gyártói infó: [cdebyte.com/products/E810-R14](https://www.cdebyte.com/products/E810-R14).
  - **Miért kell a hub akkor is, ha a master board (ESP32-S3-RS485-CAN) RS485 illesztője már eleve izolált, és a kábelek csak pár méteresek?** Mert a master-oldali izoláció a masztert védi a buszról, nem a szenzorokat egymástól – hub nélkül csillagba kötve mind ugyanazon a közös, nem izolált buszszakaszon lennének, így egy eltérő földpotenciálú (más gépházban lévő) szenzor közös módusú feszültsége simán túlmehetne a driver tartományán. A hub csatornánként külön izolál, ez oldja meg ezt – a topológia (csillag vs. daisy-chain) önmagában, pár méteres kábellel és `9600` baudon valószínűleg amúgy sem okozna gondot.
- **Modbus regisztertérkép** – ⚠️ eredetileg nem hivatalos forrásból (HA közösségi fórum) állt össze, **2026-08-12-én kibővítve** egy sokkal részletesebb, szintén nem hivatalos, de alaposabb forrással (TapHome QDW90A kompatibilitási oldala, nyers jegyzet: [`docs/hardver/qdw90a-modbus-taphome-jegyzet.md`](docs/hardver/qdw90a-modbus-taphome-jegyzet.md) – munkaanyag, nem végleges referencia), **majd ugyanazon a napon egy teljes regiszter-scan-nel (`mbpoll`, `H:0–H:39`) a valós hardveren végigmérve**. Az alábbi táblázat **minden dokumentált regisztere hardveren megerősített**:

  | Reg. | Funkció | Mért érték | Állapot |
  | --- | --- | --- | --- |
  | `H:0` | Slave cím (`1–255`) | `1` | ✅ hardveren megerősítve |
  | `H:1` | Átviteli sebesség-kód (`0`=1200 … `7`=115200) | `3` = 9600 | ✅ hardveren megerősítve |
  | `H:2` | Mértékegység-kód (`3`=bar, teljes lista a TapHome-jegyzetben) | `3` = bar | ✅ hardveren megerősítve |
  | `H:3` | Tizedesjegyek száma – `érték = H:4 / 10^H:3` | `2` | ✅ hardveren megerősítve (tehát ténylegesen `/100`) |
  | `H:4` | Mérési érték, `Int16`, skálázás `H:3` szerint | `0` (terheletlen) | ✅ hardveren megerősítve, élőben reagál valós nyomásra (ld. lent) |
  | `H:5` | Méréstartomány alsó pontja | `0` | ✅ hardveren megerősítve |
  | `H:6` | Méréstartomány felső pontja | `1000` → `/100`-zal **10.00 bar** – egyezik a szenzor `0–10 bar` méréshatárával | ✅ hardveren megerősítve, és értelmes is |
  | `H:12` | Nullponteltolás, `Int16`, írható | `0` (gyári alapállapot) | ✅ hardveren megerősítve |
  | `H:15` | Beállítás mentése – `0` írása menti tartósan a `H:0`/`H:1`/stb. módosítását (**enélkül a változtatás elveszhet újraindításkor!**) | `0` (olvasva) | ✅ olvasva, ahogy vártuk – **írás még nincs kipróbálva** |
  | `H:16` | Gyári visszaállítás – `1` írása töröl mindent (cím, kalibráció is) | `0` (olvasva, **nem lett kiváltva**) | ✅ biztonságosan csak olvasva – **írást szándékosan nem próbáltunk ki** |
  | `H:22–H:23` | Mérési érték közvetlenül IEEE754 Float32-ként, big-endian/ABCD | `0.0` | ✅ hardveren megerősítve, egyezik a `H:4`-alapú olvasással – **firmware-hez ígéretes alternatíva** a `H:4`+`H:3` kombináció helyett |
  | `H:37` | Soros paritás (`0`=nincs, `1`=páratlan, `2`=páros) | `0` = nincs | ✅ hardveren megerősítve |

  **A TapHome-jegyzetben nem szereplő, de a teljes scan során talált, valószínűsíthetően valódi paraméterblokk** (`H:22`-től kezdve 2-2 regiszteres Float32 párok, "kerek" értékekkel – ez nem véletlen mintázat):

  | Regiszterpár | Float32 érték | Jelentés |
  | --- | --- | --- |
  | `H:24–H:25` | `0.0` | ismeretlen, illik a mintázatba |
  | `H:26–H:27` | `1.0` | feltételezés: kalibrációs szorzó/gain, jelenleg semleges értéken – **nincs megerősítve** |
  | `H:28–H:29` | `0.001` | feltételezés: felbontás/lépésköz paraméter – **nincs megerősítve** |

  **Egyéb, nem dokumentált, nem nulla regiszterek** a `H:0–H:39` scanből, jelentésük ismeretlen, firmware-ben nem használjuk őket: `H:8=2`, `H:9=-174`, `H:10=-180`, `H:11=13017`, `H:14=82`, `H:21=8`, `H:33=907`, `H:39=-21829`. (A `H:20` a nagy scan-ből valóban kimaradt, de **nem hiba/kihagyás volt** – ld. lent, "kérésméret-korlát", `H:20` maga rendben olvasható, `0` értékkel.)

  **⚠️ Kérésméret-korlát, firmware-releváns**: **egy `0x03` kérésben legfeljebb 20 regiszter kérhető le egyszerre** – 21 vagy több regiszterre a szenzor a teljes kérést elutasítja, a szabványostól eltérő `Illegal Function` kivétellel (nem a várt "Illegal Data Address"-szel). Reprodukálva: `H:1–H:20` (20 db, `-r 1 -c 20`) sikeres, `H:0–H:20` (21 db, `-r 0 -c 21`) teljes hiba. Ez magyarázza a korábbi, `H:0–H:39` scan során tapasztalt "kimaradt `H:20`" jelenséget is (az a kérés minden bizonnyal 20-nál több regisztert próbált egyben lekérni, és valahogy csak részlegesen jelent meg az eredmény). **A firmware-es Modbus-olvasást ennek megfelelően kell darabolni** (max 20 regiszter/kérés), ld. részletesen: [`docs/hardver/qdw90a-modbus-referencia.md`](docs/hardver/qdw90a-modbus-referencia.md).

  - **Slave cím (`H:0`) – ✅ megerősítve valós QDW90A egységen**, 2026-08-12, kézi Modbus RTU kerettel (FTDI USB-RS485 adapter, `9600 8N1`, közvetlenül a szenzorra kötve, hub nélkül): `01 03 00 00 00 01 84 0A` kérésre `01 03 02 00 01 79 84` érkezett vissza (érték `0x0001` = `1`). A bekötés a gyári kábelszínezés szerint `Piros=24V+`, `Fekete=24V-`, `Kék=PC-A`, `Sárga=PC-B` (a "Ground Wire" felirat a csatlakozó silkscreen-jén félrevezető ezen a kivitelen – nincs külön föld-ér, csak a táp `Fekete` vezetéke). Külön GND-vezeték az FTDI és a szenzor közt **nem** kellett hozzá (rövid, asztali teszt).
  - **Nyomásérték (`H:4`) – ✅ regiszter és skálázás is teljesen megerősítve**: nyugalomban `0` (várt érték terheletlen/légköri állapotban), majd a nyomáscsatlakozóba szájjal belefújva (a tényleges mérési úton, nem mechanikai membrán-nyomogatással) folyamatosan, monoton nőtt: `0 → 3 → 4 → 5` nyers érték (mindegyik CRC-vel ellenőrizve, valódi válasz). A `H:2`/`H:3` regiszterek (`mbpoll`-lal) külön kiolvasva: `H:2 = 3` (bar) és `H:3 = 2` (2 tizedesjegy) – tehát a `0 → 3 → 4 → 5` nyers érték ténylegesen `0.00 → 0.03 → 0.04 → 0.05 bar`-t jelent (`érték = H:4 / 100`), pontosan egyezve a korábbi feltevésünkkel és az emberi fújásnyomás (`~0.01–0.05 bar`) várt tartományával. **A `H:4`-ből számolt `bar` érték firmware-kódba építhető, nincs többé feltételezés benne.**

### Funkcionális követelmények

- A dashboard "Home" oldalán megjeleníti az aktuális nyomásértéket (`bar`), a vízmérők kártyáihoz hasonló elrendezésben.
- Minden nyomásmérő pont átnevezhető (Display Name), futásidőben – ugyanaz a minta, mint a vízmérőknél.
- **Szerviz/"betanítás" folyamat**: a Modbus slave cím (és opcionálisan a baud rate, ha a gyári alapértelmezett nem egyezik egységeknél) az ESP saját webes felületéről, futásidőben állítható/módosítható, méterenként – ez a mechanizmus teszi lehetővé, hogy 3 azonos gyári címről induló egységet egy közös buszon egyedileg meg lehessen különböztetni felszereléskor, újraflashelés nélkül.
- **Betanítási előfeltétel**: egyszerre csak **1, még gyári alapcímen lévő** eszköz lehet a buszon – ha több egyforma című egység van fent egyszerre, mindegyik válaszolna a lekérdezésre, a cím nem állítható be egyértelműen. Gyakorlatban: az új szenzort a hub adott csatornáján egyedül (a többi lekötve, vagy még be sem kötve) kell címezni, csak utána köthető rá a közös buszra a többihez.

### Architekturális megfontolás: "dinamikus" nyomásmérő-hozzáadás

- Az ESPHome **fordításidőben rögzített, statikus entitásmodellt** használ – nincs natív támogatás arra, hogy futásidőben, újraflashelés nélkül vadonatúj entitás (pl. egy negyedik nyomásmérő) jöjjön létre a semmiből.
- **Javasolt kompromisszum** (a vízmérő-modul mintájára, ott is bevált): fix számú, előre bekötött "slot" – **4 db** (döntés, felülírva a korábbi 8-as tervet: pontosan annyi, ahány fizikai portja van a tervezett E810-R14 hubnak, tartalék nélkül – ha a jövőben egy második hub vagy több szenzor kerülne a rendszerbe, ez a szám firmware-oldali bővítést és újraflashelést igényelne, ez elfogadott kompromisszum). Mindegyik slot:
  - alapból inaktív/rejtett a dashboard "Home" oldalán, amíg nincs hozzá betanított (Modbus-címmel ellátott) szenzor,
  - saját, futásidőben állítható Modbus slave címmel (a fenti "betanítás"),
  - saját Display Name-mel.
- **Konfigurációs UX (v2, implementálva)**: nem egyszerű be/kikapcsoló kapcsoló, hanem **Hozzáadás/Törlés**, kompakt táblázat (Név | Cím oszlopok) a Service oldalon:
  - alapból **egyetlen betanított slot sem látszik** sem a Home, sem a Service oldalon – csak egy "+" gomb, ami egy névbekérő mezőt nyit ("Pressure Sensors New Sensor Name" + "Pressure Sensors Add" entitások, `water-collector.yaml`),
  - "Hozzáadás"-ra a soron következő szabad slot (`packages/pressure_sensor.yaml`, `try_commission` script) felveszi a megadott nevet, és **onnantól** látszik a táblázatban és a Home kártyák közt is,
  - "Törlés"-re (a meglévő JS `confirm()` visszakérdezéssel, `CONFIRM_ON_PRESS`) a slot adatai visszaállnak alapállapotba, és **teljesen eltűnik** mindkét helyről,
  - a slot sorszáma (melyik fizikai slot-ID kapja a nevet) nem jelenik meg sehol, csak a Display Name – ez szándékos, ld. `pressure_sensor.yaml` fejléce.
- **Fontos technikai tanulság, ami a végleges tervet meghatározta**: az első próbálkozás a fenti "csak akkor létezzen, ha be van tanítva" hatást az ESPHome `internal` flag-jének **futásidejű** átkapcsolásával (`set_internal()`) próbálta elérni. A ténylegesen telepített ESPHome forrásában (`esphome/core/entity_base.h`) ellenőrizve kiderült: ez **hivatalosan deprecated, "undefined behavior"** 2026.3.0 óta ("Components and Home Assistant are NOT notified... Use the 'internal:' YAML key instead"), 2027.3.0-ban törlik – tehát erre nem lehet architektúrát építeni. **A végleges megoldás**: minden entitás mindig létezik (soha nem `internal`), a "betanítva van-e" állapotot egy sima, mindig küldött `binary_sensor` ("Commissioned") **értéke** jelzi, amit a dashboard saját JS-e (`web/dashboard.js`) értelmez – ez teljesen támogatott, élőben frissül, nem deprecated API-ra épül.
  - **Ennek ára – korlát, amit el kell fogadni**: mivel nincs megbízható, támogatott mód egy entitás valódi eltüntetésére HA elől futásidőben, a **"csak a látható szenzorokat engedjük HA felé" nem valósul meg 100%-osan** – HA mindig látni fogja mind a 4 slot entitásait, csak `disabled_by_default: true`-val, alapból kikapcsolt állapotban (a meglévő konvenció). Ez a gyakorlati plafon ESPHome natív API-val, MQTT discovery-alapú, teljesen más architektúra nélkül.
  - Korábbi, elvetett "kétlépéses gomb-megerősítés" ötlet (JS `confirm()` helyett) szintén okafogyottá vált – a meglévő `CONFIRM_ON_PRESS` JS-mechanizmus (amit már Update/Restart/Forget Wi-Fi is használ) pont erre való, nem kellett hozzá semmi új.
- Ez **nem korlátlan dinamikus bővítés**, hanem "4 előre definiált, egyenként konfigurálható slot" – ez pontosan a jelenlegi vízmérő-modul mintája is (2 fix slot, futásidőben nevesíthető/kapcsolható). Ha a cél valódi, darabszám-korlát nélküli bővíthetőség, az egy jelentősen nagyobb architekturális váltás lenne – erről külön kell dönteni, ha ez ténylegesen cél.
- **Státusz (a fenti, v2 leírás)**: implementálva volt, de a lenti v3-as tervezés **felülírja/kibővíti** – a v2 "+" folyamata (üres névmező, cím utólag kézzel) helyébe a scan-alapú, cím-előre-ismert hozzáadás lép. Újraírás szükséges, még nincs implementálva.

### Architekturális megfontolás v3 (2026-08-13): scan-alapú, "dinamikus DB-nek tűnő" tábla

Hosszabb tervezési beszélgetés eredménye – a cél egy olyan felület, ami úgy **viselkedik**, mintha egy tetszőleges méretű, dinamikus adatbázis-táblát mutatna (sorok jönnek-mennek, nincs látható "slot 3/8" sorszámozás), miközben a valóságban **8 fix, fordításidőben rögzített slot** van mögötte (ESPHome-korlát, ld. fent – ezt "elfedni" lehet a felületen, de megszüntetni nem).

- **Döntés: `N` vissza `8`-ra** (a korábbi, "elég a 4" döntés felülírva) – a hub ténylegesen `32` node-ot bír a buszon, a `4` önkényesen a hub fizikai portszámához lett igazítva. A `8` olcsó tartalék, jelentősen csökkenti annak esélyét, hogy a plafon ténylegesen elő is jöjjön.
- **Szigorú szétválasztás: perzisztens vs. élő/efemer adat** – ez a kulcs-felismerés, ami az egész tervet meghatározza:
  - **Perzisztens, slotonként**: kizárólag **cím + Display Name**. Semmi más (nincs külön "betanítva" flag – ld. lent, ez már a címből következik).
  - **Élő/efemer, soha nem tárolt, mindig frissülő**: busz-scan jelenléte, folyamatos pollozás online/offline állapota (`modbus_controller` `on_online`/`on_offline`, ld. Diagnosztika) – ezekből **kliens-oldalon (JS) számolt, sosem flash-be írt** állapot.
- **A táblázat sorai egy JOIN eredménye** (busz-scan találatai × a 8 slot jelenlegi címei, cím szerint):
  - **"Regisztrált, elérhető"** – van slotunk erre a címre, ÉS a legutóbbi scan/pollozás válaszolt rá.
  - **"Regisztrált, nem elérhető"** (LOST/ERROR) – van slotunk erre a címre, de a legutóbbi scan/pollozás **nem** kapott választ – ez pontosan az eredeti "3 eszköz van bekötve, csak 2 válaszol" jelzés, a JOIN természetes mellékterméke, nem külön funkció.
  - **"Új eszköz"** – a scan talált egy választó címet, de egyik slotunk sem regisztrálja – ide kerülnek a még nem betanított eszközök.
- **Hozzáadás – soronként, nem egy közös folyamat**: ha egyszerre több "Új eszköz" sor is látszik, mindegyiknél **saját "Hozzáadás" gomb** van (nem egy általános "+", ami kétértelmű lenne, melyik eszközre vonatkozik). A hozzáadás a scan-ből már ismert címet azonnal felveszi, csak nevet kell hozzá kérni – ezzel a v2-es "előbb név, aztán kézzel beírt cím" folyamata feleslegessé válik.
- **Plafon, őszintén kezelve**: ha mind a 8 slot foglalt, és a scan egy 9. (vagy további) választó, nem regisztrált címet is talál, az a sor **továbbra is megjelenik "Új eszköz"-ként**, de a "Hozzáadás" gombja **inaktív, egyértelmű üzenettel** ("nincs szabad hely – firmware-bővítés kell hozzá") – soha nem egy csendben semmit sem csináló gomb.
- **Cím módosítása regisztrált eszközön**: **ténylegesen újraprogramozza a fizikai eszközt is** (a dokumentált kétlépéses Modbus írás+mentés szekvenciával, ld. `docs/hardver/qdw90a-modbus-referencia.md`, `include/rs485_modbus.h`'s `change_address_and_save()`), nem csak a mi rekordunkat írja át – utána a rekord frissül, hogy kövesse az új címet. Sikertelen újraprogramozás esetén a rekord **akkor is** frissül (a `TemplateNumber` optimista publikálását/perzisztálását `set_action`-ből nem lehet visszagörgetni – ld. `pressure_sensor.yaml` saját megjegyzése), ez elfogadott, önkorrigáló rés: ha a fizikai cím valójában nem változott, a következő pollozás a (tévesen felvett) új címen sikertelen lesz, a slot "Lost" állapotba kerül – ugyanaz a valódi diagnosztikai jelzés, mint egy ténylegesen leszakadt szenzornál.
- **Ez nem egy külön "Sensor Config" oldal** (ahogy korábban felmerült) – egyetlen, egységesített nézet, ami kiváltja a jelenlegi "Pressure Sensors" táblázatot.
- **Státusz**: implementálva, 2026-08-13, **valódi Modbus RTU-val** a végleges Waveshare ESP32-S3-RS485-CAN boardon (`water-collector.yaml`'s `uart: rs485_uart`, protokoll-logika `include/rs485_modbus.h`-ban – saját, kézzel írt Modbus-mester, nem az ESPHome beépített `modbus_controller:`-je, mert az egy fordításidőben rögzített slave-címet feltételez, nekünk viszont slotonként futásidőben állítható cím kell, plusz busz-szintű címscan, amire a beépített komponensnek nincs megfelelője). Lefedett funkciók: valódi busz-scan (`scan_bus()`), slotonkénti folyamatos nyomás-pollozás + élő "Online" állapot (`read_pressure_bar()`, H:22–H:23 Float32), és a fenti valódi cím-újraprogramozás (`change_address_and_save()`).
  - **Hardveren tesztelve, 2026-08-13** – a felhasználó saját HA ESPHome add-on-ján keresztül lefordítva/flashelve, valós QDW90A szenzorral. Menet közben talált és javított hibák:
    - **Watchdog-crash busz-scan alatt** – ld. "Diagnosztika", javítva.
    - **Súlyos hiba a `try_register` kölcsönös-kizárásában**: minden slot csak a *saját* címét ellenőrizte (`== 0`), nem azt, hogy egy másik slot már felvette-e ugyanazt a címet ebben a körben – friss (mind a 8 slot üres) boardon emiatt **mind a 8 slot** felvette ugyanazt a szenzort. Javítás: megosztott, nem perzisztens `modbus_add_claimed` globális flag (`water-collector.yaml` – szándékosan nem `pressure_add_claimed`, mert ez a koordináció bármilyen jövőbeli Modbus eszköztípusra vonatkozik, nem csak a nyomásmérőkre), amit az "Add" gomb minden nyomás előtt visszaállít, és amit az első sikeres `try_register` azonnal `true`-ra állít – így a körön belül tényleg csak egy slot nyerhet.
    - **A táblázat "X" (Delete) gombja hatástalan volt** – a kattintás-kezelő a megosztott `pressButton()` segédfüggvényt hívta, ami az `entity.btnEl`-t várja, de ezt semmi nem állította be a nyomásmérő-táblázat saját renderelési útján (a generikus `upsertServiceButton()`-t, ami máskor ezt tenné, a nyomásmérő-entitások sosem érik el). Kivétel nélkül elnyelt hibát dobott minden kattintásra. Javítva: a táblázat maga állítja be `btnEl`-t minden renderkor.
    - **Az "OK" státusz-jelző gombnak nézett ki** – a korábbi kitöltött, lekerekített "pill" design túl hasonlított a valódi gombokra (pl. "Scan Bus"). Átalakítva egyszerű színes pont + szöveg jelzésre, kattinthatatlan megjelenéssel.
    - **Ütközés-kimutatás** hozzáadva, ld. fent "Ütközés kimutatása" – valós, szándékos 3-eszköz-azonos-cím teszttel megerősítve.
  - **Második kör, valós hardveren, 2026-08-13 (ua. nap, a fenti javítások után)** – további talált/javított problémák:
    - **A "Scanning…" visszajelzés nem tartott a teljes scan-ig** – az eredeti megoldás a gomb saját `fetch()` promise-ának időtartamára támaszkodott, abból a (téves) feltételezésből, hogy az ESPHome `web_server`-je a gomb `on_press:`-ének teljes lefutásáig nyitva tartja a HTTP-választ. Valós hardveren megcáfolva: a `fetch()` szinte azonnal visszatért, jóval a ~6mp-es scan vége előtt. Javítás: egy valódi, élő `binary_sensor` (`modbus_scan_in_progress`), amit a scan eleje/vége explicit publikál – a felület ebből, nem a `fetch()` időzítéséből vezeti le a gomb "Scanning…" állapotát.
    - **Cím-újraprogramozás ellenőrzése tévesen hibásnak jelezte a sikeres átállítást** – a napló szerint mindkét írás (cím + mentés) sikeresen visszaigazolódott, csak a *közvetlenül utána* következő ellenőrző olvasás nem kapott választ (utána a normál pollozás azonnal, megbízhatóan működött az új címen). Valószínű ok: a `H:15=0` mentés a szenzor saját flash-írása, ami alatt pár ms-ig nem válaszol. Javítás: rövid (`50ms`) várakozás a mentés és az ellenőrző olvasás közé, ugyanaz a minta, mint a cím-írás és a mentés között már meglévő `20ms`.
    - **A táblázat Add/X gombja kilógott a sor keretéből** – a korábbi kereteket a `<tr>`-re rakta, ami `border-collapse`-es táblázatban nem biztosan követi megbízhatóan egy `width: 1%` cella tényleges (a gomb miatt szélesebb) méretét. Javítva: a keret minden `<td>`-n saját maga (a szokásos, robusztus minta), nem a `<tr>`-en.
    - **A Cím mező felesleges fel/le nyilai** eltávolítva (CSS-ből, `type="number"` megtartva a mobil numerikus billentyűzet és validáció kedvéért).
    - **Pollozási gyakoriság `5s` → `1s`, timeout `200ms` → `100ms`** – a `200–500ms`-es pollozási intervallum-javaslat a blokkolási matek miatt (8 regisztrált slot, mindegyik akár a teljes timeout-ig blokkolva, ha egyszerre elérhetetlenek) túl szorosnak bizonyult; `1s` + rövidebb timeout kb. 5×-ös reszponzivitás-javulás a korábbi blokkolási kockázat drasztikus növelése nélkül. Finomhangolható tovább, ha szükséges.
  - **Harmadik kör, felhasználói visszajelzésre, 2026-08-13 (ua. nap)**:
    - **Cím-szerkesztés után egy elavult, valójában téves "Új eszköz" sor jelent meg a régi címmel** – az első körben ezt tudatos, el nem fogadott kompromisszumnak jelöltem ("a Scan Results csak pillanatfelvétel"), de jogos ellenvetésre újragondolva: ez nem elfogadható – egy sorból logikailag nem lehet kettő, csak mert a cím megváltozott. **Javítva**: sikeres `change_address_and_save()` után a firmware maga frissíti a "Scan Results" CSV-t (`include/rs485_modbus.h`'s `update_scan_result_address()`) – kiveszi a régi címet, beteszi az újat (amit a visszaolvasás már úgyis megerősített) –, így a szellem-sor nem jöhet létre. Sikertelen újraprogramozásnál nem nyúl hozzá (ott a régi cím elvben tényleg érvényes maradhat).
    - **Pollozási gyakoriság tovább csökkentve, `1s`/`100ms` → slotonként `520–800ms` (40ms lépésekben szórva) / `50ms`** – a felhasználói kérésre ("legyen amilyen rövid csak lehet") újraszámolva: a korábbi közös, egyetlen `1s` érték helyett minden slot **saját, egymástól 40ms-mal eltérő** `update_interval`-t kapott (`water-collector.yaml`) – ezzel a 8 slot pollozása fokozatosan szétcsúszik egymástól (nem ragad örökre egy fázisban, ahogy egyetlen közös érték esetén tenné, mivel mind egyszerre indul boot-kor), csökkentve annak esélyét, hogy egyszerre több slot blokkolja a fő loop-ot. A timeout `50ms`-re csökkent (valós hardveren mért válaszidők ennél jóval gyorsabbak). Legrosszabb eset (mind a 8 egyszerre elérhetetlen): `8×50ms=400ms`, a legrövidebb slot saját `~520ms` ablakán belül.
  - **Negyedik kör, felhasználói visszajelzésre, 2026-08-13 (ua. nap)**:
    - **A nyomásmérő-táblázat blokkja láthatóan szélesebb volt, mint a Service oldal többi kártyája** – egyszerű CSS-elírás volt: `.dc-pressure-group`-nak `480px` max-width-je volt a többi kártya (`.dc-service-group`) `420px`-je helyett. Javítva.
    - **Élő ütközés-kimutatás a folyamatos pollozásban is, nem csak explicit Scan Bus-ra** – valós teszttel feltárt hiányosság: ha egy már regisztrált slot címére utólag (a legutóbbi scan óta) kerül egy második eszköz, a slot addig csak Lost/OK közt villogott, minden magyarázat nélkül, amíg valaki véletlenül újra le nem futtatta a Scan Bus-t. **Javítva**: a folyamatos pollozás (`read_pressure_bar()`) mostantól ugyanazt az "érkezett válasz, csak CRC-hibás" jelzést adja vissza, mint a scan, és ezt egy új `set_scan_collision_address()` segédfüggvénnyel közvetlenül a "Scan Collisions" CSV-be írja/törli minden pollozáskor – a dashboard meglévő JOIN-logikája (ami már eddig is ebből olvasta a regisztrált sorok "Collision?" állapotát) módosítás nélkül azonnal felveszi. Így az ütközés a felfedezés pillanatában látszik, nem csak a következő explicit scan-nél.
  - **Ötödik kör, felhasználói visszajelzésre, 2026-08-13 (ua. nap)**:
    - **Név/cím szerkesztése túl könnyen elsült** – a mezők minden `blur`-re (fókuszvesztésre) azonnal írtak, ami Cím esetén valódi újraprogramozást indított el – túl könnyű volt véletlenül elindítani (pl. Tab-bal továbblépve a soron). **Javítva**: a mezők alapból csak-olvasható (`readOnly`) állapotban vannak; egy soronkénti ceruza gomb kapcsolja szerkeszthetőre, ekkor a ceruza/kuka helyén Mentés (pipa) és Mégse (x) gomb jelenik meg – csak a Mentés írja ténylegesen az eszközt, a Mégse (vagy elnavigálás) eldobja a helyi módosítást (`web/dashboard.js`'s `upsertRegisteredPressureRow()`).
    - **"X" gomb → kuka ikon** – beszédesebb, egyértelműen "törlés"-ként olvasható első pillantásra is (ua. függvény, csak ikoncsere).
    - **Törlés után a cím azonnal, elavult adatból "Új eszköz"-ként visszatért a listába** – a Delete gomb eddig csak a mi rekordunkat törölte (cím→0, név→alapértelmezett), de nem nyúlt a legutóbbi busz-scan "Scan Results" pillanatfelvételéhez, amiben a törölt cím még mindig szerepelt – a dashboard JOIN-ja emiatt azonnal (friss scan nélkül, elavult adatból) újra "Új eszköz" sorként jelenítette meg, ami úgy nézett ki, mintha a törlés soha nem tudná ténylegesen eltávolítani az eszközt a listából. **Javítva**: a Delete gomb `on_press`-e (a cím nullázása *előtt*, amíg a slot még ismeri saját címét) kiveszi a címet a "Scan Results"/"Scan Collisions" CSV-kből is (`include/rs485_modbus.h`'s új `remove_scan_result_address()`, meglévő `set_scan_collision_address()`), így a sor ténylegesen eltűnik, amíg egy tényleges újraszkennelés meg nem erősíti, hogy még mindig válaszol ott valami.
    - **Új: a táblázat sorrendje állítható** (fizikai csőhálózati sorrend leképezéséhez, ne csak a Modbus cím sorrendje számítson) – minden slot kapott egy tisztán a felület saját belső állapotát képviselő, az eszközzel/címmel semmilyen kapcsolatban nem álló "Sort Order" mezőt (`pressure_sensor.yaml`, alapértelmezett `0` = "még nincs testreszabva", ekkor cím szerint rendez, mint eddig). Minden regisztrált soron Fel/Le nyíl gombpár (`web/dashboard.js`'s `movePressureRow()`) cseréli meg a sor pozícióját a szomszédjával – egy nyomás az **összes** regisztrált slot Sort Order értékét egyszerre írja újra (nem csak a két megcserélt sorét), így soha nem maradhat hátra elavult vagy ütköző rangsorszám, bármilyen állapotból induljon is. Törléskor a törölt slot saját Sort Order-je is nullázódik (a többi sorét nem kell újraszámozni – a fennmaradó számok közti "lyuk" a rendezés szempontjából ártalmatlan, csak a relatív sorrend számít), így egy később ugyanabba a slotba kerülő új eszköz a testreszabott sorrend aljáról indul, nem örökli a régi rangsorszámot.
    - **Válasz a felmerült kérdésre: árt-e a szenzornak, ha még gyakrabban pollozzuk?** A pollozás egy sima Modbus-olvasás (H:22–H:23 kiolvasása) – ez **nem ír semmit a szenzor flash-ébe**, tisztán a szenzor RAM-beli/ADC-mérési állapotát olvassa le digitálisan, mechanikai/élettartam-kopás nélkül (szemben a cím-átprogramozással, ami ténylegesen flash-írás – ld. `change_address_and_save()`-nál a `H:15=0` mentési lépés). Tehát a pollozási gyakoriság önmagában **nem élettartam-kérdés** a szenzoron. Amit valóban korlátoz: a fizikailag megosztott, félduplex RS485 busz sávszélessége (egyszerre csak egy tranzakció futhat rajta) – ld. a staggerelt intervallumok fenti indoklását –, illetve hogy a nyomás fizikailag lassan változik, így egy bizonyos ponton túl a gyorsabb pollozás már nem ad ténylegesen frissebb/hasznosabb adatot, csak feleslegesen terheli a buszt.
  - **Hatodik kör, felhasználói visszajelzésre, 2026-08-13 (ua. nap)**:
    - **Hozzáadás név nélkül** – az "Új eszköz" sor "Hozzáadás" gombja eddig üres névvel is elsült, ekkor a firmware egy generikus "Pressure Sensor N" alapértelmezett nevet adott a slotnak – ez hibás feltételezés, hiszen a slot nem feltétlenül nyomásmérő lesz (több Modbus eszköztípus tervezve). **Javítva**: a "Hozzáadás" gomb üres/csak-szóköz névnél inaktív (`web/dashboard.js`'s `upsertNewPressureRow()`), élőben (gépelés közben) is, nem csak a következő renderre várva.
    - **Szerkesztés gyakorlatilag lehetetlen volt** – a valódi ok egy, az előző körben (sorrend-funkció) bevezetett regresszió volt: minden renderelés (ami egy regisztrált slotnál gyakorlatilag minden egyes pollozásra lefut, ~fél-egy másodpercenként) feltétel nélkül `appendChild`-elte az összes regisztrált sort, hogy érvényesítse a sorrendet – ez akkor is "megmozgatta" (leválasztotta, majd újra beillesztette) a DOM node-ot, ha az már eleve a helyén volt, ami böngészőben elveszi a fókuszt bármelyik éppen fókuszban lévő mezőtől. **Javítva**: a sorrend-érvényesítés mostantól egy standard "csak akkor mozgat, ha tényleg nem a helyén van" algoritmust használ (`renderPressureTableBody()`) – a leggyakoribb esetben (nem történt tényleges átrendezés) nulla DOM-mozgatással jár, így a szerkesztés alatt lévő mező fókusza nem szakad meg a következő pollozás-eredményű újrarenderre.
    - **Szerkesztés/nézet mód nem volt vizuálisan megkülönböztethető, és a mezőkbe view módban is be lehetett lépni** – **javítva**: a mezők view módban `disabled` (nem csak `readOnly`) állapotban vannak, így kattintással/fókusszal sem érhetők el, amíg a ceruza gombot meg nem nyomjuk; a szerkesztés alatt álló sor emellett saját háttérszínt kap (`.dc-pressure-row-editing`), hogy a mód egyértelműen látszódjon.
    - **Ütközés-jelzés hamis "OK" villanása** – valós teszttel megerősítve: két, azonos címen ütköző eszköz esetén egyetlen tiszta (nem-ütköző) válasz nem bizonyítja, hogy az ütközés elmúlt (a buszarbitráció miatt bármelyik pollozás "véletlenül" tiszta választ kaphat, miközben a másik eszköz továbbra is ott van) – ez rövid, félrevezető "OK" villanásokat okozott. **Javítva, de nem "beragadó" megoldással** (a felhasználó kifejezett kérésére: nem szabad örökre Collision-ben ragadnia, ha közben tényleg lehúzzuk az ütköző eszközt) – hiszterézis: minden slot számol egy "egymást követő tiszta pollozások" streaket (`pressure_sensor.yaml`, `${id_prefix}_collision_clean_streak` globals), egyetlen ütköző válasz azonnal nullázza és jelzi az ütközést, de a jelzés csak **két egymást követő** tiszta pollozás után törlődik – egy sima timeout (se ütközés, se tiszta válasz) se nem nullázza a streaket, se nem törli a jelzést, mert önmagában semmit nem bizonyít.
    - **"Collision?" → "Collision"** (mind a regisztrált soron, mind az önálló ütközés-soron).
    - **Forgó "spinner" a Scan Bus gombon** a "Scanning…" felirat mellett, amíg a scan fut (`syncScanButtonBusyState()`), a szöveges visszajelzés mellett egy azonnal, első pillantásra is látható vizuális jelzésként.
    - **Service oldal szélesség-reszponzivitása** – a kártyák (`.dc-service-group`) és a nyomásmérő-táblázat (`.dc-pressure-group`) korábban egy fix, keskeny (`420px`) max-width-ön ragadtak minden képernyőméreten, feleslegesen keskenyen hagyva a helyet szélesebb (pl. asztali) nézetben. **Javítva**: `.dc-service-group` `560px`-re, `.dc-pressure-group` (mivel valódi, több oszlopos táblázatot tartalmaz, nem csak egy mezőt) `900px`-re emelve – mindkettő továbbra is csak max-width, keskeny nézetben ugyanúgy zsugorodik, mint eddig. A táblázat emellett saját, oldalirányban görgethető konténert kapott (`.dc-pressure-table-scroll`) – ha egy jövőbeli új oszlop (pl. eszköztípus – nyomásmérő vs. áramlásmérő, ld. alább) miatt a tábla szélesebb lenne, mint a rendelkezésre álló hely, ne az oszlopok zsugorodjanak olvashatatlanra vagy vágódjanak le, hanem a tábla önmagában görgethető legyen. Egy külön, mobil-specifikus "sor alatt kinyíló szerkesztő/részletek terület" elrendezés (a felhasználó saját, nem megkötő javaslata) egyelőre elhalasztva – érdemesebb majd egy konkrét új attribútum (pl. eszköztípus) bevezetésekor újragondolni, nem előre, spekulatívan.
  - **Hetedik kör, felhasználói visszajelzésre, 2026-08-13 (ua. nap)**:
    - **Kritikus hiba: a nyomásérték eltűnt a Dashboard oldal kártyájáról** – a kártya fejléce (ikon+név) rendben megjelent, de a törzs (az érték maga) végleg üresen maradt, amíg a felhasználó teljesen újra nem töltötte az oldalt. Valódi ok, kód-átvizsgálással megtalálva: `syncPressureHomeCard()` a slot regisztrálatlanná válásakor (cím átmenetileg vagy ténylegesen `0`) törli a Home-kártyát, de a "Pressure" entitáson gyorsítótárazott `entity.el` DOM-referenciát nem törölte – így egy későbbi újra-regisztrálódáskor az `upsertHomeMetric()` saját "már létrehoztam, nem kell újra" ellenőrzése (`if (!entity.el)`) tévesen igazra értékelődött ki egy már leválasztott, láthatatlan csomópontra mutató referenciával, és onnantól csak azt frissítette (soha nem csatolta vissza az új kártyához). Egy teljes oldal-újratöltés pont ezért oldotta meg (minden gyorsítótárazott referencia törlődik). **Javítva**: `syncPressureHomeCard()` a kártya törlésekor a "Pressure" entitás `.el` referenciáját is nullázza, így egy későbbi újra-megjelenítés valóban újraépíti és visszacsatolja a sort.
    - **Ütközés-jelzés "OK" villanása, második kör** – a felhasználó megerősítette, hogy az előző (streak-alapú) javítás után is előfordult még rövid, hamis "OK" villanás. Valós, statisztikailag indokolható ok: pusztán a *pollozások száma* alapú számláló (2 egymást követő tiszta olvasás) még mindig sebezhető, mert a buszarbitráció miatt akár két egymást követő pollozás is véletlenül tiszta választ kaphat, miközben az ütközés ténylegesen még fennáll. **Javítva, robusztusabb módszerrel**: a streak-számláló helyett egy **idő alapú "lehűlési" ablak** (`${id_prefix}_last_collision_ms` global, `millis()` időbélyeg) – bármely ütköző válasz azonnal nullázza az órát, és a jelzés csak akkor törlődik, ha **legalább 2 másodperce** nem volt ütközés ÉS az aktuális olvasás is tiszta. Ez nem attól függ, hány konkrét pollozás történt közben (ellentétben a streak-kel), hanem attól, hogy tényleg volt-e elég hosszú, valóban csendes időszak – statisztikailag jóval erősebb garancia, mégsem "beragadó" (ha lehúzzuk az ütköző eszközt, a felépülés ugyanúgy megtörténik, csak a lehűlési ablak leteltével).
    - **Nem-determinisztikus hiba a Scan Bus ütközés-detektálásában**: két azonos címen ütköző eszköz esetén egyetlen scan alkalmanként nem mutatta az ütközést (csak "Új eszköz" jelent meg), egy újabb scan viszont igen – ugyanaz a buszarbitráció-alapú jelenség, mint a folyamatos pollozásnál: egyetlen próba véletlenül tiszta választ kaphat, miközben a második eszköz ténylegesen ott van. **Javítva**: `scan_bus()`/`probe()` (`include/rs485_modbus.h`) minden "FOUND" (tiszta) eredményt egy második próbával megerősít, mielőtt elfogadná – ha a második próba ütközést (CRC-hibás, de érkezett választ) jelez, a cím "Collision"-re vált. Egy második `NO_RESPONSE` NEM írja felül a "FOUND"-ot (önmagában semmit nem bizonyít). Csak azokra a címekre kerül sor a plusz próbára, amik egyáltalán válaszoltak – a teljes 1-247 pásztázás sebessége gyakorlatilag változatlan marad.
    - **Scan Bus gomb hover-re kattinthatónak tűnt tiltott állapotban, és a szélessége megváltozott scan közben** – a `.dc-btn` osztálynak eddig nem volt saját `:disabled` stílusa, így a hover-kiemelés akkor is látszott, amikor a gomb inaktív volt; a "Scanning…" felirat emellett hosszabb volt mint a "Scan Bus", ezért a gomb ténylegesen szélesebbé vált scan közben. **Javítva**: `.dc-btn:disabled` explicit visszaállítja a nem-hover megjelenést; a gomb saját felirata mostantól mindig fix ("Scan Bus"), a "Scanning…" szöveg + a forgó jelző egy külön, a gomb mellett megjelenő elembe (`.dc-pressure-scan-status`) került át, így a gomb mérete állandó marad.
    - **Pollozási időzítés szórásának szűkítése**: a korábbi 40ms-onkénti létra (520-800ms, ~1.5x szórás leggyorsabb/leglassabb slot közt) helyett 10ms-onkénti, szűkebb létra (600-670ms, ~1.12x szórás) – megtartja a garantált, determinisztikus szét-csúszást (bármely 8 különböző fix érték nem esik tartós lockstep-be), de igazságosabban osztja el a frissítési sebességet a slotok közt. Valódi per-pollozás randomizálás megfontolva és elvetve: statisztikailag alkalmanként épp az egyidejű pollozást okozhatná (amit ez a mechanizmus kizárni hivatott), és nehezebben debug-olható lenne egyértelmű előny nélkül.
  - **Nyolcadik kör, felhasználói visszajelzésre, 2026-08-13 (ua. nap)**:
    - **A "Scanning…" felirat/forgó jelző függőlegesen rosszul illeszkedett a Scan Bus gombhoz** – a toolbar korábbi `align-items: flex-end` beállítása még akkor íródott, amikor a toolbarban csak maga a gomb volt; az újonnan mellé került, kisebb betűméretű `.dc-pressure-scan-status` elemmel együtt ez láthatóan a gomb közepe alá csúsztatta a szöveget/jelzőt. **Javítva**: `align-items: center`.
    - **Ütközés-jelzés hamis "OK" villanása, még mindig előfordulhat, de nem reprodukálható megbízhatóan** – a felhasználó megerősítette, hogy a jelenség ritkult, de nem tűnt el garantáltan; ez összhangban van a csatolt naplóval, ami megmutatja, hogy egy ténylegesen ütköző címen a válaszok **nem következetesen** ugyanazzal a hibával futnak (van tiszta CRC-hiba, van részlegesen megérkező majd megszakadó válasz, van teljes timeout is) – mind a három a buszarbitráció valós, változó kimenetele, amit a jelenlegi három-állapotú (`NO_RESPONSE`/`COLLISION`/`FOUND`) osztályozás és a 2 másodperces lehűlési ablak együtt már jól kezel, de statisztikailag sosem lehet 100%-ig kizárni egy nagyon szerencsés, két egymást követő tiszta választ. Nem tekintjük többé önmagában hibának – ez a mechanizmus alapvető, elfogadott korlátja (nem "bizonyíték", "erős jelzés", ld. korábbi körök), amit érdemben csak az ár (lassabb felismerés/lassabb törlés) tovább-emelésével lehetne tovább szűkíteni.
    - **Talált, valós következmény: a Dashboard oldal nyers nyomásértéke "-- bar" és "0 bar" közt villogott ütköző címen** – ez NEM új hiba, hanem a gyökérok (buszütközés) helyes, várt tünete volt, amit eddig semmi nem szűrt ki: egy ütköző címen egy CRC-helyes válasz sem bizonyítja, hogy a *saját* regisztrált szenzorunk válaszolt – ugyanolyan eséllyel a másik, ütköző eszköz saját (esetünkben `0 bar`, mert az nincs nyomás alá helyezve) értéke jött vissza. A Service oldal táblázatának "Collision" jelzője eddig is helyesen kezelte ezt (nem függ az aktuális pollozási eredménytől, a 2mp-es lehűlési ablak védi), de a Dashboard oldal nyers száma közvetlenül a legutóbbi pollozás eredményét mutatta, hysteresis nélkül. **Javítva**: a Dashboard kártya a Service táblázattal **ugyanazt** az ütközés-jelzést (a debounce-olt "Scan Collisions" CSV-t) nézi meg, és amíg egy slot ütközőnek van jelölve, `"--"`-t mutat a valódi (de nem megbízhatóan hozzárendelhető) érték helyett, ahelyett hogy villogva mutatná bármelyik ütköző eszköz aktuális válaszát.
  - **Kilencedik kör, felhasználói visszajelzésre, 2026-08-13 (ua. nap)**:
    - **"Lost" villant fel Hozzáadáskor, egy friss, egészséges szenzoron is** – valódi ok, kód-átvizsgálással megtalálva: `registeredPressureSlots()` `onlineEntity.value === true` alakú ellenőrzése `undefined === true`-t hamisra értékelte ki, amikor az Online bináris szenzor még sosem publikált (a szenzor entitása már létezik a kliens oldalon, csak a saját első pollozása még nem futott le) – a saját megjegyzése tévesen "undefined"-ot ígért erre az esetre, a tényleges kód viszont hamisat adott, ami "Lost"-ként jelent meg. **Javítva**: explicit `onlineEntity.value !== undefined` ellenőrzés, plusz egy új, harmadik állapot ("Checking…", semleges színnel) a Státusz jelzőn, ami elkülöníti "még nem tudjuk" a "megerősítetten offline"-tól.
    - **Új eszköz hozzáadása nem a lista aljára került, hanem cím szerint valahova középre** – a Sort Order alapértelmezett `0` értéke ("nincs testreszabva") eddig cím szerint rendezte egymás közt az még nem testreszabott sorokat, ami egy friss hozzáadást nem feltétlenül a lista végére tett. **Javítva**: egy új, megosztott, monoton növekvő számláló (`next_pressure_sort_order`, `water-collector.yaml`) minden sikeres regisztráláskor a slot saját Sort Order-jébe kerül, majd eggyel nő – ezzel minden új hozzáadás garantáltan magasabb rangsorszámot kap, mint bármelyik korábbi, tehát mindig a lista aljára kerül, függetlenül a címétől. A kézi Fel/Le rendezés (ami 1..N-re "tömöríti" a rangsorszámokat) nem ütközik ezzel, mivel a számláló onnantól is csak nő.
    - **A szerkesztő mezőkben Enter/Esc nem működött** – **javítva**: Enter a Mentés (pipa), Esc a Mégse (x) gombbal egyenértékű, amíg a mező ténylegesen szerkeszthető (letiltott mezőn a böngésző eleve nem küld billentyű-eseményt, tehát ez a kötés magától csak szerkesztés közben aktív).
    - **A törlés megerősítő üzenete a slot belső, szándékosan jelentés nélküli sorszámát mutatta** (pl. "Pressure Sensor 3"), nem a szenzor nevét/címét – ez pont az a belső azonosító, amiről a `pressure_sensor.yaml` fejléce is kimondja, hogy sosem szabadna megjelennie. **Javítva**: a megerősítő szöveg a Display Name-et mutatja, üres név esetén a Modbus címre esik vissza.
    - **Törlés után az új első sor kuka ikonja "beragadt" piros hover-állapotban**, amíg a felhasználó máshova nem kattintott/mozgatta az egeret – valódi böngésző-viselkedés, nem alkalmazáslogikai hiba: a törlést megerősítő natív `confirm()` dialógus blokkol, tehát a törlést kiváltó kattintás sosem járt azzal, hogy az egér ténylegesen a törlés utáni *új* első sor fölé mozdult volna – a böngésző mégis `:hover`-ként kezeli, mert az egérmutató pozíció szerint épp ott áll. **Javítva**: minden sor-eltávolítás után egy rövid életű CSS-osztály elnyomja a hover-színeket a táblán, amíg az egér ténylegesen nem mozdul.
    - **Kérdés: mennyire gyors a szenzor saját belső mérése?** A gyártói adatlap (`project-docs/docs/hardver/qdw90a-nyomastavado-adatlap.pdf`, "4.1 Basic Electrical Parameters") explicit megadja: **Response time: ≤10 ms** – ez a szenzor-mag (diffúziós szilícium piezorezisztív elem) saját analóg válaszideje, tehát az elméleti fizikai/analóg korlát jóval a jelenlegi (`600-670ms`) pollozási intervallumunk alatt van, nem az szűkíti a lehetőségeinket. A digitális (Modbus regiszter-frissítési) oldalra nincs explicit szám az adatlapban, de mivel ugyanaz a mérőmag van a 4-20mA és a Modbus kimenetű változat mögött is, ésszerű feltételezés, hogy a beépített mikrovezérlő saját frissítési ciklusa (ADC kiolvasás + regiszterírás) ennél nem sokkal lassabb – iparágban szokásos, hasonló árkategóriájú Modbus nyomástávadóknál ez tipikusan 10-100Hz (10-100ms) tartományban szokott lenni, becsült érték, nem ebből az adatlapból. Összegezve: a szenzor saját belső mérése minden valószínűség szerint **nem** a szűk keresztmetszet – a megosztott, félduplex RS485 busz és a 8 slot közti időosztás az.
  - **Tizedik kör, felhasználói visszajelzésre, 2026-08-13 (ua. nap)**:
    - **Üres név esetén a belső "Pressure Sensor N" névre esett vissza a Dashboard felirat** – a Hozzáadás gomb már korábban is megkövetelte a nevet (ld. "Hozzáadás név nélkül" fentebb), de a **szerkesztés** (ceruza → Mentés) útján egy már regisztrált slot neve továbbra is kiüríthető volt – egy üres Display Name mellett `groupLabel()` (a Dashboard-kártya és a Service-fejlécek közös segédfüggvénye) egyenesen a csoport nyers, belső azonosítójára esik vissza, pont arra a szándékosan jelentés nélküli névre, amit ez a felület mindenhol máshol gondosan elrejt. **Javítva**: a Mentés gomb is megköveteli a nem üres nevet, ugyanúgy, mint a Hozzáadás.
    - **Megjegyzett jövőbeli terv: eszköztípus (device class) alapú névjavaslat** – ld. külön "Eszköztípus (device class) – tervezett bővítés" alfejezet lentebb.
  - **Tizenegyedik kör, felhasználói visszajelzésre, 2026-08-13 (ua. nap)**:
    - **A belső "Pressure Sensor N" felirat még mindig felvillant a Dashboardon, most már friss, nem üres nevű regisztrációnál is** – a valódi ok: friss kapcsolódás/újracsatlakozás esetén az entitások nem egyszerre, hanem fokozatosan érkeznek (ESPHome egy fix, keresztmetszet-doménenkénti sorrendben dumpolja őket, nem a regisztráció sorrendjében) – a Modbus Address (number domén) simán megérkezhet a Display Name (text domén) előtt, ami alatt a kártya fejléce a `groupLabel()` régi visszaesési logikája miatt a nyers, belső csoportnevet ("Pressure Sensor N") mutatta, amíg a valódi név utol nem érte. **Explicit felhasználói kérés**: inkább semmit ne mutasson, amíg a valódi nevet nem ismerjük biztosan. **Javítva**: `groupLabel()` mostantól nyomásmérő csoportoknál sosem esik vissza a nyers névre – amíg a valódi Display Name nem ismert, üres string jelenik meg (csak az ikon látszik), a meglévő "settle" mechanizmus pedig ugyanúgy egyben korrigálja a fejlécet, amint a kezdeti adatcsomag lezárul.
    - **A `try_register` üres név esetén a belső azonosítót ("${friendly_name}") adta a szenzornak, ténylegesen elmentve azt Display Name-ként** – ez volt az az egyetlen hely, ahol a belső név ténylegesen, tartósan Display Name-mé válhatott (a dashboard Hozzáadás/Mentés gombjai már megkövetelik a nevet, de a nyers API-n keresztül ez a védőháló még mindig elérhető maradt). **Javítva, a felhasználó kifejezett kérésére ("ilyen szövegnek már nem is szabadna maradnia a kódban")**: a script mostantól egyszerűen nem regisztrál üres névvel (nem helyettesíti be a belső azonosítót).
    - **"Water Meter N" ugyanez a probléma, más doménben** – a felhasználó megerősítette, hogy ugyanez a jelenség (nyers belső név villanása) a vízmérőknél is fennáll, de kérésére **nem** kapott önálló, ideiglenes javítást – lásd a következő pontot, ami architekturálisan úgyis megoldja majd, nem érdemes duplán dolgozni rajta.
    - **Megjegyzett, nagyobb jövőbeli architekturális terv: a "Pulse meter" (jelenlegi vízmérő) szekció átalakítása a nyomásmérőkkel megegyező, dinamikus 0-2 elemű minta szerint** – a felhasználó explicit kérése: ugyanaz a megjelenés/vezérlés-logika (Hozzáadás/Törlés/szerkesztés-zár/Sort Order stb.), mint a Modbus eszközöknél, hogy a karbantartás egyszerűsödjön és a rendszer átláthatóbb legyen. Fontos különbség, amit végig kell gondolni implementáció előtt: a vízmérők nem Modbus-buszon felfedezhető eszközök, hanem fixen bekötött GPIO-pinek (`meter1_pulse_pin`/`meter2_pulse_pin`) - nincs "Scan Bus" megfelelőjük, a "slot" itt egy fizikai GPIO, nem egy busz-cím. Nyitott kérdések, amiket implementáció előtt tisztázni kell: mit jelentsen a "törlés" egy fizikailag bekötött GPIO-n (csak a megjelenítést/nyilvántartást törli, vagy a pulzusszámlálást is leállítja?), mi történjen a már felhalmozott "Összes fogyasztás" adattal törléskor, és hogyan képződjön le a "Scan"-hez hasonló felfedezési lépés egy olyan doménre, ahol nincs mit felfedezni (a 2 GPIO fixen adott). **Státusz**: **implementálva, 2026-08-13, ua. nap** - ld. "Tizenkettedik kör" alább a végleges válaszokkal és a megvalósítás részleteivel.
  - **Tizenkettedik kör: a "Pulse Meters" egységesítés megvalósítása, 2026-08-13 (ua. nap)**:
    - **Nyitott kérdések, tisztázva a felhasználóval, implementáció előtt**:
      1. **Törlés jelentése**: csak elrejti/kikapcsolja - a pulzusszámlálás és a felhalmozott Összes fogyasztás a háttérben tovább fut/megmarad, semmilyen adat nem vész el.
      2. **Hozzáadás UX, felfedezés nélkül**: mindkét fizikai GPIO automatikusan "Új eszköz" sorként jelenik meg, amíg nincs regisztrálva - nincs "Scan Bus" megfelelője, nincs rá szükség (a 2 lehetséges meter fixen adott, fordításidőben ismert).
      3. **Kalibrációs mezők láthatósága**: teljes egységesítés - a Reading/Update/Zero-Flow Timeout (és a Total Pulses diagnosztikai mező) is csak Hozzáadás után jelenik meg, pontosan úgy, mint a Modbus eszközök bármely attribútuma.
    - **Firmware (`packages/water_meter.yaml`)**: az eddigi "Show on Dashboard" kapcsoló (aminek eddig kizárólag a Dashboard-kártya láthatóságára volt hatása) átnevezve/újraértelmezve **"Registered"**-re - mostantól ez az egyetlen igazságforrás arra, hogy a meter commissionálva van-e, ugyanúgy, ahogy egy nyomásmérő slotnál a Modbus Address `!= 0`. Új **"Delete"** gomb hozzáadva (`switch.turn_off` a Registered kapcsolóra - nem nullázza a Display Name-et, mert egy vízmérő GPIO-ja mindig ugyanaz a fizikai mérő marad, nincs "másik eszköz veheti át a helyét" bizonytalanság, szemben egy nyomásmérő címmel). Minden más entitás (Total Consumption, Flow Rate, Reading, Update, Zero-Flow Timeout, Total Pulses) változatlan - továbbra is mindig lefordítva/elküldve, csak a *megjelenítés* feltételes (ugyanaz az "existence model", mint a nyomásmérőknél).
      - **Biztonsági megfontolás, első verzió (később felülbírálva, ld. "Tizenharmadik kör")**: a `restore_mode`-ot ekkor **szándékosan NEM** változtattam `RESTORE_DEFAULT_OFF`-ra, mivel az átnevezés (`name:` mező) megváltoztathatja az ESPHome saját, entitásnév-alapú restore-kulcsát, és nem volt bizonyosság hogy ez nem törli-e érvénytelenné a már bekapcsolt eszköz meglévő, flash-ben tárolt "bekapcsolva" állapotát. `RESTORE_DEFAULT_ON` maradt ekkor.
    - **`water-collector.yaml`**: új `sorting_group_pulse_meters` ("Pulse Meters") umbrella csoport, a táblázat horgonypontja - nincs hozzá saját megosztott entitás (nincs rá szükség, ld. lent). A `sorting_group_meter1`/`sorting_group_meter2` neve (és a `friendly_name`) ekkor még szándékosan "Water Meter 1/2" maradt (később átnevezve, ld. "Tizenharmadik kör").
    - **`web/dashboard.js`**: új "Pulse meter table" szekció, ami ugyanazt az Add/Delete/szerkesztés-zár mintát adja, mint a nyomásmérő táblázat, de busz/cím/scan/collision/sorrend nélkül (ezek a fogalmak nem értelmezhetők egy fix GPIO-ra) - Hozzáadás egyetlen, per-sor, közvetlen 2-lépéses fetch-csel (Display Name beállítása, majd Registered bekapcsolása), nincs szükség a nyomásmérőknél muszáj megosztott "claim" logikára, mivel itt nincs cím-egyértelműség, minden sor eleve tudja melyik fizikai meterhez tartozik. A táblázat a nyomásmérő táblázat CSS osztályait (icon-btn, row-editing, stb.) újrahasznosítja - ezek doménfüggetlenek a névük ellenére, saját stílus-duplikálás helyett.
      - **Fontos, menet közben talált és javított buborékoló hiba**: mivel ESPHome az entitásokat fix, keresztmetszet-doménenkénti sorrendben dumpolja (nem regisztráció sorrendjében), a "Registered" (switch domén) nem garantáltan érkezik a "Total Consumption" (sensor domén) előtt - enélkül egy korábban érkezett, de akkor még "nem regisztrált"-ként kihagyott entitás soha többé nem frissült volna, amíg a saját, független következő eseménye meg nem érkezik (pl. Total Consumption csak a következő impulzusra). **Javítva**: amint "Registered" ismertté válik, a rendszer minden más, már ismert entitást a csoportban újra végigfuttat a megjelenítési logikán, hogy semmi ne ragadjon be "kihagyva" állapotban.
      - **Szintén menet közben talált és javított hiba**: az entitás DOM-referenciák (`.el`/`.inputEl`/`.readoutEl`/`.toggleEl`/`.btnEl`) törlés (un-registration) esetén explicit nullázódnak minden, az adott csoporthoz tartozó entitáson - ugyanaz a hiba-osztály, amit korábban a nyomásmérő Dashboard-kártyájánál találtunk és javítottunk (egy leválasztott, láthatatlan DOM-node végtelenített frissítése újra-regisztráláskor), csak itt kiterjesztve minden gyorsítótárazott referenciatípusra, mivel egy vízmérőnek több mezője/oldala van, mint egy nyomásmérő slotnak.
      - **Elhagyott, halott kódrészek takarítása**: a régi "Show on Dashboard"-specifikus `groupEnabled`/`applyGroupVisibility` logika mostantól sehonnan nem táplálódik (megtartva, mint általános, jelenleg parlagon heverő infrastruktúra egy esetleges jövőbeli hasonló igényre), a hozzá tartozó elavult `HELP_TEXT` bejegyzés törölve.
    - **`web/dashboard.css`**: `.dc-pulsemeter-group` (`max-width: 480px` - jóval keskenyebb, mint a nyomásmérő táblázat 900px-je, mivel csak Name+Action oszlopa van, nincs Address/Status/Order).
  - **Tizenharmadik kör, felhasználói visszajelzésre, 2026-08-13 (ua. nap)**: az előző kör két, óvatosságból meghozott döntését a felhasználó explicit felülbírálta, azzal az indoklással, hogy **még nagyon DEV fázisban vagyunk, nincs PROD, nincs HA integráció** - most (és csak most) érdemes elvégezni minden olyan átalakítást, ami később, egy éles/publikált rendszeren már fájdalmas volna. Mindkettő megváltoztatva:
    - **`friendly_name`/`sorting_groups` átnevezve "Water Meter N"-ről "Pulse Meter N"-re** (`water-collector.yaml`, `packages/water_meter.yaml`, `web/dashboard.js`'s `GROUP_ICON_BY_NAME`/`PULSE_METER_RE`) - ez a Home Assistant-ban generálandó entity_id-kat is megváltoztatná, de mivel jelenleg nincs HA integráció, ez most nem jár valódi kockázattal, csak egy pusztán kozmetikai, korábban indokolatlanul halogatott átnevezés. Az `id_prefix` (`meter1`/`meter2`) változatlan maradt - ennek átnevezése ténylegesen elveszítené a meglévő kalibrációs eltolást (`offset_m3`, flash-preference kulcsban), ez egy valódi, ha kicsi és helyrehozható (egy manuális ismételt szinkron) költség, HA-tól függetlenül is, ezért ezt továbbra sem érte meg átnevezni.
    - **`restore_mode`: `RESTORE_DEFAULT_ON` → `RESTORE_DEFAULT_OFF`** a Registered kapcsolón - egy vadonatúj eszköz mostantól valóban "nem regisztrált" állapotban indul, konzisztensen a nyomásmérő slotokkal. Ez a jelenleg futó eszközön a következő flashelés után **elméletileg** visszaállíthatja mindkét meter Registered állapotát kikapcsoltra (ha az átnevezés ténylegesen érvényteleníti az ESPHome saját restore-kulcsát) - ezt a kockázatot most tudatosan vállaltuk (a felhasználó explicit kérésére): ha bekövetkezik, a következmény pusztán annyi, hogy mindkét metert egyszer újra hozzá kell adni a táblázatban (semmilyen adat - `pulse_count`, `offset_m3`, azaz a felhalmozott fogyasztás - nem vész el, azok teljesen más, `id_prefix`-alapú preference-kulcsot használnak).
  - **Tizennegyedik kör, felhasználói visszajelzésre, 2026-08-13 (ua. nap)** - Modbus:
    - **"Lost" villant fel új Modbus eszköz Hozzáadásakor is (nem csak régin)** – gyökérok megtalálva: a Delete gomb explicit `false`-ra állítja az "Online" bináris szenzort (mivel regisztrálatlan állapotban ez az érték amúgy sem látszik/számít sehol), de ha ugyanaz a slot **később újra** felvesz egy eszközt, ez a régi, elavult `false` érték ott marad, amíg az első valódi pollozás (akár `~670ms`-ig) le nem futna. **Javítva**: `try_register` sikeres regisztráláskor azonnal, optimistán publikálja `Online: true`-ra – ez indokolt, mivel Hozzáadás csak olyan címre történhet, amit a busz-scan épp az imént talált válaszolónak; ha ez a feltételezés téved, a következő valódi pollozás úgyis korrigálja.
    - **A Service oldali Sort Order-nek (Fel/Le rendezés) nem volt hatása a Dashboard oldalra** – a Dashboard-kártyák sorrendje eddig a fordításidőben rögzített slot-súlyból jött (mindig slot-sorszám szerint), sosem a táblázat saját, dinamikus Sort Order-jéből. **Javítva**: `syncPressureHomeCard()` minden híváskor újraszámolja és felülírja az érintett kártya súlyát a Service tábla saját (Sort Order-alapú) sorrendje szerint, majd újrarendezi a Dashboard kártyákat, ha bármi változott.
  - **Tizenötödik kör, felhasználói visszajelzésre, 2026-08-13 (ua. nap)** - Pulse meters:
    - **A táblázat szélessége igazítva a Modbus táblázathoz** (`480px` → `900px`) – direkt kérésre a két commissioning-tábla egységesen ugyanakkora, nem a saját aktuális oszlopszámukhoz méretezi magát mindegyik.
    - **"Meter name" → "Device name"** (a nyomásmérő táblázat korábbi "Sensor name" placeholder-e is átállítva ugyanerre, a konzisztencia kedvéért).
    - **Nagyobb architekturális átalakítás: az attribútumok (Reading, Zero-Flow Timeout) a táblázat saját sorába költöztek** – korábban ezek egy mindig-látható, külön Service-szekcióban jelentek meg (6 sor 2 valódi mezőre), most a megfelelő táblázatsor alatt nyílnak ki, kizárólag amíg az a sor szerkesztés módban van (ugyanaz a ceruza nyitja, mint a Name mezőt, Pipa/X zárja) - a felhasználó explicit kérése, azzal a megjegyzéssel hogy ha bevállik, a Modbus eszközöknél is átvesszük majd ezt a mintát. Kompakt, egy-egy sor: "Reading" label + beviteli mező + Update gomb egy sorban; "Zero-Flow Timeout" label + beviteli mező egy sorban. **Csak egy tábla-sor szerkeszthető egyszerre az egész táblázatban** - egy másik sor szerkesztésbe nyitása a korábban nyitottat automatikusan menti-eldobja (Mégse-ekvivalens) és becsukja, mielőtt az újat megnyitná. Az összetartozás vizuálisan egyértelmű: a kinyíló sor ugyanazt az akcentszínű háttéret kapja, mint maga a szerkesztett sor, és a köztük lévő szegély el van tüntetve (folytonos, összetartozó blokknak látszik).
      - **Ez a redesign egyúttal javította a "csak page reload után jelennek meg a mezők" jelenséget is** (menet közben talált, valós hiba: mivel a Reading/Update/Zero-Flow Timeout entitások nem garantáltan a "Registered" kapcsoló előtt/után érkeznek ESPHome fix, keresztmetszet-doménenkénti dump-sorrendje miatt, egy korábban érkezett, de akkor még "nem regisztrált"-ként kihagyott mező korábban soha többé nem frissült élőben, csak egy teljes újratöltésre). A dinamikus élő-frissítés logikáját is megerősítettük: mostantól **minden** entitás-frissítés a csoportban újra végigfuttatja az összes ismert mezőt a megjelenítési logikán (nem csak a "Registered"/"Delete"/"Display Name" változásokkor), ugyanaz a "mindig újraszinkronizál" elv, amit a nyomásmérő Dashboard-kártyája is követ.
    - **Nyitott, tisztázatlan pontok, amikhez visszajelzés/screenshot kell**: (1) Enter/Esc a szerkesztő mezőkben a kódvizsgálat szerint helyesen van bekötve (ugyanaz a minta, mint a Modbus táblázatnál, ami működik) - nem találtam benne hibát, de mivel a teljes szerkesztési UX ebben a körben jelentősen átalakult, újra le kell tesztelni; (2) egy külön látható "Registered" gomb/kapcsoló, amit a felhasználó a kuka mellett lát - a kódvizsgálat szerint a "Registered" entitás sehol nem juthat el egy önálló, látható vezérlőelemig (mindenhol el van fogva, mielőtt az általános renderelési útra kerülne) - vagy egy elavult build tesztelése történt, vagy screenshot kell a pontos beazonosításhoz.
  - **Tizenhatodik kör, felhasználói kezdeményezésre ("Mit gondolsz?"), 2026-08-13 (ua. nap) - a két táblázat egyesítése egy közös "Devices" táblázattá**:
    - **Egy közös táblázat mindkét eszköztípusnak** - Elnevezés/Státusz/Edit/Delete/Order oszlopok, mindkét típusra érvényesen. A korábbi külön Address oszlop megszűnt: Modbus soroknál a cím a kinyíló szerkesztő-sávba költözött (ugyanaz a minta, mint a Reading/Zero-Flow Timeout a pulse metereknél - "Tizenötödik kör"), szerkesztéskor csak Név+Cím módosítható, ahogy kérve volt. Egy szkennelt-de-még-nem-regisztrált cím vagy egy ütköző cím esetén a cím most egy kis, halványított "Address N" feliratként jelenik meg a Név cella tetején.
    - **Egy közös "Scan Bus" + "Add" eszköztár** a táblázat felett. A Scan Bus pontosan úgy működik, mint eddig (Modbus-specifikus, a szkennelt sorok saját, egykattintásos Add gombjukkal maradtak). Az "Add" egy kliens-oldali, entitás nélküli kapcsoló, ami egy beépített táblázatsort nyit/csuk - típusválasztóval (Modbus / Pulse Meter), ami a választás szerint a megfelelő mezőket mutatja: Modbus esetén Név+Cím (kézzel beírt cím, ld. lentebb), Pulse Meter esetén Név+GPIO-slot választó (csak a még szabad "Pulse Meter N" slotok közül; ha mindkettő foglalt, a Pulse Meter opció letiltva a típusválasztóban).
    - **Kézzel beírt Modbus cím - próba-lekérdezéssel védve**: mivel ez nem szkennelésből jön (nincs előzetes megerősítés, hogy ott tényleg válaszol valami), a megosztott "Pressure Sensors Add" gomb (`water-collector.yaml`) mostantól a regisztrálás előtt egyszer megpróbálja a célcímet (`rs485_modbus::probe()`, ugyanaz a mechanizmus, amit a `scan_bus()` is használ) - csak tiszta `FOUND` esetén regisztrál. Egy új "Pressure Sensors Add Result" diagnosztikai szöveg (`ok`/`no_response`) viszi vissza az eredményt a dashboardnak, ami hiba esetén inline hibaüzenetet mutat a beviteli mezők mellett (nem natív `alert()`-tel, konzisztensen a fájl többi részével). A szkennelt sorok saját Add gombja ugyanezen az úton megy át (ugyanaz a `pressure_add` gomb), ártalmatlanul redundánsan újra-próbálva egy már az imént megerősített címet.
    - **Az Order (Sort Order) mostantól típusok között KÖZÖS** - explicit felhasználói kérés, mivel ez határozza meg a Dashboard sorrendjét is. A korábban `next_pressure_sort_order` néven csak a nyomásmérőket kiszolgáló, monoton növekvő számláló (`water-collector.yaml`) átnevezve `next_device_sort_order`-re, és mindkét eszköztípus try_register/Add-folyamata ugyanabból veszi a következő értéket - egy friss Add, bármelyik típusé, mindig a lista aljára kerül, típustól függetlenül. A pulse meterek is kaptak egy saját "Sort Order" number entitást (`water_meter.yaml`, ugyanaz a minta, mint a nyomásmérőknél) + Fel/Le gombot a táblázatban. A Dashboard (Home oldal) kártyasorrendje is ugyanezt a közös sorrendet követi mostantól (`resyncDeviceHomeCardOrder()`, a korábbi, csak-nyomásmérős `resyncPressureHomeCardOrder()` általánosítása) - korábban ez csak a nyomásmérőknél volt igaz.
    - **GPIO-slot választás Pulse Meter Add-nál**: egyelőre nincs általános "engedélyezhető GPIO-k" beállítóképernyő (ez egy megjegyzett, jövőbeli ötlet - ld. lent), csak annyi, hogy a jelenlegi 2 fix slot (`Pulse Meter 1`/`2`, GPIO1/GPIO2) közül a még szabad(ak) közül lehet választani a hozzáadáskor; ha mindkettő foglalt, a Pulse Meter típus nem választható.
    - **Pulse meter státuszjelzés finomítva**: a korábbi, nyomásmérőktől kölcsönzött OK/Lost-szerű jelzés helyett egy egyszerű, kétállapotú "Ready" (zöld pötty, mindig ez amíg regisztrálva van - egy helyi GPIO-impulzusszámlálónak nincs "elérhetetlen" állapota) + egy rövid, ~0,7 mp-es "gyűrű" felvillanás (`flashPulseMeterActivity()`, CSS `@keyframes dc-pulse-ring`) minden alkalommal, amikor egy valódi impulzus ténylegesen megérkezik (a "Total Pulses" diagnosztikai szenzor - a perzisztens `pulse_count` nyers leolvasása - változásán figyelve, ami csakis valódi impulzusra frissül). A felhasználó jelezte, hogy ezt még tovább finomítjuk később, hogy "szépebb és informatívabb" legyen - ez az első, funkcionális változat.
    - **Nyitva hagyott, tudatosan el nem végzett finomítás**: egy szkennelt (nem kézzel beírt) sor Add gombja nem kap explicit hiba-visszajelzést, ha a cím a szkennelés óta megszűnt válaszolni (a próba ugyanúgy lefut, csak a dashboard nem figyeli külön ennek az eredményét azon az úton) - a sor egyszerűen nem jelenik meg regisztráltként, ugyanúgy, mint a funkció bevezetése előtt volt, tehát ez nem regresszió, csak egy nem-továbbfejlesztett rész.
  - **Tizenhetedik kör, felhasználói visszajelzésre, 2026-08-13 (ua. nap) - a "Devices" táblázat finomítása**:
    - **Eszköztár a táblázat aljára költözött, Scan Bus és Add felcserélve**: korábban a táblázat FELETT volt egy "Scan Bus" + "Add" eszköztár; mostantól a táblázat ALATT van, és a sorrend is megfordult: Add-jellegű gombok elöl, Scan Bus utánuk.
    - **Egy "Add" gomb + típusválasztó helyett két önálló gomb**: "Add Pulse Meter" és "Add Modbus Device" - felhasználói felvetésre ("lehet jobb lenne a legördülő helyett?"), mert mindkét gomb önállóan tudja jelezni/tiltani magát (pl. "Add Pulse Meter" letiltva, ha mindkét GPIO-slot foglalt), és egyértelműbb elsőre, mint egy generikus "Add" + egy azt követő legördülő. Bármelyik megnyomása bezárja az esetlegesen nyitva lévő szerkesztés-módú sort (Mégse-ekvivalens), ugyanúgy, ahogy egy szerkesztés megnyitása is bezárja a nyitva lévő Add-sort - csak egy interaktív dolog lehet nyitva egyszerre, a táblázat egészében.
    - **A beépített Add-sor "Add" szövegű gombja helyett Pipa/X ikon**: vizuálisan konzisztensen a táblázat többi Mentés/Mégse ikonjával, nem egy külön kinéző, önálló gombként.
    - **Busz-szkennelés utáni "New device" sor Add gombja is Pipa/X-re cserélve**: a Pipa ugyanazt a regisztrációs folyamatot indítja, mint eddig; az X viszont NEM a szervert hívja, hanem csak ennyi a munkamenetre "elrejti" azt a címet (a következő valódi Scan Bus lenyomásakor törlődik ez a lista) - lásd a következő pontot, ez ugyanabból az elvből következik.
    - **Szkennelés eredménye mostantól munkamenet-helyi, nem perzisztens** - explicit felhasználói kérés/észrevétel: "az logikus, ha kilépek (vagy page refresh) akkor eltűnjenek [a New device sorok], ne okozzon perzisztens műveletet, ha csak szkeneltem a buszt". A "Scan Results"/"Scan Collisions" szenzorok maguk továbbra is a firmware-ben élnek (nem törlődnek), de a dashboard client-oldalon mostantól csak azokat az értékeiket veszi figyelembe "New device"/"Collision" sor építésére, amik a kezdeti SSE-dump (kapcsolódáskor mindig lezajló, teljes állapot-átküldés) LEZÁRULÁSA UTÁN érkeznek (a már meglévő `initialSettled` flag felhasználásával, amit korábban csak a csoport-láthatóság/-címke logika használt) - egy kapcsolódáskor kapott, már réginek számító szkennelési eredmény emiatt sosem villan fel "New device" sorként, csakis egy ezután ténylegesen megnyomott Scan Bus (akár ettől, akár egy másik klienstől) hozza létre újra a sorokat. Egy oldal-frissítés emiatt automatikusan "elfelejti" mind a régi szkennelési eredményt, mind bármely korábbi X-szel elutasított címet (ami amúgy is csak egy JS-memóriában élő `Set`, ami frissítéskor eltűnik).
    - **Típus-ikon minden sor elején** (mérőóra-ikon = Modbus nyomásmérő, csepp-ikon = pulse meter, ugyanazok az ikonok, amiket a Dashboard oldal kártya-fejlécei is használnak) - mivel a táblázat mostantól mindkét típust vegyesen listázza, egy átnevezett eszköz neve önmagában már nem árulja el a típusát.

### Eszköztípus (device class) – tervezett bővítés (megjegyezve, 2026-08-13, még nem implementálva)

Amikor a rendszer a nyomásmérőn túl más Modbus eszköztípusokat is támogat (pl. áramlásmérő), egy **device class** entitást kell bevezetni slotonként (a `try_register`/scan folyamat már ma is eszköztípus-függetlenül van elnevezve - ld. `modbus_add_claimed` saját megjegyzése -, ez a következő lépés lenne rá építve). A felhasználó explicit kérése: a driver/eszköztípus-felismerés határozza meg a class-t, és **ebből** ajánljon fel a felhasználónak név-sablont a Hozzáadás előtt (pl. "Pressure"/"Flow Meter", a ténylegesen elérhető class-ok közül, egymástól megkülönböztethetően), a Modbus címmel együtt/mellett - nem egy általános, típus-független "Sensor name" placeholder, ahogy ma. Ez közvetlenül kapcsolódik a "Service oldal szélesség-reszponzivitása" (fenti "Nyolcadik kör") alatt már megjegyzett, ugyanerre az alkalomra elhalasztott tábla-oszlop kérdéshez is. A "Tizenhatodik kör" közös "Devices" táblázatának típusválasztója (Modbus / Pulse Meter) már most is ebbe az irányba mutat, de ez egyelőre kézzel, nem device-class alapon van elágaztatva.

### GPIO-bővítés – tervezett, elhalasztott ötlet (megjegyezve, 2026-08-13, "Tizenhatodik kör")

A jelenlegi hardver (`docs/hardver/esp32-s3-rs485-can-board.md`) csak a dedikált SH1.0 csatlakozón kivezetett GPIO1/GPIO2-t huzalozza impulzusszámlálásra; a szélesebb, 2,0 mm-es pin header (IO3-IO14, IO19/20) elvben alkalmas lenne további impulzusszámlálókra, de ehhez a felhasználó egy jövőbeli "setup képernyőt" képzel el, ahol további GPIO-k engedélyezhetők/kapcsolhatók - ez **egyelőre nincs implementálva**, csak megjegyezve. Az ESPHome beépített `pulse_meter` platformja fordításidőben köti a GPIO-t; futásidejű GPIO-váltás csak egyedi C++ kóddal volna megvalósítható (hasonlóan ahhoz, ahogy a projekt saját, kézzel írt Modbus-mestert használ a `modbus_controller:` helyett).

### Busz-szkennelés és cím-váltás (2026-08-13: implementálva, valós Modbus-szal)

- **Busz-szkennelős betanítás**: a busz automatikus végigpásztázása (`1–247` cím, rövid `~25ms`/cím timeout-tal), hogy listázza, mely Modbus-címek elérhetők, de még nincsenek felvéve a mi listánkba – **implementálva** (`include/rs485_modbus.h`'s `scan_bus()`, a "Pressure Sensors Scan Bus" gomb, `water-collector.yaml`). A cím írása/mentése/olvasása a `docs/hardver/qdw90a-modbus-referencia.md`-ban dokumentált, gyártói hivatalos Modbus-doksi alapján megerősített eljárást követi.
- **Cím-váltás a már bekötött eszközön**: a pontos, hardveren megerősített eljárás – **implementálva** (`include/rs485_modbus.h`'s `change_address_and_save()`, `pressure_sensor.yaml`'s Modbus Address `set_action`) – **fontos részlet**: ez **nem egy Modbus-művelet**, a cím-írás válasza még a régi címről jön, a mentést (`H:15=0`) már az **új** címre kell küldeni; ezt a kétlépéses logikát a firmware kezeli, végén egy read-back-kel ellenőrizve.
- **Ütközés kimutatása – két különböző dolog, nem szabad összekeverni**:
  - **"Ugyanaz a cím szerepel kétszer a mi listánkban"** – ez tiszta szoftver, **implementálva van** (a táblázat Address mezőjének módosításakor kliens-oldali figyelmeztetés jön, ha a beírt cím már foglalt egy másik, általunk már betanított slotnál – csak figyelmeztet, nem tilt, hiszen ez nem a fizikai buszt ellenőrzi).
  - **Valódi elektromos/protokoll-szintű ütközés** (két fizikai eszköz tényleg ugyanazon a címen válaszol egyszerre) – **pontosítva, 2026-08-13, valós hardveren tesztelve**: a korábbi feltételezés ("megkülönböztethetetlen attól, mintha senki nem válaszolt volna") túl pesszimista volt. Szándékosan 3 eszközt azonos (`1`) címre állítva a busz-scan **nem** timeoutot kapott, hanem egy ténylegesen megérkezett, de CRC-hibás választ (`include/rs485_modbus.h`'s `scan_bus()`/`probe()` most ezt a két esetet külön kezeli: `NO_RESPONSE` vs. `COLLISION`) – tehát a "valami válaszolt, csak korrupt" **igenis megkülönböztethető** a "tényleg senki nem válaszolt"-tól, méréssel megerősítve. Ez **nem bizonyíték** ütközésre (elvben sima buszzaj is okozhatná), de **erős, gyakorlatban használható jelzés** – a dashboard táblázatban külön "Collision?" státuszként jelenik meg (mind regisztrált slotnál, mind egy még nem regisztrált címnél). **Élő, folyamatos ütközés-figyelés** (a slotonkénti pollozásra is kiterjesztve, nem csak a scan-re) **implementálva**, ld. fent "Negyedik kör".

### Kalibráció

- A nyers regiszterérték (`H:4`) `bar`, 2 tizedesjegy skálázással érkezik (`/100`) – **hardveren és a gyártó hivatalos Modbus-doksijával is megerősítve** (`docs/hardver/qdw90a-modbus-referencia.md`), külön finomskálázás nem szükséges.
- **Nyitott kérdés lezárva**: kell-e finomkalibrációs eltolás? **Igen, és ez már be van építve magába az eszközbe** – a `H:12` (nullponteltolás) regiszter írható, `kimeneti nyomás = kalibrált mérés + H:12` képlettel (gyártói hivatalos doksi, 2026-08-13). **Nem kell saját szoftveres offset-réteget építeni az ESP oldalán** – elég egy UI-mező, ami közvetlenül ezt a regisztert írja (majd `H:15=0`-val menti), a vízmérők "Reading" mezőjéhez hasonló UX-szel.

### Diagnosztika

- Kommunikációs állapot méterenként (Modbus timeout/hiba) – egy busz-osztott szenzor hibája **nem befolyásolhatja** a többi szenzor működését (a busz maga megosztott, de a hibakezelés méterenként független legyen).
- **"Communication OK" – implementálva, 2026-08-13, a tervezettől eltérő úton**: az eredeti terv az ESPHome beépített `modbus_controller` komponensének `on_online`/`on_offline` mechanizmusára épített volna – ez viszont egy **fordításidőben rögzített** slave-címet feltételez controllerenként, ami összeegyeztethetetlen azzal, hogy nálunk slotonként **futásidőben** változhat a cím (ld. "Architekturális megfontolás v3"). Emiatt a `modbus_controller:`-t végül sehol nem használjuk – helyette saját, kézzel írt Modbus-mester (`include/rs485_modbus.h`), és a "Communication OK" jelzést egy slotonkénti "Online" `binary_sensor` adja (`pressure_sensor.yaml`), amit a nyomás-pollozás lambdája frissít minden sikeres/sikertelen olvasás után.
  - Nincs `max_cmd_retries`-szerű "N sikertelen próba után offline" logika – minden egyes `5s`-enkénti pollozás önmagában dönt, egyetlen kimaradt válasz azonnal "Lost"-ra váltja a slotot, a következő sikeres pollozás pedig azonnal vissza "OK"-ra. Ez egyszerűbb, mint a tervezett retry-alapú logika, és eddig elegendőnek tűnik – ha a gyakorlatban túl "villódzónak" bizonyul (átmeneti zajra túl érzékeny), egy egyszerű "N egymást követő hiba után Lost" számláló utólag hozzáadható.
  - **Érték-tartomány elleni külön védelem (`0–10 bar`) egyelőre nincs beépítve** – a pollozás jelenleg a Modbus-kommunikáció sikerességét tekinti "OK"-nak, a leolvasott értéket magát nem validálja tartomány szerint. Nyitva hagyott finomítás, nem blokkoló.
- **Gyors, teljes busz-scan diagnosztikai eszköz – implementálva, 2026-08-13**: a "Busz-szkennelés és cím-váltás" szakaszban leírt `scan_bus()` pontosan ezt a szerepet is betölti (jelzi pl. "3 eszköz van bekötve, csak 2 cím válaszol" eltérést) – a teljes `1–247` Modbus-tartományt pásztázza (nem csak `1–32`-t, ahogy eredetileg tervezve volt, mivel ez a korlát nem tartotta vissza a megvalósítást). **Egy halott eszköz és egy cím-ütközés mostantól megkülönböztethető** (ld. fent "Ütközés kimutatása") – **nem** tudja viszont megmondani, hány/melyik eszköz ütközik egy adott címen, csak azt, hogy ott valami korrupt választ ad.
- **Watchdog-crash a busz-scan alatt, valós hardveren reprodukálva és javítva, 2026-08-13**: egy teljes `1–247` scan szinkron módon, egy gombnyomásból fut le (~6mp, ha semmi nem válaszol) – ez alatt sosem tér vissza az ESPHome saját fő ciklusába, ami rendes körülmények között eteti az ESP-IDF task-watchdogot, ezért ~5mp után az eszköz összeomlott/újraindult. Javítás: a várakozó ciklus (`include/rs485_modbus.h`'s `wait_for_bytes()` – minden Modbus-tranzakció mögött ott van, nem csak a scan-nél) explicit `App.feed_wdt()`-et hív minden ciklus-fordulón.
- **Modbus-kommunikáció naplózása – implementálva, 2026-08-13**: a `rs485_modbus.h` minden művelete a `"modbus"` log-tag alatt naplóz. Alapból (a `logger:` `initial_level: DEBUG` beállításával) is látszik a busz-scan kezdete/vége (talált címek listája) és egy regisztrált slot sikertelen pollozása (a "Lost" jelzés oka) – ehhez nem kell semmit bekapcsolni. A teljes vezetékszintű részletezés (minden kiküldött/fogadott Modbus-keret hexában, CRC-hiba/időtúllépés/rossz cím oka soronként) `VERY_VERBOSE` szinten érhető el, amit a Log oldal saját "Debug: Modbus" kapcsolója (`water-collector.yaml`'s `switch:` szekció, `logger.set_level` akció) futásidőben be/ki kapcsol – alapból kikapcsolva, mert egyetlen teljes scan is ~250 sornyi naplót termelne rajta.

### Perzisztencia

- A slave cím (betanítás eredménye) és a Display Name futásidőben, azonnal perzisztálódjon (NVS) – a vízmérők `offset_m3`-ához hasonlóan, nem várja meg a checkpointot.
- A pillanatnyi nyomásérték maga **nem** perzisztens állapot (nincs "felhalmozás", mint a vízmérőknél) – újraindítás után egyszerűen az első sikeres Modbus-olvasás adja az aktuális értéket.

## Kezdeti implementációs döntések

- A `pulse_meter` végzi az impulzusérzékelést, pergésmentesítést és periódusidő-alapú rátamérést. A futásidőben állítható nulla-átfolyás timeout külön ESPHome logikával történik, mert a `pulse_meter.timeout` nem futásidőben konfigurálható.
- Közös logika egy ESPHome csomagban, vízóránkénti példányosítás `substitutions`-szel (`meter1`/`meter2` id-prefix – szándékosan generikus, semmilyen konkrét telepítésre jellemző név (pl. "Fő"/"Locsoló") nincs a kódba égetve; a kódban és fájlnevekben angol terminológia, lásd `water-collector.yaml`). Melyik fizikai mérő melyik, az a dashboard "Display Name" mezőjével, futásidőben állítható be.
- **Checkpoint (`pulse_count`)**: a futó `pulse_count` egy ESPHome `preferences`/`global` állapotban él, minden impulzusnál frissül. A tényleges flash-írást a `flash_write_interval: 60s` korlátozza – ez önmagában **nem** a checkpoint mechanizmusa, csak a fizikai flush gyakoriságát szabályozza, a checkpointot maga a preference-alapú tárolás adja. Ha a `pulse_count` az adott flush-időszak alatt nem változott, ne történjen felesleges fizikai írás – ezt implementáció közben ellenőrizni kell (az ESP-IDF NVS réteg elvben már önmagában kihagyja az azonos érték újraírását, de erre tesztelés nélkül nem szabad vakon támaszkodni).
- **Kézi szinkron (`offset_m3`)**: módosításkor explicit, azonnali preference-sync/`save()` történik, nem várja meg a `flash_write_interval`-t.
- **Nulla-átfolyás timeout megvalósítása**: saját, egyszerű "watchdog" logika – egy `interval:` komponens rendszeresen összeveti az utolsó impulzus időbélyegét a beállított timeout-tal, és lejáratkor explicit 0-ra állítja a Térfogatáram/Impulzusráta szenzorokat.
- **Helyi `web_server_idf` komponens-felülírás** (`components/web_server_idf/`, bekötve `water-collector.yaml`-ban `external_components:`-tel): valós eszközön reprodukált, forrásból (backtrace `addr2line`-nal dekódolva + upstream forrás olvasva) megerősített use-after-free hiba javítása az ESPHome beépített webszerver-komponensében – a "stuck EventSource connection" takarítás korábban a saját C++ objektumot törölte, mielőtt a `httpd` ténylegesen lezárta volna a hozzá tartozó socketet/session-t, ami később (más, ártatlan `free()` hívásnál, jellemzően az lwIP saját housekeeping-jében) heap-korrupcióhoz vezetett. A javítás a részletes indoklással együtt magában a felülírt `web_server_idf.cpp`-ben van dokumentálva. Upstreamben (esphome/esphome, 2026.7.3 és a jelenlegi main is) még nincs javítva; ha ez megtörténik, ez a felülírás (és a teljes `components/web_server_idf/` másolat) törölhető.

## Jelenleg nem követelmény

- Hibaészlelés/riasztás (szivárgás, csőtörés, tartós nulla fogyasztás stb.) – később, HA oldalon kerül definiálásra. A szükséges nyers adat (historizált `Összes fogyasztás`) már rendelkezésre áll ehhez, a jelenlegi adatmodell emiatt nem igényel bővítést. Felbontási korlát: `liters_per_pulse`-nál (1 l) kisebb szivárgás egy impulzusköznyi időn belül nem észlelhető.
- Fizikai/környezeti kialakítás (ház, védettség, tápellátás) – nem szoftverkövetelmény.
- **Nyomásmérő modul**: riasztás/küszöbérték-figyelés (alacsony/magas nyomás, pl. szivattyú-védelem) – ugyanúgy HA oldalon, később, ugyanazon elv szerint, mint a vízmérőknél. Historizáció szintén HA feladata (Scope szerint amúgy is általános elv).
- **Nyomásmérő modul**: finomkalibrációs UI (`H:12` nullponteltolás) – a regiszter írható és a képlet ismert (ld. "Kalibráció"), de nekünk explicit nem szükséges (a gyártói gyári kalibráció elég), ezért egyelőre nincs hozzá felületi elem.
